{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"havn -- Data in safe waters","text":"<p>havn is a self-hosted data platform -- a lightweight, Nordic alternative to Databricks and Snowflake. It uses DuckDB for OLAP analytics, plain SQL for transforms, and Python for ingest/export scripts. All data lives in a single <code>warehouse.duckdb</code> file. No data leaves your machine.</p>"},{"location":"#quick-start","title":"Quick Start","text":"<pre><code>pip install havn\nhavn init my-project\ncd my-project\nhavn jobs run full-refresh\nhavn serve\n</code></pre> <p>Or with Docker:</p> <pre><code>docker run -v $(pwd):/project -p 3000:3000 ghcr.io/chraltro/havn serve\n</code></pre>"},{"location":"#why-havn","title":"Why havn?","text":"havn Databricks dbt + Airflow Snowflake Runs on Your machine Cloud Cloud / self-hosted Cloud Data stays Local Vendor cloud Depends Vendor cloud Setup time 1 minute Hours Hours Hours Cost Free $$$ $ - $$$ $$$ SQL dialect DuckDB (Postgres-compatible) Spark SQL Varies Snowflake SQL Templating None -- plain SQL Jinja Jinja None"},{"location":"#features","title":"Features","text":""},{"location":"#transform-engine","title":"Transform Engine","text":"<p>Plain SQL with <code>@config</code> directives. Dependencies are auto-extracted from <code>FROM</code> and <code>JOIN</code> clauses, so the DAG builds itself. SHA256 content hashing means only changed models rebuild. No Jinja, no templating, just SQL.</p>"},{"location":"#data-quality","title":"Data Quality","text":"<p>Inline assertions (<code>@assert row_count &gt; 0</code>, <code>@assert unique(id)</code>), YAML contracts, column profiling, and freshness monitoring.</p>"},{"location":"#connectors","title":"Connectors","text":"<p>Pre-built connectors for PostgreSQL, MySQL, Stripe, Shopify, Google Sheets, REST APIs, S3/GCS, CSV, and webhooks.</p>"},{"location":"#web-ui","title":"Web UI","text":"<p>React SPA with Monaco code editor, interactive DAG visualization, table browser, query runner, and notebook support.</p>"},{"location":"#ai-native","title":"AI-Native","text":"<p>Ships with <code>CLAUDE.md</code>, <code>.cursorrules</code>, and Copilot instructions. Run <code>havn context</code> to generate an AI-ready project summary.</p>"},{"location":"#single-file-database","title":"Single-File Database","text":"<p>Everything in one <code>warehouse.duckdb</code> file. Back up with <code>cp</code>. No servers, no clusters, no cloud accounts.</p>"},{"location":"#architecture","title":"Architecture","text":"<pre><code>Ingest Scripts (.py / .dpnb)\n        |\n        v\n  +-----------+      +----------+      +----------+      +----------+\n  |  landing  | ---&gt; |  bronze  | ---&gt; |  silver  | ---&gt; |   gold   |\n  |  (raw)    |      | (cleaned)|      | (business|      | (consume)|\n  +-----------+      +----------+      +----------+      +----------+\n        ^                                                       |\n        |                                                       v\n   External Sources                                      Export Scripts\n   (APIs, DBs, files)                                    (reports, APIs)\n</code></pre> <ul> <li>landing -- Raw data ingested from external sources via Python scripts or notebooks</li> <li>bronze -- Light cleanup: column renaming, type casting, deduplication</li> <li>silver -- Business logic: joins, aggregations, calculations</li> <li>gold -- Consumption-ready tables for dashboards, APIs, and reports</li> </ul>"},{"location":"#documentation","title":"Documentation","text":"Category Pages Getting Started Getting Started, Configuration, Environments Core Concepts Transforms, Pipelines, Seeds, Sources Data Integration Connectors, CDC Data Quality Quality, Contracts, Lineage Security Auth, Masking Advanced Scheduler, Notebooks, Versioning Reference CLI Reference, API Reference"},{"location":"api-reference/","title":"API Reference","text":"<p>havn exposes a REST API via FastAPI at <code>http://localhost:3000</code>. All endpoints are prefixed with <code>/api/</code>. When authentication is enabled (<code>havn serve --auth</code>), include an <code>Authorization: Bearer &lt;token&gt;</code> header.</p>"},{"location":"api-reference/#authentication","title":"Authentication","text":""},{"location":"api-reference/#post-apiauthlogin","title":"POST /api/auth/login","text":"<p>Authenticate and receive a token.</p> <pre><code>{\"username\": \"admin\", \"password\": \"your-password\"}\n</code></pre> <p>Returns: <code>{\"token\": \"...\", \"username\": \"admin\"}</code></p>"},{"location":"api-reference/#get-apiauthme","title":"GET /api/auth/me","text":"<p>Get the current authenticated user. Returns username, role, and display name.</p>"},{"location":"api-reference/#get-apiauthstatus","title":"GET /api/auth/status","text":"<p>Check if auth is enabled and whether initial setup is needed.</p> <p>Returns: <code>{\"auth_enabled\": true, \"needs_setup\": false}</code></p>"},{"location":"api-reference/#post-apiauthsetup","title":"POST /api/auth/setup","text":"<p>Create the first admin user. Only works when no users exist.</p> <pre><code>{\"username\": \"admin\", \"password\": \"password\", \"role\": \"admin\"}\n</code></pre>"},{"location":"api-reference/#user-management","title":"User Management","text":"<p>Requires <code>admin</code> role.</p>"},{"location":"api-reference/#get-apiusers","title":"GET /api/users","text":"<p>List all users (no passwords returned).</p>"},{"location":"api-reference/#post-apiusers","title":"POST /api/users","text":"<p>Create a new user.</p> <pre><code>{\"username\": \"analyst\", \"password\": \"pass\", \"role\": \"viewer\", \"display_name\": \"Data Analyst\"}\n</code></pre>"},{"location":"api-reference/#put-apiusersusername","title":"PUT /api/users/{username}","text":"<p>Update user role, password, or display name.</p> <pre><code>{\"role\": \"editor\", \"display_name\": \"Senior Analyst\"}\n</code></pre>"},{"location":"api-reference/#delete-apiusersusername","title":"DELETE /api/users/{username}","text":"<p>Delete a user and revoke all their tokens.</p>"},{"location":"api-reference/#secrets","title":"Secrets","text":"<p>Requires <code>admin</code> role.</p>"},{"location":"api-reference/#get-apisecrets","title":"GET /api/secrets","text":"<p>List secrets (keys and masked values).</p>"},{"location":"api-reference/#post-apisecrets","title":"POST /api/secrets","text":"<p>Set or update a secret.</p> <pre><code>{\"key\": \"DB_PASSWORD\", \"value\": \"new_value\"}\n</code></pre>"},{"location":"api-reference/#delete-apisecretskey","title":"DELETE /api/secrets/{key}","text":"<p>Delete a secret from <code>.env</code>.</p>"},{"location":"api-reference/#files","title":"Files","text":""},{"location":"api-reference/#get-apifiles","title":"GET /api/files","text":"<p>List project files as a tree structure.</p>"},{"location":"api-reference/#get-apifilespath","title":"GET /api/files/{path}","text":"<p>Read a file's content. Returns <code>{path, content, language}</code>.</p>"},{"location":"api-reference/#put-apifilespath","title":"PUT /api/files/{path}","text":"<p>Save or create a file. Allowed extensions: <code>.sql</code>, <code>.py</code>, <code>.yml</code>, <code>.yaml</code>, <code>.dpnb</code>, <code>.sqlfluff</code>.</p> <pre><code>{\"content\": \"SELECT 1\"}\n</code></pre>"},{"location":"api-reference/#delete-apifilespath","title":"DELETE /api/files/{path}","text":"<p>Delete a file. Optional <code>?drop_object=true</code> to also drop the corresponding database object.</p>"},{"location":"api-reference/#query","title":"Query","text":""},{"location":"api-reference/#post-apiquery","title":"POST /api/query","text":"<p>Execute an ad-hoc SQL query with a role-based timeout (admin 300s, editor 120s, viewer 60s by default).</p> <pre><code>{\"sql\": \"SELECT * FROM gold.summary WHERE region = $region\", \"params\": {\"region\": \"US\"}, \"limit\": 1000, \"offset\": 0}\n</code></pre> <p><code>params</code> binds named <code>$name</code> placeholders as DuckDB prepared-statement values (no string interpolation, so values cannot inject SQL). Numbers and booleans are bound typed; strings can be cast in SQL (<code>$day::DATE</code>).</p> <p>Returns: <code>{columns, column_types, rows, truncated, offset, limit}</code></p> <p>Also intercepts masking SQL commands: <code>SHOW MASKING POLICIES</code>, <code>CREATE MASKING POLICY ON ...</code>, <code>DROP MASKING POLICY &lt;id&gt;</code>.</p>"},{"location":"api-reference/#post-apiqueryexplain","title":"POST /api/query/explain","text":"<p>Return the DuckDB EXPLAIN plan (structured + raw text). Accepts the same <code>sql</code> and <code>params</code> fields.</p>"},{"location":"api-reference/#post-apiqueryexport-csv","title":"POST /api/query/export-csv","text":"<p>Stream full query results as a CSV download (no row limit). Accepts the same <code>sql</code> and <code>params</code> fields.</p>"},{"location":"api-reference/#tables","title":"Tables","text":""},{"location":"api-reference/#get-apitables","title":"GET /api/tables","text":"<p>List warehouse tables and views. Optional <code>?schema=gold</code> filter.</p> <p>Returns: <code>[{schema, name, type}]</code></p>"},{"location":"api-reference/#get-apitablesschematable","title":"GET /api/tables/{schema}/{table}","text":"<p>Describe a table's columns.</p> <p>Returns: <code>{schema, name, columns: [{name, type, nullable}]}</code></p>"},{"location":"api-reference/#get-apitablesschematablesample","title":"GET /api/tables/{schema}/{table}/sample","text":"<p>Get sample rows with pagination.</p> <p>Query params: <code>?limit=100&amp;offset=0</code></p> <p>Returns: <code>{schema, table, columns, rows, limit, offset}</code></p>"},{"location":"api-reference/#get-apitablesschematableprofile","title":"GET /api/tables/{schema}/{table}/profile","text":"<p>Get column-level statistics: null counts, distinct counts, min/max, averages, sample values.</p>"},{"location":"api-reference/#get-apiautocomplete","title":"GET /api/autocomplete","text":"<p>Get table and column names for query editor autocomplete.</p>"},{"location":"api-reference/#models","title":"Models","text":""},{"location":"api-reference/#get-apimodels","title":"GET /api/models","text":"<p>List all SQL transform models with metadata.</p> <p>Returns: <code>[{name, schema, full_name, materialized, depends_on, path, content_hash}]</code></p>"},{"location":"api-reference/#post-apitransform","title":"POST /api/transform","text":"<p>Run the SQL transformation pipeline.</p> <pre><code>{\"targets\": null, \"force\": false}\n</code></pre>"},{"location":"api-reference/#post-apimodelscreate","title":"POST /api/models/create","text":"<p>Create a new SQL model file.</p> <pre><code>{\"name\": \"my_model\", \"schema_name\": \"silver\", \"materialized\": \"table\", \"sql\": \"SELECT 1\"}\n</code></pre>"},{"location":"api-reference/#post-apicheck","title":"POST /api/check","text":"<p>Validate models, run assertions, and run contracts.</p>"},{"location":"api-reference/#post-apidiff","title":"POST /api/diff","text":"<p>Compare SQL output against materialized tables.</p> <pre><code>{\"targets\": null, \"target_schema\": null, \"full\": false}\n</code></pre>"},{"location":"api-reference/#get-apimodelsmodel_namenotebook-view","title":"GET /api/models/{model_name}/notebook-view","text":"<p>Get a notebook-style view combining SQL source, sample data, lineage, and dependencies.</p>"},{"location":"api-reference/#dag","title":"DAG","text":""},{"location":"api-reference/#get-apidag","title":"GET /api/dag","text":"<p>Get the model dependency DAG (nodes and edges).</p>"},{"location":"api-reference/#get-apidagfull","title":"GET /api/dag/full","text":"<p>Get the full DAG including seeds, sources, ingest scripts, and exposures.</p>"},{"location":"api-reference/#lineage","title":"Lineage","text":""},{"location":"api-reference/#get-apilineagemodel_name","title":"GET /api/lineage/{model_name}","text":"<p>Get column-level lineage for a model. Returns <code>{model, columns, depends_on}</code>.</p>"},{"location":"api-reference/#get-apilineage","title":"GET /api/lineage","text":"<p>Get column-level lineage for all models.</p>"},{"location":"api-reference/#get-apiimpactmodel_name","title":"GET /api/impact/{model_name}","text":"<p>Analyze downstream impact. Optional <code>?column=name</code> for column-level analysis.</p>"},{"location":"api-reference/#pipeline","title":"Pipeline","text":""},{"location":"api-reference/#post-apirun","title":"POST /api/run","text":"<p>Run an ingest or export script.</p> <pre><code>{\"script_path\": \"ingest/customers.py\"}\n</code></pre>"},{"location":"api-reference/#post-apistreamstream_namestart","title":"POST /api/stream/{stream_name}/start","text":"<p>Start a pipeline in a background thread. Returns immediately with <code>{\"status\": \"started\", \"stream_name\": \"...\"}</code> or <code>{\"status\": \"already_running\"}</code>. This is the preferred entry point; the legacy single-shot <code>POST /api/stream/{name}</code> still works for short pipelines.</p> <p>Optional query parameter: <code>?force=true</code>.</p>"},{"location":"api-reference/#get-apistreamevents","title":"GET /api/stream/events","text":"<p>Server-sent events for the currently running pipeline. Emits: <code>start</code>, <code>step_start</code>, <code>model_start</code>, <code>model_end</code>, <code>complete</code>, <code>error</code>. Events are pushed via a server-side <code>threading.Condition</code>, so listeners do not poll. Heartbeat (<code>: keepalive</code>) every 15 seconds when idle.</p>"},{"location":"api-reference/#get-apistreamactive","title":"GET /api/stream/active","text":"<p>Snapshot of the current pipeline state: <code>{ running, operation, operation_label, stream_name, started_at, total_events, finished, status, duration_seconds }</code>. Stale runs older than 10 minutes are auto-cleared.</p>"},{"location":"api-reference/#post-apistreamcancel","title":"POST /api/stream/cancel","text":"<p>Request cancellation of the running pipeline. Workers check the cancel flag between steps; in-flight queries are not interrupted by default.</p>"},{"location":"api-reference/#get-apistreams","title":"GET /api/streams","text":"<p>List configured streams with steps and schedules.</p>"},{"location":"api-reference/#get-apihistory","title":"GET /api/history","text":"<p>Get run history. Optional <code>?limit=50</code>.</p>"},{"location":"api-reference/#get-apischeduler","title":"GET /api/scheduler","text":"<p>Get scheduler status and scheduled streams.</p>"},{"location":"api-reference/#connectors","title":"Connectors","text":""},{"location":"api-reference/#get-apiconnectorsavailable","title":"GET /api/connectors/available","text":"<p>List all available connector types with parameters.</p>"},{"location":"api-reference/#get-apiconnectors","title":"GET /api/connectors","text":"<p>List connectors configured in the project.</p>"},{"location":"api-reference/#post-apiconnectorstest","title":"POST /api/connectors/test","text":"<p>Test a connector without setup.</p> <pre><code>{\"connector_type\": \"postgres\", \"config\": {\"host\": \"...\", \"database\": \"...\"}}\n</code></pre>"},{"location":"api-reference/#post-apiconnectorsdiscover","title":"POST /api/connectors/discover","text":"<p>Discover available resources for a connector.</p> <pre><code>{\"connector_type\": \"postgres\", \"config\": {\"host\": \"...\"}}\n</code></pre>"},{"location":"api-reference/#post-apiconnectorssetup","title":"POST /api/connectors/setup","text":"<p>Full connector setup: test, generate script, update config.</p> <pre><code>{\n  \"connector_type\": \"postgres\",\n  \"connection_name\": \"prod_db\",\n  \"config\": {\"host\": \"...\", \"database\": \"...\"},\n  \"tables\": [\"users\", \"orders\"],\n  \"target_schema\": \"landing\",\n  \"schedule\": \"0 6 * * *\"\n}\n</code></pre>"},{"location":"api-reference/#post-apiconnectorsregenerateconnection_name","title":"POST /api/connectors/regenerate/{connection_name}","text":"<p>Regenerate the ingest script for an existing connector.</p>"},{"location":"api-reference/#post-apiconnectorssyncconnection_name","title":"POST /api/connectors/sync/{connection_name}","text":"<p>Run sync for a configured connector.</p>"},{"location":"api-reference/#delete-apiconnectorsconnection_name","title":"DELETE /api/connectors/{connection_name}","text":"<p>Remove a connector (script and config).</p>"},{"location":"api-reference/#get-apiconnectorshealth","title":"GET /api/connectors/health","text":"<p>Get last sync status for each connector.</p>"},{"location":"api-reference/#post-apiwebhookwebhook_name","title":"POST /api/webhook/{webhook_name}","text":"<p>Receive webhook data and store in <code>landing.&lt;name&gt;_inbox</code>.</p> <p>Authentication: requires a shared secret. Set <code>HAVN_WEBHOOK_SECRET_&lt;NAME&gt;</code> (per-webhook) or <code>HAVN_WEBHOOK_SECRET</code> (global) and pass it as the <code>X-Havn-Webhook-Secret</code> header. To run open for local development set <code>HAVN_WEBHOOK_OPEN=true</code>. Payloads are capped at 5 MB.</p>"},{"location":"api-reference/#cdc","title":"CDC","text":""},{"location":"api-reference/#get-apicdc","title":"GET /api/cdc","text":"<p>Get CDC state for all tracked connectors.</p>"},{"location":"api-reference/#get-apicdcconnector_name","title":"GET /api/cdc/{connector_name}","text":"<p>Get CDC state for a specific connector.</p>"},{"location":"api-reference/#post-apicdcconnector_namereset","title":"POST /api/cdc/{connector_name}/reset","text":"<p>Reset CDC watermarks for a connector.</p>"},{"location":"api-reference/#data-quality","title":"Data Quality","text":""},{"location":"api-reference/#get-apifreshness","title":"GET /api/freshness","text":"<p>Check model freshness. Optional <code>?max_hours=24</code>.</p>"},{"location":"api-reference/#get-apiprofiles","title":"GET /api/profiles","text":"<p>Get profile stats for all models.</p>"},{"location":"api-reference/#get-apiprofilesmodel_name","title":"GET /api/profiles/{model_name}","text":"<p>Get profile stats for a specific model.</p>"},{"location":"api-reference/#get-apiassertions","title":"GET /api/assertions","text":"<p>Get recent assertion results. Optional <code>?limit=100</code>.</p>"},{"location":"api-reference/#get-apiassertionsmodel_name","title":"GET /api/assertions/{model_name}","text":"<p>Get assertion results for a specific model.</p>"},{"location":"api-reference/#get-apialerts","title":"GET /api/alerts","text":"<p>Get alert history. Optional <code>?limit=50</code>.</p>"},{"location":"api-reference/#post-apialertstest","title":"POST /api/alerts/test","text":"<p>Send a test alert.</p> <pre><code>{\"channel\": \"slack\", \"slack_webhook_url\": \"https://hooks.slack.com/...\"}\n</code></pre>"},{"location":"api-reference/#get-apicontracts","title":"GET /api/contracts","text":"<p>List all discovered contracts.</p>"},{"location":"api-reference/#post-apicontractsrun","title":"POST /api/contracts/run","text":"<p>Run all data contracts.</p>"},{"location":"api-reference/#get-apicontractshistory","title":"GET /api/contracts/history","text":"<p>Get contract evaluation history.</p>"},{"location":"api-reference/#masking","title":"Masking","text":""},{"location":"api-reference/#get-apimaskingpolicies","title":"GET /api/masking/policies","text":"<p>List all masking policies.</p>"},{"location":"api-reference/#post-apimaskingpolicies","title":"POST /api/masking/policies","text":"<p>Create a new masking policy.</p> <pre><code>{\n  \"schema_name\": \"gold\",\n  \"table_name\": \"customers\",\n  \"column_name\": \"email\",\n  \"method\": \"hash\",\n  \"exempted_roles\": [\"admin\"]\n}\n</code></pre>"},{"location":"api-reference/#get-apimaskingpoliciespolicy_id","title":"GET /api/masking/policies/{policy_id}","text":"<p>Get a specific masking policy.</p>"},{"location":"api-reference/#put-apimaskingpoliciespolicy_id","title":"PUT /api/masking/policies/{policy_id}","text":"<p>Update a masking policy.</p>"},{"location":"api-reference/#delete-apimaskingpoliciespolicy_id","title":"DELETE /api/masking/policies/{policy_id}","text":"<p>Delete a masking policy.</p>"},{"location":"api-reference/#backup","title":"Backup","text":""},{"location":"api-reference/#post-apibackup","title":"POST /api/backup","text":"<p>Create a verified backup with SHA-256 checksum. Optional body:</p> <pre><code>{\"no_verify\": false, \"note\": \"before deploy\", \"keep\": 10}\n</code></pre> <p>Returns: <code>{path, size_bytes, sha256, timestamp}</code></p>"},{"location":"api-reference/#get-apibackups","title":"GET /api/backups","text":"<p>List all tracked backups from the manifest.</p>"},{"location":"api-reference/#post-apibackupverify","title":"POST /api/backup/verify","text":"<p>Verify a backup file's integrity against its stored checksum.</p> <pre><code>{\"path\": \"_backups/warehouse_20260407_120000.duckdb\"}\n</code></pre>"},{"location":"api-reference/#post-apibackuprestore","title":"POST /api/backup/restore","text":"<p>Restore the warehouse from a backup.</p> <pre><code>{\"path\": \"_backups/warehouse_20260407_120000.duckdb\"}\n</code></pre>"},{"location":"api-reference/#post-apibackupcleanup","title":"POST /api/backup/cleanup","text":"<p>Remove old backups, keeping the most recent N.</p> <pre><code>{\"keep\": 5}\n</code></pre>"},{"location":"api-reference/#catalog","title":"Catalog","text":""},{"location":"api-reference/#get-apiseeds","title":"GET /api/seeds","text":"<p>List all seed CSV files.</p>"},{"location":"api-reference/#post-apiseeds","title":"POST /api/seeds","text":"<p>Load all seeds. Body: <code>{\"force\": false, \"schema_name\": \"seeds\"}</code></p>"},{"location":"api-reference/#get-apisources","title":"GET /api/sources","text":"<p>List declared sources from sources.yml.</p>"},{"location":"api-reference/#get-apisourcesfreshness","title":"GET /api/sources/freshness","text":"<p>Check source freshness against SLAs.</p>"},{"location":"api-reference/#get-apiexposures","title":"GET /api/exposures","text":"<p>List declared exposures.</p>"},{"location":"api-reference/#get-apienvironment","title":"GET /api/environment","text":"<p>Get current and available environments.</p>"},{"location":"api-reference/#put-apienvironmentenv_name","title":"PUT /api/environment/{env_name}","text":"<p>Switch the active environment.</p>"},{"location":"api-reference/#get-apioverview","title":"GET /api/overview","text":"<p>Get platform overview: schemas, tables, rows, recent runs, connectors.</p>"},{"location":"api-reference/#versioning","title":"Versioning","text":""},{"location":"api-reference/#get-apiversions","title":"GET /api/versions","text":"<p>List all warehouse versions.</p>"},{"location":"api-reference/#post-apiversions","title":"POST /api/versions","text":"<p>Create a new version snapshot.</p>"},{"location":"api-reference/#get-apiversionsversion_id","title":"GET /api/versions/{version_id}","text":"<p>Get version details.</p>"},{"location":"api-reference/#get-apiversionsfrom_versiondiff","title":"GET /api/versions/{from_version}/diff","text":"<p>Diff two versions. Optional <code>?to_version=...</code> (defaults to current state).</p>"},{"location":"api-reference/#post-apiversionsversion_idrestore","title":"POST /api/versions/{version_id}/restore","text":"<p>Restore tables from a version.</p>"},{"location":"api-reference/#get-apiversionstimelinetable_name","title":"GET /api/versions/timeline/{table_name}","text":"<p>Get version history for a specific table.</p>"},{"location":"api-reference/#documentation","title":"Documentation","text":""},{"location":"api-reference/#get-apidocsmarkdown","title":"GET /api/docs/markdown","text":"<p>Generate markdown documentation for the project.</p>"},{"location":"api-reference/#get-apidocsstructured","title":"GET /api/docs/structured","text":"<p>Generate structured documentation for the UI.</p>"},{"location":"api-reference/#lint","title":"Lint","text":""},{"location":"api-reference/#post-apilint","title":"POST /api/lint","text":"<p>Lint SQL files. Body: <code>{\"fix\": false}</code></p>"},{"location":"api-reference/#git","title":"Git","text":""},{"location":"api-reference/#get-apigitstatus","title":"GET /api/git/status","text":"<p>Get git status: branch, dirty flag, changed files, last commit.</p>"},{"location":"api-reference/#wiki","title":"Wiki","text":""},{"location":"api-reference/#get-apiwiki","title":"GET /api/wiki","text":"<p>List all wiki pages with slugs, titles, and categories.</p>"},{"location":"api-reference/#get-apiwikislug","title":"GET /api/wiki/{slug}","text":"<p>Get a wiki page by slug. Returns title, content (markdown), and category.</p>"},{"location":"api-reference/#collaboration","title":"Collaboration","text":""},{"location":"api-reference/#websocket-wscollaborate","title":"WebSocket /ws/collaborate","text":"<p>Real-time collaboration WebSocket for concurrent file editing.</p>"},{"location":"api-reference/#notebooks","title":"Notebooks","text":""},{"location":"api-reference/#various-notebook-endpoints","title":"Various notebook endpoints","text":"<p>Notebook endpoints handle listing, reading, executing cells, and saving <code>.dpnb</code> files. See the FastAPI auto-generated docs at <code>/docs</code> for the full notebook API.</p>"},{"location":"auth/","title":"Authentication and Authorization","text":"<p>havn includes built-in authentication with role-based access control (RBAC). When enabled, users must log in to access the web UI and API. Roles control what actions each user can perform.</p>"},{"location":"auth/#enabling-authentication","title":"Enabling Authentication","text":"<p>Start the server with the <code>--auth</code> flag:</p> <pre><code>havn serve --auth\n</code></pre> <p>Without <code>--auth</code>, all endpoints are accessible without authentication (suitable for local development).</p>"},{"location":"auth/#initial-setup","title":"Initial Setup","text":"<p>On first launch with <code>--auth</code>, no users exist. The web UI presents a setup screen to create the first admin user. Alternatively, use the API:</p> <pre><code>curl -X POST http://localhost:3000/api/auth/setup \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\"username\": \"admin\", \"password\": \"your-password\", \"role\": \"admin\"}'\n</code></pre> <p>This creates the first admin user and returns a token:</p> <pre><code>{\n  \"token\": \"abc123...\",\n  \"username\": \"admin\",\n  \"role\": \"admin\"\n}\n</code></pre> <p>The setup endpoint only works when no users exist. After the first user is created, additional users must be created by an admin.</p>"},{"location":"auth/#roles-and-permissions","title":"Roles and Permissions","text":"<p>havn defines three roles with cumulative permissions:</p> Role Permissions Description <code>viewer</code> <code>read</code> Browse tables, view DAG, run queries (read-only) <code>editor</code> <code>read</code>, <code>write</code>, <code>execute</code> All viewer permissions plus run pipelines, edit files, manage data <code>admin</code> <code>read</code>, <code>write</code>, <code>execute</code>, <code>manage_users</code>, <code>manage_secrets</code> Full access including user management and secrets"},{"location":"auth/#permission-details","title":"Permission Details","text":"<ul> <li>read -- View tables, run SELECT queries, browse files, view DAG, view history</li> <li>write -- Edit files, save models, create versions, manage masking policies</li> <li>execute -- Run pipelines, execute scripts, trigger transforms, import data</li> <li>manage_users -- Create, update, delete users</li> <li>manage_secrets -- View, set, delete secrets in <code>.env</code></li> </ul>"},{"location":"auth/#authentication-flow","title":"Authentication Flow","text":""},{"location":"auth/#login","title":"Login","text":"<pre><code>curl -X POST http://localhost:3000/api/auth/login \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\"username\": \"admin\", \"password\": \"your-password\"}'\n</code></pre> <p>Returns:</p> <pre><code>{\n  \"token\": \"abc123...\",\n  \"username\": \"admin\"\n}\n</code></pre>"},{"location":"auth/#using-tokens","title":"Using Tokens","text":"<p>Include the token in the <code>Authorization</code> header for all subsequent requests:</p> <pre><code>curl http://localhost:3000/api/tables \\\n  -H \"Authorization: Bearer abc123...\"\n</code></pre>"},{"location":"auth/#token-lifetime","title":"Token Lifetime","text":"<p>Tokens expire after 30 days by default. After expiration, the user must log in again to get a new token.</p>"},{"location":"auth/#check-auth-status","title":"Check Auth Status","text":"<pre><code>curl http://localhost:3000/api/auth/status\n</code></pre> <p>Returns whether auth is enabled and whether initial setup is needed:</p> <pre><code>{\n  \"auth_enabled\": true,\n  \"needs_setup\": false\n}\n</code></pre>"},{"location":"auth/#get-current-user","title":"Get Current User","text":"<pre><code>curl http://localhost:3000/api/auth/me \\\n  -H \"Authorization: Bearer abc123...\"\n</code></pre> <p>Returns:</p> <pre><code>{\n  \"username\": \"admin\",\n  \"role\": \"admin\",\n  \"display_name\": \"Admin User\"\n}\n</code></pre>"},{"location":"auth/#user-management","title":"User Management","text":"<p>All user management operations require the <code>admin</code> role.</p>"},{"location":"auth/#list-users","title":"List Users","text":"<pre><code>curl http://localhost:3000/api/users \\\n  -H \"Authorization: Bearer &lt;admin-token&gt;\"\n</code></pre> <p>Returns:</p> <pre><code>[\n  {\n    \"username\": \"admin\",\n    \"role\": \"admin\",\n    \"display_name\": \"Admin User\",\n    \"created_at\": \"2025-01-15 06:00:00\",\n    \"last_login\": \"2025-01-15 12:30:00\"\n  }\n]\n</code></pre>"},{"location":"auth/#create-user","title":"Create User","text":"<pre><code>curl -X POST http://localhost:3000/api/users \\\n  -H \"Authorization: Bearer &lt;admin-token&gt;\" \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\n    \"username\": \"analyst\",\n    \"password\": \"secure-password\",\n    \"role\": \"viewer\",\n    \"display_name\": \"Data Analyst\"\n  }'\n</code></pre> <p>Username constraints: alphanumeric, underscores, dots, and hyphens. Password minimum: 4 characters.</p>"},{"location":"auth/#update-user","title":"Update User","text":"<pre><code>curl -X PUT http://localhost:3000/api/users/analyst \\\n  -H \"Authorization: Bearer &lt;admin-token&gt;\" \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\"role\": \"editor\", \"display_name\": \"Senior Analyst\"}'\n</code></pre> <p>You can update <code>role</code>, <code>password</code>, and <code>display_name</code> independently.</p>"},{"location":"auth/#delete-user","title":"Delete User","text":"<pre><code>curl -X DELETE http://localhost:3000/api/users/analyst \\\n  -H \"Authorization: Bearer &lt;admin-token&gt;\"\n</code></pre> <p>Deleting a user also revokes all their active tokens.</p>"},{"location":"auth/#password-security","title":"Password Security","text":"<p>Passwords are stored using PBKDF2 with SHA-256 and 100,000 iterations:</p> <ul> <li>A 32-byte random salt is generated per user</li> <li>The password is hashed with <code>PBKDF2(SHA256, password, salt, 100000)</code></li> <li>Only the hash and salt are stored -- passwords are never stored in plaintext</li> </ul>"},{"location":"auth/#rate-limiting","title":"Rate Limiting","text":"<p>The login endpoint is rate-limited to prevent brute-force attacks. Each client IP is limited in its login attempt frequency.</p>"},{"location":"auth/#secrets-management","title":"Secrets Management","text":"<p>Admin users can manage secrets (<code>.env</code> variables) through the API:</p>"},{"location":"auth/#list-secrets","title":"List Secrets","text":"<pre><code>curl http://localhost:3000/api/secrets \\\n  -H \"Authorization: Bearer &lt;admin-token&gt;\"\n</code></pre> <p>Returns keys with masked values (never exposes actual secret values in the API response).</p>"},{"location":"auth/#set-a-secret","title":"Set a Secret","text":"<pre><code>curl -X POST http://localhost:3000/api/secrets \\\n  -H \"Authorization: Bearer &lt;admin-token&gt;\" \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\"key\": \"DB_PASSWORD\", \"value\": \"new-password\"}'\n</code></pre>"},{"location":"auth/#delete-a-secret","title":"Delete a Secret","text":"<pre><code>curl -X DELETE http://localhost:3000/api/secrets/DB_PASSWORD \\\n  -H \"Authorization: Bearer &lt;admin-token&gt;\"\n</code></pre>"},{"location":"auth/#data-storage","title":"Data Storage","text":"<p>Auth data is stored in the DuckDB database under <code>_havn</code>:</p> <ul> <li><code>_havn.users</code> -- User accounts (username, password hash, salt, role)</li> <li><code>_havn.tokens</code> -- Active authentication tokens with expiration</li> </ul>"},{"location":"auth/#related-pages","title":"Related Pages","text":"<ul> <li>Masking -- Data masking with role-based exemptions</li> <li>Configuration -- Project configuration</li> <li>API Reference -- Full API endpoint reference</li> </ul>"},{"location":"cdc/","title":"Change Data Capture (CDC)","text":"<p>CDC enables incremental data extraction from external sources. Instead of re-fetching all data on every sync, havn tracks what has changed and only fetches new or modified records. This reduces load on source systems and speeds up pipelines.</p>"},{"location":"cdc/#cdc-modes","title":"CDC Modes","text":"<p>havn supports three CDC modes:</p>"},{"location":"cdc/#high_watermark","title":"high_watermark","text":"<p>Tracks the maximum value of a column (typically <code>updated_at</code> or <code>id</code>) and only fetches rows where the column exceeds the stored watermark.</p> <pre><code>connectors:\n  prod_users:\n    type: postgres\n    connection: prod_postgres\n    target_schema: landing\n    tables:\n      - name: users\n        cdc_mode: high_watermark\n        cdc_column: updated_at\n</code></pre> <p>How it works:</p> <ol> <li>On first sync, all rows are fetched (no watermark stored yet)</li> <li>After sync, the maximum value of <code>updated_at</code> is stored as the watermark</li> <li>On subsequent syncs, only rows where <code>updated_at &gt; &lt;stored_watermark&gt;</code> are fetched</li> <li>New rows are appended to the target table (not replaced)</li> </ol> <p>Best for: Database tables with a monotonically increasing timestamp or ID column.</p>"},{"location":"cdc/#file_tracking","title":"file_tracking","text":"<p>Tracks last-modified timestamps for file sources and only re-ingests when files have changed.</p> <pre><code>connectors:\n  data_files:\n    type: csv\n    connection: local_files\n    target_schema: landing\n    tables:\n      - name: transactions\n        cdc_mode: file_tracking\n</code></pre> <p>How it works:</p> <ol> <li>On first sync, the file is read and its modification time (<code>mtime</code>) is stored</li> <li>On subsequent syncs, the current file <code>mtime</code> is compared against the stored value</li> <li>If the file has been modified, it is re-read (full replacement of the target table)</li> <li>If unchanged, the sync is skipped</li> </ol> <p>Supported file types: CSV, Parquet, JSON, NDJSON.</p>"},{"location":"cdc/#full_refresh","title":"full_refresh","text":"<p>Always fetches all data. No CDC tracking.</p> <pre><code>connectors:\n  reference_data:\n    type: postgres\n    connection: prod_postgres\n    target_schema: landing\n    tables:\n      - name: country_codes\n        cdc_mode: full_refresh\n</code></pre> <p>Use this for small reference tables that change infrequently and are cheap to fully reload.</p>"},{"location":"cdc/#configuration","title":"Configuration","text":""},{"location":"cdc/#in-projectyml","title":"In project.yml","text":"<pre><code>connectors:\n  prod_users:\n    type: postgres\n    connection: prod_postgres\n    target_schema: landing\n    schedule: \"*/30 * * * *\"\n    tables:\n      - name: users\n        cdc_mode: high_watermark\n        cdc_column: updated_at\n      - name: roles\n        cdc_mode: full_refresh\n      - name: permissions\n        cdc_mode: high_watermark\n        cdc_column: id\n        source_query: \"SELECT id, name, level FROM permissions\"\n</code></pre> <p>Table options:</p> Option Type Default Description <code>name</code> string required Source table name <code>cdc_mode</code> string <code>full_refresh</code> CDC mode: <code>high_watermark</code>, <code>file_tracking</code>, <code>full_refresh</code> <code>cdc_column</code> string <code>null</code> Column to track (required for <code>high_watermark</code>) <code>source_query</code> string <code>null</code> Custom SELECT query (default: <code>SELECT * FROM table</code>)"},{"location":"cdc/#cdc-state","title":"CDC State","text":"<p>CDC state is stored in <code>_havn.cdc_state</code>:</p> Column Type Description <code>connector_name</code> VARCHAR Connector identifier <code>table_name</code> VARCHAR Table being tracked <code>cdc_mode</code> VARCHAR Active CDC mode <code>watermark_value</code> VARCHAR Current high-watermark value <code>file_mtime</code> DOUBLE File modification timestamp <code>last_sync_at</code> TIMESTAMP Last successful sync time <code>rows_synced</code> BIGINT Rows synced in last run"},{"location":"cdc/#managing-cdc-state","title":"Managing CDC State","text":""},{"location":"cdc/#view-cdc-status","title":"View CDC Status","text":"<pre><code>havn cdc status\n</code></pre> <p>Shows all tracked tables with their watermarks and last sync times.</p> <p>Filter by connector:</p> <pre><code>havn cdc status --connector prod_users\n</code></pre>"},{"location":"cdc/#reset-watermarks","title":"Reset Watermarks","text":"<p>Reset all watermarks for a connector (forces full re-sync):</p> <pre><code>havn cdc reset --connector prod_users\n</code></pre> <p>Reset a specific table:</p> <pre><code>havn cdc reset --connector prod_users --table users\n</code></pre>"},{"location":"cdc/#api-access","title":"API Access","text":"<pre><code># View all CDC state\ncurl http://localhost:3000/api/cdc\n\n# View state for a specific connector\ncurl http://localhost:3000/api/cdc/prod_users\n\n# Reset watermarks\ncurl -X POST http://localhost:3000/api/cdc/prod_users/reset\n</code></pre>"},{"location":"cdc/#sync-results","title":"Sync Results","text":"<p>Each sync returns a <code>CDCSyncResult</code> with:</p> Field Description <code>table</code> Table name <code>status</code> <code>success</code>, <code>skipped</code>, or <code>error</code> <code>rows_synced</code> Number of rows synced <code>duration_ms</code> Sync duration in milliseconds <code>cdc_mode</code> CDC mode used <code>watermark_before</code> Watermark value before sync <code>watermark_after</code> Watermark value after sync <code>error</code> Error message (if failed)"},{"location":"cdc/#best-practices","title":"Best Practices","text":"<ol> <li> <p>Use <code>high_watermark</code> for large, append-heavy tables -- Only new rows are fetched, reducing query time and network transfer.</p> </li> <li> <p>Ensure the watermark column is indexed -- The source database should have an index on the CDC column for efficient filtering.</p> </li> <li> <p>Use <code>full_refresh</code> for small reference tables -- The overhead of CDC tracking is not worth it for tables with fewer than a few thousand rows.</p> </li> <li> <p>Reset watermarks after schema changes -- If the source table schema changes, reset the watermark to re-sync all data.</p> </li> <li> <p>Monitor CDC state -- Use <code>havn cdc status</code> regularly to verify syncs are completing and watermarks are advancing.</p> </li> </ol>"},{"location":"cdc/#related-pages","title":"Related Pages","text":"<ul> <li>Connectors -- Setting up data connectors</li> <li>Pipelines -- Running CDC as part of pipelines</li> <li>Configuration -- Connector configuration in project.yml</li> <li>Scheduler -- Automating CDC syncs on a schedule</li> </ul>"},{"location":"cli-reference/","title":"CLI Reference","text":"<p>Complete reference for all <code>havn</code> CLI commands. Run <code>havn --help</code> or <code>havn &lt;command&gt; --help</code> for built-in help.</p>"},{"location":"cli-reference/#project-management","title":"Project Management","text":""},{"location":"cli-reference/#havn-init","title":"havn init","text":"<p>Scaffold a new data platform project.</p> <pre><code>havn init [NAME] [--dir PATH]\n</code></pre> Argument/Flag Default Description <code>NAME</code> <code>my-project</code> Project name <code>--dir, -d</code> <code>./&lt;NAME&gt;</code> Target directory <p>Creates project structure with sample earthquake data pipeline, seeds, contracts, and notebooks.</p>"},{"location":"cli-reference/#havn-validate","title":"havn validate","text":"<p>Validate project structure, config, and SQL model dependencies.</p> <pre><code>havn validate [--project PATH]\n</code></pre> <p>Checks <code>project.yml</code> parsing, directory structure, stream actions, model dependencies, circular dependencies, and environment variable references.</p>"},{"location":"cli-reference/#havn-status","title":"havn status","text":"<p>Show project health: git info, warehouse stats, last run.</p> <pre><code>havn status [--project PATH]\n</code></pre>"},{"location":"cli-reference/#havn-context","title":"havn context","text":"<p>Generate a project summary to paste into any AI assistant.</p> <pre><code>havn context [--project PATH]\n</code></pre> <p>Outputs a comprehensive markdown summary of the project including configuration, models, scripts, warehouse tables, and recent history.</p>"},{"location":"cli-reference/#havn-checkpoint","title":"havn checkpoint","text":"<p>Smart git commit: stages files, auto-generates commit message.</p> <pre><code>havn checkpoint [--message TEXT] [--project PATH]\n</code></pre> <p>Automatically stages all files except <code>.env</code>, generates a descriptive commit message from changed file paths, and commits.</p>"},{"location":"cli-reference/#havn-backup","title":"havn backup","text":"<p>Create a verified backup of the warehouse database with SHA-256 checksum.</p> <pre><code>havn backup [--output PATH] [--no-verify] [--note TEXT] [--keep N] [--project PATH]\n</code></pre> Flag Default Description <code>--output, -o</code> <code>_backups/</code> Output path <code>--no-verify</code> false Skip post-backup integrity check <code>--note</code> none Attach a note to the backup manifest entry <code>--keep</code> none Retention: keep only the last N backups, remove older ones <p>Flushes the DuckDB WAL, copies the database file, computes a SHA-256 checksum, and tracks the backup in <code>_backups/manifest.json</code>.</p>"},{"location":"cli-reference/#havn-backup-list","title":"havn backup-list","text":"<p>List all tracked backups from the manifest.</p> <pre><code>havn backup-list [--project PATH]\n</code></pre>"},{"location":"cli-reference/#havn-backup-verify","title":"havn backup-verify","text":"<p>Verify the integrity of a backup file against its stored checksum.</p> <pre><code>havn backup-verify BACKUP_PATH [--project PATH]\n</code></pre>"},{"location":"cli-reference/#havn-restore","title":"havn restore","text":"<p>Restore the warehouse database from a backup.</p> <pre><code>havn restore BACKUP_PATH [--project PATH]\n</code></pre>"},{"location":"cli-reference/#pipeline-execution","title":"Pipeline Execution","text":""},{"location":"cli-reference/#havn-run","title":"havn run","text":"<p>Run a single ingest or export script.</p> <pre><code>havn run SCRIPT [--project PATH]\n</code></pre> <p>Examples: <pre><code>havn run ingest/customers.py\nhavn run ingest/earthquakes.dpnb\nhavn run export/daily_report.py\n</code></pre></p>"},{"location":"cli-reference/#havn-seed","title":"havn seed","text":"<p>Load CSV files from seeds/ directory.</p> <pre><code>havn seed [--force] [--schema NAME] [--env NAME] [--project PATH]\n</code></pre> Flag Default Description <code>--force, -f</code> false Reload all seeds (ignore change detection) <code>--schema, -s</code> <code>seeds</code> Target schema <code>--env, -e</code> none Environment override"},{"location":"cli-reference/#havn-transform","title":"havn transform","text":"<p>Build SQL models in dependency order.</p> <pre><code>havn transform [TARGETS...] [--force] [--parallel] [--workers N] [--env NAME] [--skip-check] [--project PATH]\n</code></pre> Flag Default Description <code>TARGETS</code> all Specific models to build <code>--force, -f</code> false Rebuild all (ignore change detection) <code>--parallel</code> false Run independent models concurrently <code>--workers, -w</code> 4 Max parallel workers <code>--env, -e</code> none Environment override <code>--skip-check</code> false Skip pre-transform validation"},{"location":"cli-reference/#havn-jobs-run","title":"havn jobs run","text":"<p>Run an orchestration job from project.yml.</p> <pre><code>havn jobs run NAME [--force] [--env NAME] [--project PATH]\n</code></pre>"},{"location":"cli-reference/#havn-lint","title":"havn lint","text":"<p>Lint SQL files with SQLFluff.</p> <pre><code>havn lint [--fix] [--project PATH]\n</code></pre> Flag Description <code>--fix</code> Auto-fix violations"},{"location":"cli-reference/#querying-and-inspection","title":"Querying and Inspection","text":""},{"location":"cli-reference/#havn-query","title":"havn query","text":"<p>Run an ad-hoc SQL query.</p> <pre><code>havn query \"SQL\" [--csv] [--json] [--limit N] [--env NAME] [--project PATH]\n</code></pre> Flag Default Description <code>--csv</code> false Output as CSV <code>--json</code> false Output as JSON <code>--limit, -n</code> 0 (all) Max rows to return <code>--env, -e</code> none Environment override"},{"location":"cli-reference/#havn-tables","title":"havn tables","text":"<p>List tables and views in the warehouse.</p> <pre><code>havn tables [SCHEMA] [--env NAME] [--project PATH]\n</code></pre>"},{"location":"cli-reference/#havn-history","title":"havn history","text":"<p>Show recent run history.</p> <pre><code>havn history [--limit N] [--project PATH]\n</code></pre>"},{"location":"cli-reference/#data-quality","title":"Data Quality","text":""},{"location":"cli-reference/#havn-check","title":"havn check","text":"<p>Validate SQL models, run assertions, and run contracts.</p> <pre><code>havn check [TARGETS...] [--env NAME] [--project PATH]\n</code></pre> <p>Runs model validation, inline assertions, and YAML contracts.</p>"},{"location":"cli-reference/#havn-freshness","title":"havn freshness","text":"<p>Check model and source freshness.</p> <pre><code>havn freshness [--hours N] [--alert] [--sources] [--env NAME] [--project PATH]\n</code></pre> Flag Default Description <code>--hours, -h</code> 24.0 Max age before a model is stale <code>--alert</code> false Send alerts for stale models <code>--sources</code> false Check source freshness from sources.yml"},{"location":"cli-reference/#havn-profile","title":"havn profile","text":"<p>Show model profile statistics.</p> <pre><code>havn profile [MODEL] [--project PATH]\n</code></pre> <p>Without a model name, shows summary for all models. With a model name, shows detailed column statistics.</p>"},{"location":"cli-reference/#havn-assertions","title":"havn assertions","text":"<p>Show recent assertion results.</p> <pre><code>havn assertions [--project PATH]\n</code></pre>"},{"location":"cli-reference/#havn-contracts","title":"havn contracts","text":"<p>Run data contracts from the contracts/ directory.</p> <pre><code>havn contracts [TARGETS...] [--history] [--project PATH]\n</code></pre> Flag Description <code>TARGETS</code> Contract names or model names to run <code>--history</code> Show contract history instead of running"},{"location":"cli-reference/#model-analysis","title":"Model Analysis","text":""},{"location":"cli-reference/#havn-lineage","title":"havn lineage","text":"<p>Show column-level lineage for a model.</p> <pre><code>havn lineage MODEL [--json] [--project PATH]\n</code></pre>"},{"location":"cli-reference/#havn-impact","title":"havn impact","text":"<p>Analyze downstream impact of changing a model or column.</p> <pre><code>havn impact MODEL [--column NAME] [--json] [--project PATH]\n</code></pre>"},{"location":"cli-reference/#havn-promote","title":"havn promote","text":"<p>Promote SQL to a transform model file.</p> <pre><code>havn promote SQL_SOURCE [--name NAME] [--schema NAME] [--desc TEXT] [--file PATH] [--overwrite] [--project PATH]\n</code></pre>"},{"location":"cli-reference/#havn-debug","title":"havn debug","text":"<p>Generate a debug notebook for a failed model.</p> <pre><code>havn debug MODEL [--project PATH]\n</code></pre> <p>Creates a <code>.dpnb</code> notebook pre-populated with error info, upstream queries, and the failing SQL.</p>"},{"location":"cli-reference/#diff-and-versioning","title":"Diff and Versioning","text":""},{"location":"cli-reference/#havn-diff","title":"havn diff","text":"<p>Compare model SQL output against materialized tables. Three modes:</p> <pre><code># Single model \u2014 diff one specific table\nhavn diff gold.orders\n\n# Changed + downstream (default) \u2014 only diff models with SQL changes\nhavn diff\nhavn diff --changed\n\n# Full database \u2014 diff everything (old behavior)\nhavn diff --all\n</code></pre> <pre><code>havn diff [TARGETS...] [--changed] [--all] [--target SCHEMA] [--format FMT] [--rows] [--full] [--against REF] [--snapshot NAME] [--project PATH]\n</code></pre> Flag Default Description <code>TARGETS</code> none Specific models to diff (single mode) <code>--changed</code> true Only diff models with SQL changes + downstream <code>--all</code> false Diff all models (full database) <code>--target, -t</code> none Diff all models in a schema <code>--format, -f</code> <code>table</code> Output format: <code>table</code> or <code>json</code> <code>--rows</code> false Include sample rows <code>--full</code> false Show all changed rows <code>--against</code> none Git-aware: only diff models changed vs a branch <code>--snapshot</code> none Compare against a named snapshot <p>When no targets or flags are given, <code>--changed</code> is the default \u2014 it uses change detection to only diff models whose SQL (or upstream) actually changed, plus their downstream dependents. This is much faster than diffing the entire database.</p>"},{"location":"cli-reference/#connectors","title":"Connectors","text":""},{"location":"cli-reference/#havn-connect","title":"havn connect","text":"<p>Set up a data connector.</p> <pre><code>havn connect TYPE [--name NAME] [--tables LIST] [--schema NAME] [--schedule CRON] [--test] [--discover] [--config JSON] [--set KEY=VALUE] [--host H] [--port P] [--database D] [--user U] [--password P] [--url U] [--api-key K] [--token T] [--path P] [--project PATH]\n</code></pre> <p>Use <code>havn connect list</code> to show available connector types.</p>"},{"location":"cli-reference/#havn-connectors-list","title":"havn connectors list","text":"<p>List configured connectors.</p> <pre><code>havn connectors list [--project PATH]\n</code></pre>"},{"location":"cli-reference/#havn-connectors-test","title":"havn connectors test","text":"<p>Test a configured connector.</p> <pre><code>havn connectors test CONNECTION_NAME [--project PATH]\n</code></pre>"},{"location":"cli-reference/#havn-connectors-sync","title":"havn connectors sync","text":"<p>Run sync for a connector.</p> <pre><code>havn connectors sync CONNECTION_NAME [--project PATH]\n</code></pre>"},{"location":"cli-reference/#havn-connectors-regenerate","title":"havn connectors regenerate","text":"<p>Regenerate the ingest script for a connector.</p> <pre><code>havn connectors regenerate CONNECTION_NAME [--project PATH]\n</code></pre>"},{"location":"cli-reference/#havn-connectors-remove","title":"havn connectors remove","text":"<p>Remove a connector (script and config).</p> <pre><code>havn connectors remove CONNECTION_NAME [--project PATH]\n</code></pre>"},{"location":"cli-reference/#havn-connectors-available","title":"havn connectors available","text":"<p>List all available connector types.</p> <pre><code>havn connectors available\n</code></pre>"},{"location":"cli-reference/#cdc","title":"CDC","text":""},{"location":"cli-reference/#havn-cdc","title":"havn cdc","text":"<p>View and manage CDC state.</p> <pre><code>havn cdc ACTION [--connector NAME] [--table NAME] [--project PATH]\n</code></pre> <p>Actions: - <code>status</code> -- Show CDC state for all connectors - <code>reset</code> -- Reset watermarks (requires <code>--connector</code>)</p>"},{"location":"cli-reference/#scheduling","title":"Scheduling","text":""},{"location":"cli-reference/#havn-schedule","title":"havn schedule","text":"<p>Start the cron scheduler.</p> <pre><code>havn schedule [--project PATH]\n</code></pre>"},{"location":"cli-reference/#havn-watch","title":"havn watch","text":"<p>Watch for file changes and auto-rebuild.</p> <pre><code>havn watch [--project PATH]\n</code></pre>"},{"location":"cli-reference/#masking","title":"Masking","text":""},{"location":"cli-reference/#havn-mask-list","title":"havn mask list","text":"<p>List all masking policies.</p> <pre><code>havn mask list [--project PATH]\n</code></pre>"},{"location":"cli-reference/#havn-mask-add","title":"havn mask add","text":"<p>Add a masking policy. Supports all 14 methods (<code>hash</code>, <code>redact</code>, <code>null</code>, <code>partial</code>, <code>truncate</code>, <code>email</code>, <code>phone</code>, <code>first_initial</code>, <code>ip_address</code>, <code>credit_card</code>, <code>range</code>, <code>noise</code>, <code>date_shift</code>, <code>consistent_hash</code>).</p> <pre><code>havn mask add --schema S --table T --column C --method M [--show-first N] [--show-last N] [--project PATH]\n</code></pre>"},{"location":"cli-reference/#havn-mask-remove","title":"havn mask remove","text":"<p>Remove a masking policy by ID.</p> <pre><code>havn mask remove --id POLICY_ID [--project PATH]\n</code></pre>"},{"location":"cli-reference/#server","title":"Server","text":""},{"location":"cli-reference/#havn-serve","title":"havn serve","text":"<p>Start the web UI server.</p> <pre><code>havn serve [--port PORT] [--host HOST] [--auth] [--env NAME] [--project PATH]\n</code></pre> Flag Default Description <code>--port</code> 3000 Server port <code>--host</code> 127.0.0.1 Server host <code>--auth</code> false Enable authentication <code>--env</code> none Environment to use"},{"location":"cli-reference/#version","title":"Version","text":""},{"location":"cli-reference/#havn-env","title":"havn env","text":"<p>Manage active environment.</p> <pre><code>havn env ACTION [NAME]\n</code></pre> <p>Actions: - <code>list</code> \u2014 Show all environments, mark active with star - <code>use &lt;name&gt;</code> \u2014 Set active environment (writes <code>.havn-env</code>) - <code>show</code> \u2014 Show current active environment - <code>reset</code> \u2014 Clear active environment</p>"},{"location":"cli-reference/#havn-macros","title":"havn macros","text":"<p>List registered Python SQL macros.</p> <pre><code>havn macros [--project PATH]\n</code></pre> <p>Shows all macros discovered from the <code>macros/</code> directory: name, parameters, return type, source file, and docstring.</p>"},{"location":"cli-reference/#havn-version","title":"havn version","text":"<p>Show havn version.</p> <pre><code>havn version\n</code></pre>"},{"location":"cli-reference/#interactive","title":"Interactive","text":""},{"location":"cli-reference/#havn-shell","title":"havn shell","text":"<p>Open an interactive SQL REPL connected to the warehouse. Supports multi-line queries, history, and tab completion.</p> <pre><code>havn shell [--env NAME] [--project PATH]\n</code></pre>"},{"location":"cli-reference/#havn-explain","title":"havn explain","text":"<p>Print a model's query plan with operator timings.</p> <pre><code>havn explain MODEL [--analyze] [--project PATH]\n</code></pre> <p><code>--analyze</code> runs the model's query with <code>EXPLAIN ANALYZE</code> and reports operator-level wall-clock times.</p>"},{"location":"cli-reference/#pipeline-rewind","title":"Pipeline Rewind","text":""},{"location":"cli-reference/#havn-rewind","title":"havn rewind","text":"<p>Inspect rewind metadata.</p> <pre><code>havn rewind runs [--limit N] [--project PATH]\nhavn rewind snapshot --run RUN_ID [--limit N] [--project PATH]\nhavn rewind sample --run RUN_ID --model NAME [--limit N] [--project PATH]\nhavn rewind gc [--project PATH]\n</code></pre>"},{"location":"cli-reference/#schema-sentinel","title":"Schema Sentinel","text":""},{"location":"cli-reference/#havn-sentinel","title":"havn sentinel","text":"<p>Detect upstream schema drift and analyze impact.</p> <pre><code>havn sentinel check [--source NAME] [--project PATH]\nhavn sentinel diffs [--limit N] [--project PATH]\nhavn sentinel impacts --diff DIFF_ID [--limit N] [--project PATH]\nhavn sentinel history --source NAME [--limit N] [--project PATH]\n</code></pre>"},{"location":"cli-reference/#pull-requests-github","title":"Pull Requests (GitHub)","text":""},{"location":"cli-reference/#havn-pr","title":"havn pr","text":"<p>Create and review havn pipeline changes via GitHub pull requests.</p> <pre><code>havn pr open                    # open the current branch as a PR\nhavn pr review NUMBER           # local data diff for a PR\n</code></pre>"},{"location":"cli-reference/#arrow-flight-sql","title":"Arrow Flight SQL","text":""},{"location":"cli-reference/#havn-flight","title":"havn flight","text":"<p>Start an Arrow Flight SQL server that exposes the warehouse to Flight-aware clients (BI tools, Python <code>pyarrow.flight</code>, etc.).</p> <pre><code>havn flight start [--host HOST] [--port PORT] [--project PATH]\n</code></pre>"},{"location":"cli-reference/#streaming","title":"Streaming","text":""},{"location":"cli-reference/#havn-streaming","title":"havn streaming","text":"<p>Drive long-lived streaming sources from the CLI.</p> <pre><code>havn streaming start [--project PATH]\nhavn streaming stop [--project PATH]\nhavn streaming status [--project PATH]\nhavn streaming poll-once SOURCE [--project PATH]\n</code></pre>"},{"location":"cli-reference/#migration","title":"Migration","text":""},{"location":"cli-reference/#havn-migrate","title":"havn migrate","text":"<p>Convert between backends (DuckDB to DuckLake or vice versa). Reads the current backend, writes to the target.</p> <pre><code>havn migrate --target ducklake [--project PATH]\n</code></pre>"},{"location":"configuration/","title":"Configuration","text":"<p>All havn project settings live in <code>project.yml</code> at the project root. This page documents every configuration option.</p>"},{"location":"configuration/#minimal-configuration","title":"Minimal Configuration","text":"<pre><code>name: my-project\ndatabase:\n  path: warehouse.duckdb\n</code></pre>"},{"location":"configuration/#full-reference","title":"Full Reference","text":""},{"location":"configuration/#project-name","title":"Project Name","text":"<pre><code>name: my-project\n</code></pre> <p>Human-readable project name. Used in logging and documentation.</p>"},{"location":"configuration/#database","title":"Database","text":"<pre><code>database:\n  path: warehouse.duckdb\n</code></pre> <ul> <li><code>path</code> -- Path to the DuckDB database file, relative to the project root.</li> </ul>"},{"location":"configuration/#connections","title":"Connections","text":"<p>Define external database connections. Values support environment variable expansion via <code>${VAR}</code> syntax, resolved from <code>.env</code>:</p> <pre><code>connections:\n  prod_postgres:\n    type: postgres\n    host: ${DB_HOST}\n    port: 5432\n    database: ${DB_NAME}\n    user: ${DB_USER}\n    password: ${DB_PASSWORD}\n\n  analytics_mysql:\n    type: mysql\n    host: localhost\n    database: analytics\n    user: reader\n    password: ${MYSQL_PASSWORD}\n</code></pre> <p>Connection parameters vary by type. See Connectors for available types.</p>"},{"location":"configuration/#streams","title":"Streams","text":"<p>Define data pipelines with ordered steps:</p> <pre><code>streams:\n  full-refresh:\n    description: \"Full pipeline rebuild\"\n    schedule: \"0 6 * * *\"\n    retries: 2\n    retry_delay: 10\n    webhook_url: \"https://hooks.slack.com/services/...\"\n    steps:\n      - seed: [all]\n      - ingest: [all]\n      - transform: [all]\n      - export: [all]\n\n  quick-transform:\n    description: \"Just rebuild models\"\n    steps:\n      - transform: [all]\n</code></pre> <p>Stream options:</p> Option Type Default Description <code>description</code> string <code>\"\"</code> Human-readable description <code>schedule</code> string <code>null</code> Cron expression (5-field) <code>retries</code> int <code>0</code> Retry attempts per failed step <code>retry_delay</code> int <code>5</code> Seconds between retries <code>webhook_url</code> string <code>null</code> URL for completion notifications <p>Step actions: <code>ingest</code>, <code>seed</code>, <code>transform</code>, <code>export</code>. Each takes a list of targets or <code>[all]</code>.</p>"},{"location":"configuration/#lint","title":"Lint","text":"<p>Configure SQLFluff SQL linting:</p> <pre><code>lint:\n  dialect: duckdb\n  rules:\n    - L001\n    - L002\n    - L003\n</code></pre> <ul> <li><code>dialect</code> -- SQL dialect for SQLFluff. Default: <code>duckdb</code>.</li> <li><code>rules</code> -- List of SQLFluff rules to enable. Default: all rules.</li> </ul>"},{"location":"configuration/#sources","title":"Sources","text":"<p>Declare external data sources with metadata and freshness SLAs:</p> <pre><code>sources:\n  - name: production_db\n    schema: landing\n    description: \"Production PostgreSQL database\"\n    connection: prod_postgres\n    freshness_hours: 24\n    tables:\n      - name: customers\n        description: \"Customer records\"\n        loaded_at_column: updated_at\n        columns:\n          - name: customer_id\n            description: \"Primary key\"\n          - name: email\n            description: \"Customer email address\"\n      - name: orders\n        description: \"Order records\"\n</code></pre> <p>See Sources for details.</p>"},{"location":"configuration/#exposures","title":"Exposures","text":"<p>Declare downstream consumers of your data:</p> <pre><code>exposures:\n  - name: sales_dashboard\n    description: \"Executive sales dashboard\"\n    owner: analytics-team\n    type: dashboard\n    url: \"https://dashboard.internal/sales\"\n    depends_on:\n      - gold.daily_revenue\n      - gold.customer_summary\n</code></pre> <p>Exposures appear in the DAG visualization and documentation.</p>"},{"location":"configuration/#environments","title":"Environments","text":"<p>Define environment-specific overrides:</p> <pre><code>environments:\n  dev:\n    database:\n      path: dev_warehouse.duckdb\n  prod:\n    database:\n      path: prod_warehouse.duckdb\n  test:\n    database:\n      path: \":memory:\"\n</code></pre> <p>Switch environments with <code>--env</code>:</p> <pre><code>havn transform --env prod\nhavn serve --env dev\n</code></pre> <p>See Environments for details.</p>"},{"location":"configuration/#alerts","title":"Alerts","text":"<p>Configure alerting for pipeline events:</p> <pre><code>alerts:\n  channels:\n    - slack\n    - webhook\n  slack_webhook_url: ${SLACK_WEBHOOK_URL}\n  webhook_url: \"https://alerts.example.com/havn\"\n  on_success: true\n  on_failure: true\n</code></pre> <ul> <li><code>channels</code> -- List of alert channels: <code>slack</code>, <code>webhook</code>, <code>log</code></li> <li><code>slack_webhook_url</code> -- Slack incoming webhook URL</li> <li><code>webhook_url</code> -- Custom webhook URL for alerts</li> <li><code>on_success</code> -- Send alerts on pipeline success (default: false)</li> <li><code>on_failure</code> -- Send alerts on pipeline failure (default: true)</li> </ul>"},{"location":"configuration/#connectors-cdc","title":"Connectors (CDC)","text":"<p>Configure CDC-enabled connectors for incremental data extraction:</p> <pre><code>connectors:\n  prod_users:\n    type: postgres\n    connection: prod_postgres\n    target_schema: landing\n    tables:\n      - name: users\n        cdc_mode: high_watermark\n        cdc_column: updated_at\n      - name: roles\n        cdc_mode: full_refresh\n    schedule: \"*/30 * * * *\"\n</code></pre> <p>See CDC for details.</p>"},{"location":"configuration/#environment-variable-expansion","title":"Environment Variable Expansion","text":"<p>Any value in <code>project.yml</code> can reference environment variables using <code>${VAR}</code> syntax:</p> <pre><code>connections:\n  prod:\n    type: postgres\n    host: ${DB_HOST}\n    password: ${DB_PASSWORD}\n</code></pre> <p>Variables are resolved from the <code>.env</code> file at the project root:</p> <pre><code>DB_HOST=db.production.internal\nDB_PASSWORD=s3cure_p@ssw0rd\n</code></pre> <p>The <code>.env</code> file is included in <code>.gitignore</code> by default and should never be committed.</p>"},{"location":"configuration/#managing-secrets","title":"Managing Secrets","text":"<p>Via the CLI:</p> <pre><code>havn secrets list                   # show secret keys (values masked)\nhavn secrets set DB_PASSWORD value  # set or update a secret\nhavn secrets delete DB_PASSWORD     # delete a secret\n</code></pre> <p>Via the API (when auth is enabled):</p> <pre><code>curl -X POST http://localhost:3000/api/secrets \\\n  -H \"Authorization: Bearer &lt;token&gt;\" \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\"key\": \"DB_PASSWORD\", \"value\": \"new_password\"}'\n</code></pre>"},{"location":"configuration/#related-pages","title":"Related Pages","text":"<ul> <li>Getting Started -- Project setup walkthrough</li> <li>Pipelines -- Stream configuration details</li> <li>Environments -- Multi-environment support</li> <li>Connectors -- Connection types and parameters</li> <li>Scheduler -- Cron scheduling reference</li> </ul>"},{"location":"connectors/","title":"Connectors","text":"<p>Connectors automate data ingestion from external sources. havn includes pre-built connectors for databases, SaaS APIs, file storage, and webhooks. Each connector tests the connection, discovers available resources, generates an ingest script, and updates <code>project.yml</code>.</p>"},{"location":"connectors/#available-connectors","title":"Available Connectors","text":"Connector Type Description PostgreSQL <code>postgres</code> PostgreSQL database tables MySQL <code>mysql</code> MySQL/MariaDB database tables CSV Files <code>csv</code> Local or remote CSV files Stripe <code>stripe</code> Stripe payments data Shopify <code>shopify</code> Shopify e-commerce data HubSpot <code>hubspot</code> HubSpot CRM data Google Sheets <code>google_sheets</code> Google Spreadsheets REST API <code>rest_api</code> Generic REST API endpoints S3/GCS <code>s3_gcs</code> Amazon S3 or Google Cloud Storage files Webhook <code>webhook</code> Receive webhook data via HTTP POST Snowflake <code>snowflake</code> Snowflake data warehouse (Arrow transfer) BigQuery <code>bigquery</code> Google BigQuery (Arrow transfer) Redshift <code>redshift</code> Amazon Redshift (via DuckDB postgres extension) <p>List all available connectors:</p> <pre><code>havn connectors available\n</code></pre>"},{"location":"connectors/#setting-up-a-connector","title":"Setting Up a Connector","text":""},{"location":"connectors/#interactive-setup","title":"Interactive Setup","text":"<p>Use <code>havn connect</code> to set up a connector interactively:</p> <pre><code># PostgreSQL\nhavn connect postgres --host localhost --database mydb --user admin --password secret\n\n# Stripe\nhavn connect stripe --api-key sk_live_xxx\n\n# CSV file\nhavn connect csv --path /data/customers.csv\n\n# Google Sheets\nhavn connect google-sheets --set spreadsheet_id=1BxiMVs0XRA5nFMdKvBdBZjgmUUqptlbs74OgVE2upms\n\n# With JSON config\nhavn connect postgres --config '{\"host\":\"db.prod\",\"database\":\"app\",\"user\":\"ro\",\"password\":\"s3cret\"}'\n\n# From a config file\nhavn connect postgres --config ./postgres.json\n</code></pre> <p>The setup process:</p> <ol> <li>Tests the connection -- Verifies credentials and connectivity</li> <li>Discovers resources -- Lists available tables, endpoints, or sheets</li> <li>Generates an ingest script -- Creates <code>ingest/connector_&lt;name&gt;.py</code></li> <li>Updates project.yml -- Adds the connection and creates a sync stream</li> <li>Stores secrets in .env -- Passwords and API keys go to <code>.env</code>, not <code>project.yml</code></li> </ol>"},{"location":"connectors/#configuration-options","title":"Configuration Options","text":"<pre><code>havn connect &lt;type&gt; [OPTIONS]\n\nOptions:\n  --name, -n          Connection name (default: auto-generated)\n  --tables, -t        Comma-separated tables to sync\n  --schema, -s        Target schema (default: landing)\n  --schedule          Cron schedule for automatic sync\n  --test              Only test the connection\n  --discover          Only list available resources\n  --config, -c        JSON string or file path with params\n  --set key=value     Set individual parameters (repeatable)\n</code></pre> <p>Convenience shortcuts for common parameters:</p> <pre><code>  --host              Hostname\n  --port              Port number\n  --database          Database name\n  --user              Username\n  --password          Password\n  --url               URL\n  --api-key           API key\n  --token             Access token\n  --path              File or bucket path\n</code></pre>"},{"location":"connectors/#managing-connectors","title":"Managing Connectors","text":""},{"location":"connectors/#list-configured-connectors","title":"List Configured Connectors","text":"<pre><code>havn connectors list\n</code></pre> <p>Shows all connectors in <code>project.yml</code> with their type, script path, and status.</p>"},{"location":"connectors/#test-a-connection","title":"Test a Connection","text":"<pre><code>havn connectors test prod_postgres\n</code></pre> <p>Verifies that the connection still works with the stored credentials.</p>"},{"location":"connectors/#sync-data","title":"Sync Data","text":"<pre><code>havn connectors sync prod_postgres\n</code></pre> <p>Runs the generated ingest script for a connector.</p>"},{"location":"connectors/#regenerate-script","title":"Regenerate Script","text":"<pre><code>havn connectors regenerate prod_postgres\n</code></pre> <p>Re-discovers resources and regenerates the ingest script. Useful when the connector code is updated or configuration changes.</p>"},{"location":"connectors/#remove-a-connector","title":"Remove a Connector","text":"<pre><code>havn connectors remove prod_postgres\n</code></pre> <p>Deletes the ingest script and removes the connection from <code>project.yml</code>.</p>"},{"location":"connectors/#connector-architecture","title":"Connector Architecture","text":"<p>Each connector implements the <code>BaseConnector</code> contract:</p> <ul> <li><code>test_connection(config)</code> -- Verify the connection works</li> <li><code>discover(config)</code> -- List available tables/resources</li> <li><code>generate_script(config, tables, target_schema)</code> -- Emit a Python ingest script</li> </ul> <p>The generated ingest script is a standard havn Python script that uses the <code>db</code> DuckDB connection. You can customize it after generation.</p>"},{"location":"connectors/#secret-handling","title":"Secret Handling","text":"<p>Connector parameters marked as <code>secret</code> (passwords, API keys, tokens) are:</p> <ol> <li>Stored in <code>.env</code> as environment variables (e.g., <code>PROD_POSTGRES_PASSWORD=...</code>)</li> <li>Referenced in <code>project.yml</code> as <code>${ENV_VAR_NAME}</code> placeholders</li> <li>Never written to <code>project.yml</code> in plaintext</li> </ol>"},{"location":"connectors/#webhook-connector","title":"Webhook Connector","text":"<p>The webhook connector receives data via HTTP POST and stores it in a landing table:</p> <pre><code>havn connect webhook --name orders_webhook\n</code></pre> <p>Once configured, send data to the webhook endpoint:</p> <pre><code>curl -X POST http://localhost:3000/api/webhook/orders \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\"order_id\": 123, \"amount\": 99.99}'\n</code></pre> <p>Data is stored in <code>landing.&lt;webhook_name&gt;_inbox</code> with columns: <code>id</code>, <code>received_at</code>, <code>payload</code> (JSON).</p>"},{"location":"connectors/#warehouse-connectors","title":"Warehouse Connectors","text":"<p>Install optional dependencies for warehouse connectors:</p> <pre><code>pip install havn[snowflake]     # Snowflake\npip install havn[bigquery]      # BigQuery\npip install havn[redshift]      # Redshift (uses DuckDB postgres extension, no extra deps)\npip install havn[warehouses]    # All warehouse connectors\n</code></pre>"},{"location":"connectors/#snowflake","title":"Snowflake","text":"<pre><code>havn connect snowflake --set account=xy12345.us-east-1 --set warehouse=COMPUTE_WH \\\n  --user myuser --password secret --database PROD --tables customers,orders\n</code></pre> <p>Supports full and CDC incremental sync. Data is transferred via Arrow (<code>fetch_arrow_all()</code>) for high throughput.</p>"},{"location":"connectors/#bigquery","title":"BigQuery","text":"<pre><code>havn connect bigquery --set project_id=my-gcp-project --set dataset=analytics \\\n  --set service_account_b64=&lt;base64-encoded-json&gt; --tables events,users\n</code></pre> <p>Uses Arrow transfer via <code>to_arrow()</code>. Service account credentials are base64-encoded and stored in <code>.env</code>.</p>"},{"location":"connectors/#redshift","title":"Redshift","text":"<pre><code>havn connect redshift --host my-cluster.abc123.us-east-1.redshift.amazonaws.com \\\n  --database analytics --user admin --password secret --tables orders,products\n</code></pre> <p>Uses DuckDB's built-in postgres extension for direct connectivity with no additional Python dependencies.</p>"},{"location":"connectors/#connector-health","title":"Connector Health","text":"<p>Check the last sync status for each connector:</p> <pre><code># Via API\ncurl http://localhost:3000/api/connectors/health\n</code></pre> <p>Returns the most recent run status, timestamp, and duration for each ingest script.</p>"},{"location":"connectors/#related-pages","title":"Related Pages","text":"<ul> <li>CDC -- Incremental sync with change data capture</li> <li>Configuration -- Connection configuration in project.yml</li> <li>Pipelines -- Running connectors as pipeline steps</li> <li>API Reference -- Connector API endpoints</li> </ul>"},{"location":"contracts/","title":"Data Contracts","text":"<p>Data contracts are standalone YAML files that define data quality rules. They complement inline <code>@assert</code> directives in SQL models by providing reusable, centralized quality definitions with severity levels and historical tracking.</p>"},{"location":"contracts/#contract-format","title":"Contract Format","text":"<p>Contracts live in the <code>contracts/</code> directory as YAML files:</p> <pre><code># contracts/orders.yml\ncontracts:\n  - name: orders_not_empty\n    description: \"Orders table must have data\"\n    model: gold.orders\n    severity: error\n    assertions:\n      - row_count &gt; 0\n      - no_nulls(order_id)\n      - unique(order_id)\n      - accepted_values(status, ['pending', 'shipped', 'delivered'])\n      - \"total_amount &gt;= 0\"\n\n  - name: customers_fresh\n    description: \"Customers must be loaded within 24h\"\n    model: silver.customers\n    severity: warn\n    assertions:\n      - row_count &gt; 0\n</code></pre> <p>Each YAML file can contain multiple contracts under the <code>contracts:</code> key.</p>"},{"location":"contracts/#contract-properties","title":"Contract Properties","text":"Property Type Required Default Description <code>name</code> string yes file stem Unique contract identifier <code>model</code> string yes -- Target model as <code>schema.table</code> <code>description</code> string no <code>\"\"</code> Human-readable description <code>severity</code> string no <code>error</code> <code>error</code> or <code>warn</code> <code>assertions</code> list yes -- List of assertion expressions"},{"location":"contracts/#assertion-types","title":"Assertion Types","text":"<p>Contracts support the same assertion expressions as inline <code>@assert</code> directives:</p>"},{"location":"contracts/#row_count-n","title":"<code>row_count &gt; N</code>","text":"<pre><code>assertions:\n  - row_count &gt; 0\n  - row_count &gt; 1000\n  - row_count &gt;= 100\n</code></pre>"},{"location":"contracts/#uniquecolumn","title":"<code>unique(column)</code>","text":"<pre><code>assertions:\n  - unique(order_id)\n  - unique(email)\n</code></pre>"},{"location":"contracts/#no_nullscolumn","title":"<code>no_nulls(column)</code>","text":"<pre><code>assertions:\n  - no_nulls(customer_id)\n  - no_nulls(email)\n</code></pre>"},{"location":"contracts/#accepted_valuescolumn-values","title":"<code>accepted_values(column, [values])</code>","text":"<pre><code>assertions:\n  - accepted_values(status, ['pending', 'shipped', 'delivered'])\n</code></pre>"},{"location":"contracts/#custom-sql-expressions","title":"Custom SQL Expressions","text":"<pre><code>assertions:\n  - \"AVG(amount) &gt; 0\"\n  - \"MAX(created_at) &gt; CURRENT_DATE - INTERVAL '7 days'\"\n</code></pre>"},{"location":"contracts/#severity-levels","title":"Severity Levels","text":""},{"location":"contracts/#error-default","title":"<code>error</code> (default)","text":"<p>Contract failures with <code>error</code> severity cause <code>havn check</code> and <code>havn contracts</code> to exit with code 1. Use this for critical data quality rules that must be enforced.</p>"},{"location":"contracts/#warn","title":"<code>warn</code>","text":"<p>Contract failures with <code>warn</code> severity are reported but do not cause command failure. Use this for aspirational quality goals or non-critical checks.</p> <pre><code>contracts:\n  - name: revenue_positive\n    model: gold.daily_revenue\n    severity: error                # Must pass\n    assertions:\n      - row_count &gt; 0\n\n  - name: high_cardinality\n    model: gold.daily_revenue\n    severity: warn                 # Nice to have\n    assertions:\n      - \"COUNT(DISTINCT region) &gt; 5\"\n</code></pre>"},{"location":"contracts/#running-contracts","title":"Running Contracts","text":""},{"location":"contracts/#run-all-contracts","title":"Run All Contracts","text":"<pre><code>havn contracts\n</code></pre> <p>Discovers all YAML files in <code>contracts/</code>, evaluates each contract, and reports results:</p> <pre><code>Running 3 contract(s)...\n\n  PASS  orders_not_empty (gold.orders) [12ms]\n         pass  row_count &gt; 0\n         pass  unique(order_id)\n         pass  no_nulls(order_id)\n\n  FAIL  customers_fresh (silver.customers) [8ms]\n         pass  row_count &gt; 0\n         FAIL  \"MAX(updated_at) &gt; CURRENT_DATE - INTERVAL '24 hours'\" (value: false)\n\n  PASS  revenue_check (gold.daily_revenue) [5ms]\n         pass  row_count &gt; 0\n\n2 contract(s) passed, 1 failed.\n</code></pre>"},{"location":"contracts/#run-specific-contracts","title":"Run Specific Contracts","text":"<p>Filter by contract name or model name:</p> <pre><code>havn contracts orders_not_empty\nhavn contracts gold.orders\n</code></pre>"},{"location":"contracts/#view-contract-history","title":"View Contract History","text":"<pre><code>havn contracts --history\n</code></pre> <p>Shows the pass/fail history of all contract evaluations, ordered by most recent.</p>"},{"location":"contracts/#contract-history","title":"Contract History","text":"<p>Every contract evaluation is recorded in <code>_havn.contract_results</code>:</p> Column Type Description <code>id</code> VARCHAR Unique result ID <code>contract_name</code> VARCHAR Contract identifier <code>model</code> VARCHAR Target model <code>passed</code> BOOLEAN Whether all assertions passed <code>severity</code> VARCHAR <code>error</code> or <code>warn</code> <code>detail</code> JSON Per-assertion results <code>checked_at</code> TIMESTAMP Evaluation timestamp"},{"location":"contracts/#history-via-api","title":"History via API","text":"<pre><code>curl http://localhost:3000/api/contracts/history\n</code></pre>"},{"location":"contracts/#contracts-via-api","title":"Contracts via API","text":""},{"location":"contracts/#list-contracts","title":"List Contracts","text":"<pre><code>curl http://localhost:3000/api/contracts\n</code></pre> <p>Returns all discovered contracts with their names, models, assertions, and severity.</p>"},{"location":"contracts/#run-contracts","title":"Run Contracts","text":"<pre><code>curl -X POST http://localhost:3000/api/contracts/run\n</code></pre> <p>Returns:</p> <pre><code>{\n  \"total\": 3,\n  \"passed\": 2,\n  \"failed\": 1,\n  \"results\": [\n    {\n      \"contract_name\": \"orders_not_empty\",\n      \"model\": \"gold.orders\",\n      \"passed\": true,\n      \"severity\": \"error\",\n      \"duration_ms\": 12,\n      \"error\": null,\n      \"assertions\": [\n        {\"expression\": \"row_count &gt; 0\", \"passed\": true, \"detail\": \"1234\"}\n      ]\n    }\n  ]\n}\n</code></pre>"},{"location":"contracts/#contracts-in-havn-check","title":"Contracts in havn check","text":"<p>The <code>havn check</code> command includes contract evaluation:</p> <pre><code>havn check\n</code></pre> <p>This runs: 1. SQL model validation 2. Inline assertions (<code>@assert</code>) 3. YAML contracts from <code>contracts/</code></p> <p>All three must pass for <code>havn check</code> to exit with code 0.</p>"},{"location":"contracts/#contracts-vs-inline-assertions","title":"Contracts vs Inline Assertions","text":"Feature Inline Assertions YAML Contracts Location Inside SQL files Separate YAML files Evaluated During <code>havn transform</code> During <code>havn contracts</code> or <code>havn check</code> Severity Always error Configurable (error/warn) History Stored in <code>assertion_results</code> Stored in <code>contract_results</code> Use case Model-specific checks Cross-model, reusable rules <p>Use inline assertions for model-specific quality checks that should run on every build. Use contracts for cross-cutting quality rules, SLA enforcement, and checks that should be run independently of transforms.</p>"},{"location":"contracts/#best-practices","title":"Best Practices","text":"<ol> <li> <p>Start with row_count -- Every critical table should have <code>row_count &gt; 0</code> as a minimum quality check.</p> </li> <li> <p>Enforce primary keys -- Use <code>unique(pk_column)</code> and <code>no_nulls(pk_column)</code> for every table that has a primary key.</p> </li> <li> <p>Use severity wisely -- Reserve <code>error</code> for must-have guarantees. Use <code>warn</code> for aspirational goals or new checks that are not yet enforced.</p> </li> <li> <p>Organize by domain -- Group related contracts in the same YAML file: <code>contracts/finance.yml</code>, <code>contracts/customers.yml</code>, etc.</p> </li> <li> <p>Run in CI/CD -- Add <code>havn contracts</code> to your CI/CD pipeline to catch quality regressions before deployment.</p> </li> </ol>"},{"location":"contracts/#related-pages","title":"Related Pages","text":"<ul> <li>Quality -- Data quality overview</li> <li>Transforms -- Inline assertions in SQL models</li> <li>CLI Reference -- Contract-related commands</li> <li>API Reference -- Contract API endpoints</li> </ul>"},{"location":"environments/","title":"Environments","text":"<p>havn supports multiple environments so you can maintain separate databases for development, staging, and production. Environments are defined in <code>project.yml</code> and selected at runtime via the <code>--env</code> flag or the web UI.</p>"},{"location":"environments/#defining-environments","title":"Defining Environments","text":"<p>Add an <code>environments:</code> section to your <code>project.yml</code>:</p> <pre><code>name: my-project\ndatabase:\n  path: warehouse.duckdb          # Default database path\n\nenvironments:\n  dev:\n    database:\n      path: dev_warehouse.duckdb\n  staging:\n    database:\n      path: staging_warehouse.duckdb\n  prod:\n    database:\n      path: prod_warehouse.duckdb\n  test:\n    database:\n      path: \":memory:\"             # In-memory database for tests\n</code></pre> <p>Each environment can override the <code>database.path</code> setting. When no environment is specified, the top-level <code>database.path</code> is used.</p>"},{"location":"environments/#using-environments","title":"Using Environments","text":""},{"location":"environments/#setting-an-active-environment","title":"Setting an Active Environment","text":"<p>Instead of passing <code>--env</code> on every command, set a persistent active environment:</p> <pre><code># Set the active environment\nhavn env use prod\n\n# Now all commands use prod automatically\nhavn transform              # uses prod\nhavn query \"SELECT 1\"       # uses prod\nhavn jobs run daily-etl      # uses prod\n\n# Check which environment is active\nhavn env show\n\n# List all available environments\nhavn env list\n\n# Reset to default (no environment override)\nhavn env reset\n</code></pre> <p>The active environment is stored in a <code>.havn-env</code> file in the project root. This file is local (added to <code>.gitignore</code>) and not shared with the team.</p> <p>The <code>--env</code> flag still works and overrides the active environment:</p> <pre><code>havn env use prod\nhavn transform --env dev    # overrides to dev for this command only\n</code></pre>"},{"location":"environments/#cli-commands","title":"CLI Commands","text":"<p>Most CLI commands accept an <code>--env</code> flag:</p> <pre><code># Transform against the dev database\nhavn transform --env dev\n\n# Query the production database\nhavn query \"SELECT COUNT(*) FROM gold.customers\" --env prod\n\n# Run a job against staging\nhavn jobs run daily-etl --env staging\n\n# List tables in the dev database\nhavn tables --env dev\n\n# Load seeds into the dev environment\nhavn seed --env dev\n\n# Check freshness in production\nhavn freshness --env prod\n</code></pre>"},{"location":"environments/#web-ui","title":"Web UI","text":"<p>Start the server with a specific environment:</p> <pre><code>havn serve --env staging\n</code></pre> <p>You can also switch environments at runtime through the API:</p> <pre><code>curl -X PUT http://localhost:3000/api/environment/prod \\\n  -H \"Authorization: Bearer &lt;token&gt;\"\n</code></pre> <p>The current environment is shown in the API response:</p> <pre><code>curl http://localhost:3000/api/environment\n</code></pre> <p>Returns:</p> <pre><code>{\n  \"active\": \"staging\",\n  \"available\": [\"dev\", \"staging\", \"prod\", \"test\"],\n  \"database_path\": \"staging_warehouse.duckdb\"\n}\n</code></pre>"},{"location":"environments/#environment-variable-expansion","title":"Environment Variable Expansion","text":"<p>Environment variables in <code>project.yml</code> are resolved from the <code>.env</code> file. This is independent of the <code>environments:</code> feature but works well together:</p> <pre><code>connections:\n  production_db:\n    type: postgres\n    host: ${DB_HOST}\n    password: ${DB_PASSWORD}\n</code></pre> <p>The <code>.env</code> file at the project root:</p> <pre><code>DB_HOST=db.production.internal\nDB_PASSWORD=s3cure_p@ssw0rd\nSLACK_WEBHOOK_URL=https://hooks.slack.com/services/...\n</code></pre>"},{"location":"environments/#per-environment-env-files","title":"Per-Environment .env Files","text":"<p>havn resolves variables from a single <code>.env</code> file. If you need different secrets per environment, manage this outside havn (e.g., using separate <code>.env.dev</code> and <code>.env.prod</code> files and symlinking, or using a secrets manager).</p>"},{"location":"environments/#common-patterns","title":"Common Patterns","text":""},{"location":"environments/#development-vs-production","title":"Development vs Production","text":"<pre><code>environments:\n  dev:\n    database:\n      path: dev_warehouse.duckdb\n  prod:\n    database:\n      path: /data/prod_warehouse.duckdb\n</code></pre> <p>Development:</p> <pre><code>havn transform --env dev\nhavn serve --env dev\n</code></pre> <p>Production:</p> <pre><code>havn jobs run daily-etl --env prod\n</code></pre>"},{"location":"environments/#in-memory-testing","title":"In-Memory Testing","text":"<p>Use <code>:memory:</code> for fast, disposable test databases:</p> <pre><code>environments:\n  test:\n    database:\n      path: \":memory:\"\n</code></pre> <pre><code>havn transform --env test\nhavn check --env test\n</code></pre>"},{"location":"environments/#isolated-feature-development","title":"Isolated Feature Development","text":"<p>Create a per-branch database to avoid conflicts:</p> <pre><code>havn transform --env dev\n</code></pre> <p>Each developer can have their own <code>dev_warehouse.duckdb</code> file that is not checked into version control.</p>"},{"location":"environments/#how-it-works","title":"How It Works","text":"<p>When you specify <code>--env &lt;name&gt;</code>:</p> <ol> <li>havn loads <code>project.yml</code> as normal</li> <li>It looks up the environment by name in <code>environments:</code></li> <li>It overlays the environment-specific settings onto the base config</li> <li>The merged config is used for all operations</li> </ol> <p>Currently, only <code>database.path</code> can be overridden per environment. All other settings (connections, streams, lint config) are shared across environments.</p>"},{"location":"environments/#related-pages","title":"Related Pages","text":"<ul> <li>Configuration -- Full <code>project.yml</code> reference</li> <li>Getting Started -- Project setup</li> <li>Pipelines -- Running streams with environments</li> </ul>"},{"location":"getting-started/","title":"Getting Started","text":"<p>This guide walks you through installing havn, creating your first project, running a pipeline, and exploring data in the web UI.</p>"},{"location":"getting-started/#prerequisites","title":"Prerequisites","text":"<ul> <li>Python 3.10 or later</li> <li>pip (Python package manager)</li> </ul>"},{"location":"getting-started/#installation","title":"Installation","text":""},{"location":"getting-started/#install-from-pypi","title":"Install from PyPI","text":"<pre><code>pip install havn\n</code></pre> <p>The web UI frontend is bundled into the package \u2014 no separate build step needed.</p>"},{"location":"getting-started/#install-from-source-development","title":"Install from Source (development)","text":"<pre><code>git clone https://github.com/chraltro/havn\ncd havn\npip install -e \".[dev]\"\ncd frontend &amp;&amp; npm install &amp;&amp; npm run build\n</code></pre>"},{"location":"getting-started/#docker","title":"Docker","text":"<pre><code>docker run -v $(pwd):/project -p 3000:3000 ghcr.io/chraltro/havn serve\n</code></pre>"},{"location":"getting-started/#create-a-new-project","title":"Create a New Project","text":"<p>Scaffold a new project with <code>havn init</code>:</p> <pre><code>havn init my-project\ncd my-project\n</code></pre> <p>This creates the following structure:</p> <pre><code>my-project/\n  ingest/               # Python scripts and .dpnb notebooks for data ingestion\n    earthquakes.dpnb    # Sample ingest notebook (USGS earthquake data)\n  transform/\n    bronze/             # Light cleanup SQL\n      earthquakes.sql\n    silver/             # Business logic SQL\n      earthquake_events.sql\n      earthquake_daily.sql\n    gold/               # Consumption-ready SQL\n      earthquake_summary.sql\n      top_earthquakes.sql\n      region_risk.sql\n  export/               # Python scripts for exporting data\n    earthquake_report.py\n  seeds/                # CSV reference data\n    magnitude_scale.csv\n  contracts/            # YAML data quality contracts\n    quality.yml\n  notebooks/            # Interactive .dpnb notebooks\n    explore.dpnb\n  project.yml           # Project configuration\n  .env                  # Secrets (never commit this)\n  .gitignore\n  warehouse.duckdb      # Created after first pipeline run\n</code></pre>"},{"location":"getting-started/#run-your-first-pipeline","title":"Run Your First Pipeline","text":"<p>The scaffolded project includes a complete earthquake data pipeline. Run it:</p> <pre><code>havn jobs run full-refresh\n</code></pre> <p>This executes the pipeline steps defined in <code>project.yml</code>:</p> <ol> <li>Ingest -- Fetches earthquake data from the USGS API (falls back to sample data offline)</li> <li>Seed -- Loads <code>seeds/magnitude_scale.csv</code> as a reference table</li> <li>Transform -- Builds SQL models in dependency order: <code>bronze</code> -&gt; <code>silver</code> -&gt; <code>gold</code></li> <li>Export -- Generates a summary report</li> </ol>"},{"location":"getting-started/#explore-your-data","title":"Explore Your Data","text":""},{"location":"getting-started/#list-tables","title":"List Tables","text":"<pre><code>havn tables\n</code></pre> <p>This shows all tables and views in the warehouse, organized by schema.</p>"},{"location":"getting-started/#run-queries","title":"Run Queries","text":"<pre><code>havn query \"SELECT * FROM gold.earthquake_summary LIMIT 10\"\n</code></pre> <p>Output options:</p> <pre><code>havn query \"SELECT * FROM gold.top_earthquakes\" --csv\nhavn query \"SELECT * FROM gold.top_earthquakes\" --json\nhavn query \"SELECT COUNT(*) FROM landing.earthquakes\" --limit 5\n</code></pre>"},{"location":"getting-started/#check-data-quality","title":"Check Data Quality","text":"<pre><code>havn contracts\n</code></pre> <p>This runs all YAML contracts from the <code>contracts/</code> directory and reports pass/fail results.</p>"},{"location":"getting-started/#view-run-history","title":"View Run History","text":"<pre><code>havn history\n</code></pre> <p>Shows recent pipeline runs with status, duration, and row counts.</p>"},{"location":"getting-started/#start-the-web-ui","title":"Start the Web UI","text":"<pre><code>havn serve\n</code></pre> <p>This starts the web server on <code>http://localhost:3000</code> with:</p> <ul> <li>File Browser -- Edit SQL and Python files with Monaco editor</li> <li>Query Panel -- Run ad-hoc SQL queries with autocomplete and <code>$name</code> parameters</li> <li>Table Browser -- Browse schemas, tables, and column profiles</li> <li>DAG Viewer -- Interactive dependency graph visualization</li> <li>Notebook Runner -- Execute <code>.dpnb</code> notebooks interactively</li> <li>Pipeline Controls -- Run streams and view history</li> </ul>"},{"location":"getting-started/#with-authentication","title":"With Authentication","text":"<pre><code>havn serve --auth\n</code></pre> <p>On first launch with <code>--auth</code>, you will be prompted to create an admin user through the web UI. See Auth for details.</p>"},{"location":"getting-started/#project-configuration","title":"Project Configuration","text":"<p>The <code>project.yml</code> file is the central configuration. See Configuration for the full reference. Here is a minimal example:</p> <pre><code>name: my-project\ndatabase:\n  path: warehouse.duckdb\nstreams:\n  full-refresh:\n    description: \"Full pipeline rebuild\"\n    steps:\n      - seed: [all]\n      - ingest: [all]\n      - transform: [all]\n      - export: [all]\n</code></pre>"},{"location":"getting-started/#next-steps","title":"Next Steps","text":"<ul> <li>Transforms -- Learn how to write SQL transform models</li> <li>Pipelines -- Configure multi-step data pipelines</li> <li>Connectors -- Connect to external data sources</li> <li>Quality -- Add data quality checks</li> <li>CLI Reference -- Full command reference</li> </ul>"},{"location":"lineage/","title":"Lineage","text":"<p>havn provides column-level lineage tracking using AST-based SQL analysis. Lineage traces how each output column in a model maps back to its source columns in upstream tables, through CTEs, joins, subqueries, and expressions.</p>"},{"location":"lineage/#how-lineage-works","title":"How Lineage Works","text":"<p>havn uses sqlglot to parse SQL models into an Abstract Syntax Tree (AST) and traces column references through:</p> <ul> <li>Direct column references (<code>SELECT c.name FROM customers c</code>)</li> <li>Aliased expressions (<code>SELECT c.first_name || ' ' || c.last_name AS full_name</code>)</li> <li>CTEs (<code>WITH cte AS (SELECT ...) SELECT cte.col FROM cte</code>)</li> <li>Subqueries</li> <li>Window functions</li> <li>CASE expressions</li> <li>UNION ALL queries</li> <li><code>SELECT *</code> expansion (when a database connection is available)</li> </ul>"},{"location":"lineage/#viewing-lineage","title":"Viewing Lineage","text":""},{"location":"lineage/#cli","title":"CLI","text":"<pre><code>havn lineage gold.earthquake_summary\n</code></pre> <p>Output:</p> <pre><code>Column lineage for gold.earthquake_summary:\n\n  region &lt;- silver.earthquake_events.region\n  total_events &lt;- (computed)\n  avg_magnitude &lt;- silver.earthquake_events.magnitude\n  max_magnitude &lt;- silver.earthquake_events.magnitude\n  latest_event &lt;- silver.earthquake_events.event_time\n</code></pre> <p>JSON output:</p> <pre><code>havn lineage gold.earthquake_summary --json\n</code></pre>"},{"location":"lineage/#api","title":"API","text":"<p>Get lineage for a specific model:</p> <pre><code>curl http://localhost:3000/api/lineage/gold.earthquake_summary\n</code></pre> <p>Returns:</p> <pre><code>{\n  \"model\": \"gold.earthquake_summary\",\n  \"columns\": {\n    \"region\": [\n      {\"source_table\": \"silver.earthquake_events\", \"source_column\": \"region\"}\n    ],\n    \"total_events\": [],\n    \"avg_magnitude\": [\n      {\"source_table\": \"silver.earthquake_events\", \"source_column\": \"magnitude\"}\n    ]\n  },\n  \"depends_on\": [\"silver.earthquake_events\"]\n}\n</code></pre> <p>Get lineage for all models:</p> <pre><code>curl http://localhost:3000/api/lineage\n</code></pre>"},{"location":"lineage/#table-level-dependencies","title":"Table-Level Dependencies","text":""},{"location":"lineage/#auto-extracted-from-sql","title":"Auto-Extracted from SQL","text":"<p>By default, havn parses your <code>FROM</code> and <code>JOIN</code> clauses with <code>sqlglot</code> and uses those references to build the DAG. The example model below depends on <code>bronze.customers</code> and <code>bronze.orders</code> automatically, no directive required:</p> <pre><code>@config materialized=table, schema=silver\n\nSELECT c.customer_id, COUNT(o.order_id) AS order_count\nFROM bronze.customers c\nLEFT JOIN bronze.orders o USING (customer_id)\nGROUP BY 1\n</code></pre>"},{"location":"lineage/#depends_on-override","title":"<code>@depends_on</code> Override","text":"<p>When the parser can't see a reference (a model name passed through a function or built up in a string), declare it explicitly:</p> <pre><code>@depends_on bronze.customers, bronze.orders\n</code></pre> <p>When <code>@depends_on</code> is present, havn uses your list and skips auto-extraction.</p> <p>(Legacy <code>-- depends_on: ...</code> comment syntax still parses for back-compat.)</p>"},{"location":"lineage/#dag-visualization","title":"DAG Visualization","text":"<p>The web UI displays an interactive dependency graph:</p> <pre><code>havn serve\n# Navigate to the DAG tab\n</code></pre> <p>Or via API:</p> <pre><code># Basic DAG (models only)\ncurl http://localhost:3000/api/dag\n\n# Full DAG (models + seeds + sources + exposures + ingest scripts)\ncurl http://localhost:3000/api/dag/full\n</code></pre> <p>The DAG response includes:</p> <ul> <li>Nodes -- Models (table/view), sources, seeds, ingest scripts, exposures</li> <li>Edges -- Dependencies between nodes</li> </ul>"},{"location":"lineage/#impact-analysis","title":"Impact Analysis","text":"<p>Impact analysis answers: \"If I change this model or column, what downstream models are affected?\"</p>"},{"location":"lineage/#cli_1","title":"CLI","text":"<pre><code># Model-level impact\nhavn impact silver.customers\n\n# Column-level impact\nhavn impact silver.customers --column email\n</code></pre> <p>Output:</p> <pre><code>Impact analysis for silver.customers\n  Column: email\n\n  3 downstream model(s) affected:\n    gold.customer_summary\n    gold.email_analytics\n    gold.marketing_segments\n\n  Affected columns:\n    gold.customer_summary.email\n    gold.email_analytics.email_domain\n    gold.marketing_segments.contact_email\n\n  Impact chain:\n    silver.customers -&gt; gold.customer_summary, gold.email_analytics\n    gold.email_analytics -&gt; gold.marketing_segments\n</code></pre> <p>JSON output:</p> <pre><code>havn impact silver.customers --column email --json\n</code></pre>"},{"location":"lineage/#api_1","title":"API","text":"<pre><code># Model-level impact\ncurl http://localhost:3000/api/impact/silver.customers\n\n# Column-level impact\ncurl \"http://localhost:3000/api/impact/silver.customers?column=email\"\n</code></pre> <p>Returns:</p> <pre><code>{\n  \"model\": \"silver.customers\",\n  \"column\": \"email\",\n  \"downstream_models\": [\n    \"gold.customer_summary\",\n    \"gold.email_analytics\"\n  ],\n  \"affected_columns\": [\n    {\"model\": \"gold.customer_summary\", \"column\": \"email\"},\n    {\"model\": \"gold.email_analytics\", \"column\": \"email_domain\"}\n  ],\n  \"impact_chain\": {\n    \"silver.customers\": [\"gold.customer_summary\", \"gold.email_analytics\"]\n  }\n}\n</code></pre>"},{"location":"lineage/#cte-tracing","title":"CTE Tracing","text":"<p>havn traces lineage through CTEs correctly. For example:</p> <pre><code>WITH customer_orders AS (\n    SELECT\n        c.customer_id,\n        c.name,\n        COUNT(o.order_id) AS order_count\n    FROM bronze.customers c\n    LEFT JOIN bronze.orders o ON c.customer_id = o.customer_id\n    GROUP BY 1, 2\n)\nSELECT\n    customer_id,\n    name,\n    order_count,\n    CASE WHEN order_count &gt; 10 THEN 'VIP' ELSE 'regular' END AS tier\nFROM customer_orders\n</code></pre> <p>Lineage for <code>name</code> correctly traces through the CTE back to <code>bronze.customers.name</code>.</p>"},{"location":"lineage/#full-dag-components","title":"Full DAG Components","text":"<p>The full DAG (<code>/api/dag/full</code>) includes all project components:</p> Node Type Description <code>source</code> External data sources declared in <code>sources:</code> <code>seed</code> CSV files from <code>seeds/</code> <code>ingest</code> Python ingest scripts from <code>ingest/</code> <code>import</code> Data imported via the import wizard <code>table</code> SQL models materialized as tables <code>view</code> SQL models materialized as views <code>exposure</code> Downstream consumers declared in <code>exposures:</code> <p>Ingest scripts are linked to their target tables by scanning the script content for <code>CREATE TABLE</code> and <code>INSERT INTO</code> patterns.</p>"},{"location":"lineage/#model-notebook-view","title":"Model Notebook View","text":"<p>The API provides a notebook-style view for each model that combines lineage, SQL source, sample data, and upstream/downstream relationships:</p> <pre><code>curl http://localhost:3000/api/models/gold.earthquake_summary/notebook-view\n</code></pre> <p>Returns the SQL source, sample data rows, column lineage, upstream dependencies, and downstream consumers in a single response.</p>"},{"location":"lineage/#limitations","title":"Limitations","text":"<ul> <li>Dynamic SQL -- Lineage cannot trace through SQL built dynamically in Python ingest scripts.</li> <li>DuckDB-specific syntax -- Some DuckDB-specific functions may fall back to regex-based extraction when sqlglot cannot parse them.</li> <li>SELECT * -- Star expansion requires a live database connection. Without it, <code>SELECT *</code> columns are attributed to the first upstream dependency.</li> </ul>"},{"location":"lineage/#related-pages","title":"Related Pages","text":"<ul> <li>Transforms -- SQL model format and <code>@depends_on</code> overrides</li> <li>Quality -- Using lineage for data quality</li> <li>Sources -- Sources in the DAG</li> <li>Seeds -- Seeds in the DAG</li> <li>API Reference -- Lineage and impact API endpoints</li> </ul>"},{"location":"macros/","title":"Python SQL Macros","text":"<p>havn lets you write Python functions that are callable directly in SQL. No Jinja templating -- just Python functions registered as DuckDB UDFs. \"Why template SQL when you have Python right there?\"</p>"},{"location":"macros/#quick-start","title":"Quick Start","text":"<p>Create a Python file in the <code>macros/</code> directory with <code>@macro</code>-decorated functions:</p> <pre><code># macros/utils.py\nfrom havn import macro\n\n@macro\ndef mask_email(email: str) -&gt; str:\n    \"\"\"Mask the local part of an email, keep domain.\"\"\"\n    if not email or \"@\" not in email:\n        return \"***\"\n    local, domain = email.split(\"@\", 1)\n    return f\"***@{domain}\"\n\n@macro\ndef fiscal_quarter(d: str) -&gt; int:\n    \"\"\"Return fiscal quarter (FY starts April).\"\"\"\n    from datetime import datetime\n    month = datetime.fromisoformat(d).month\n    return ((month - 4) % 12) // 3 + 1\n</code></pre> <p>Use them in any SQL model:</p> <pre><code>-- transform/silver/customers.sql\n@config materialized=table, schema=silver\n\nSELECT\n    customer_id,\n    mask_email(email) AS masked_email,\n    fiscal_quarter(created_date) AS fiscal_q\nFROM bronze.customers\n</code></pre> <p>That's it. The functions are automatically discovered and registered when havn connects to the database.</p>"},{"location":"macros/#how-it-works","title":"How It Works","text":"<ol> <li>On <code>connect()</code>, havn scans the <code>macros/</code> directory for <code>.py</code> files</li> <li>Functions decorated with <code>@macro</code> are collected with their type signatures</li> <li>Each macro is registered as a DuckDB scalar UDF via <code>create_function()</code></li> <li>The functions are then available in any SQL query or transform</li> </ol>"},{"location":"macros/#type-mapping","title":"Type Mapping","text":"<p>Python type hints are mapped to DuckDB types:</p> Python Type DuckDB Type <code>str</code> <code>VARCHAR</code> <code>int</code> <code>INTEGER</code> <code>float</code> <code>DOUBLE</code> <code>bool</code> <code>BOOLEAN</code> <code>datetime.date</code> <code>DATE</code> <code>datetime.datetime</code> <code>TIMESTAMP</code> No annotation <code>VARCHAR</code> (default)"},{"location":"macros/#sql-macros","title":"SQL Macros","text":"<p>You can also place <code>.sql</code> files in <code>macros/</code> containing <code>CREATE MACRO</code> statements:</p> <pre><code>-- macros/date_helpers.sql\nCREATE MACRO quarter_start(d) AS\n    DATE_TRUNC('quarter', d::DATE);\n\nCREATE MACRO is_weekend(d) AS\n    EXTRACT(DOW FROM d::DATE) IN (0, 6);\n</code></pre> <p>SQL macros are executed directly on connection -- they don't need the <code>@macro</code> decorator.</p>"},{"location":"macros/#listing-macros","title":"Listing Macros","text":"<pre><code>havn macros\n</code></pre> <p>Shows all registered macros with their name, parameters, return type, source file, and documentation.</p> <p>Via API:</p> <pre><code>curl http://localhost:3000/api/macros\n</code></pre>"},{"location":"macros/#conventions","title":"Conventions","text":"<ul> <li>Files prefixed with <code>_</code> (e.g., <code>_helpers.py</code>) are skipped</li> <li>Errors in individual macro files are logged but don't prevent other macros from loading</li> <li>Macros are registered before transforms run, so they're available in all SQL models</li> <li><code>havn init</code> creates a <code>macros/</code> directory with example functions</li> </ul>"},{"location":"macros/#related-pages","title":"Related Pages","text":"<ul> <li>Transforms -- Using macros in SQL models</li> <li>CLI Reference -- <code>havn macros</code> command</li> <li>Configuration -- Project structure</li> </ul>"},{"location":"masking/","title":"Data Masking","text":"<p>havn provides column-level data masking to protect sensitive data. Masking is applied via pre-query SQL rewriting (with post-query fallback), preventing alias bypass and inference attacks. Policies support multiple masking methods, conditional application, and role-based exemptions.</p>"},{"location":"masking/#masking-methods","title":"Masking Methods","text":"<p>havn ships fourteen masking methods covering general redaction, PII categories, financial data, and analytics-safe transformations. Use the catalog API <code>GET /api/masking/methods</code> to fetch the live list with config schemas.</p>"},{"location":"masking/#general","title":"General","text":""},{"location":"masking/#hash","title":"hash","text":"<p>Replaces the value with the first 8 characters of its SHA-256 hash. Deterministic per value, NOT JOIN-safe (use <code>consistent_hash</code> for that).</p> <pre><code>\"john@example.com\" -&gt; \"a1b2c3d4\"\n</code></pre>"},{"location":"masking/#redact","title":"redact","text":"<p>Replaces the value with <code>***</code>.</p>"},{"location":"masking/#null","title":"null","text":"<p>Replaces the value with NULL.</p>"},{"location":"masking/#partial","title":"partial","text":"<p>Shows the first and/or last N characters, masking the middle with <code>*</code>.</p> <pre><code># show_first=2, show_last=4\n\"555-123-4567\" -&gt; \"55*******4567\"\n</code></pre> <p>Configuration: <code>show_first</code> (default 0), <code>show_last</code> (default 0).</p>"},{"location":"masking/#truncate","title":"truncate","text":"<p>Shows the first N characters followed by <code>...</code>. Useful for analytics where the column prefix is informative.</p> <p>Configuration: <code>length</code> (default 3).</p>"},{"location":"masking/#pii","title":"PII","text":""},{"location":"masking/#email","title":"email","text":"<p>Hides the local part of an email address, keeps the domain.</p> <pre><code>\"john.doe@company.com\" -&gt; \"***@company.com\"\n</code></pre>"},{"location":"masking/#phone","title":"phone","text":"<p>Keeps the last N digits of a phone number, masks the rest.</p> <p>Configuration: <code>show_last</code> (default 4).</p>"},{"location":"masking/#first_initial","title":"first_initial","text":"<p>Reduces a name to initials separated by periods.</p> <pre><code>\"Jane Smith Doe\" -&gt; \"J. S. D.\"\n</code></pre>"},{"location":"masking/#ip_address","title":"ip_address","text":"<p>Masks host octets of an IPv4 address.</p> <pre><code># keep_octets=2\n\"192.168.1.42\" -&gt; \"192.168.x.x\"\n</code></pre> <p>Configuration: <code>keep_octets</code> (default 2).</p>"},{"location":"masking/#financial","title":"Financial","text":""},{"location":"masking/#credit_card","title":"credit_card","text":"<p>PCI-DSS-compliant: masks all but the last N digits of a credit card number.</p> <pre><code>\"4111-1111-1111-1234\" -&gt; \"************1234\"\n</code></pre> <p>Configuration: <code>show_last</code> (default 4).</p>"},{"location":"masking/#analytics-safe","title":"Analytics-safe","text":""},{"location":"masking/#range","title":"range","text":"<p>Buckets a numeric value into a fixed-size range string. Preserves rough distributions for analytics while hiding precise values.</p> <pre><code># bucket_size=10000\n42_500 -&gt; \"40000-49999\"\n</code></pre> <p>Configuration: <code>bucket_size</code> (default 10000).</p>"},{"location":"masking/#noise","title":"noise","text":"<p>Adds deterministic random noise within <code>+/- percentage</code>. Same input always returns the same output for a given <code>seed_key</code>.</p> <pre><code># percentage=10, seed_key=\"salary\"\n50000 -&gt; 51234   (or 48721, etc., but stable per value)\n</code></pre> <p>Configuration: <code>percentage</code> (default 10.0), <code>seed_key</code> (default \"\").</p>"},{"location":"masking/#date_shift","title":"date_shift","text":"<p>Shifts a date or timestamp by a deterministic random number of days within <code>+/- max_days</code>. Stable per value+seed.</p> <p>Configuration: <code>max_days</code> (default 30), <code>seed_key</code> (default \"\").</p>"},{"location":"masking/#consistent_hash","title":"consistent_hash","text":"<p>JOIN-safe pseudonym: same input always produces the same output. Use this when masked columns are used as join keys.</p> <pre><code># prefix=\"cust_\", length=8\n\"123e4567-...\" -&gt; \"cust_a1b2c3d4\"\n</code></pre> <p>Configuration: <code>prefix</code> (default \"\"), <code>length</code> (default 8).</p>"},{"location":"masking/#creating-masking-policies","title":"Creating Masking Policies","text":""},{"location":"masking/#via-sql-commands","title":"Via SQL Commands","text":"<p>havn intercepts special SQL commands in the query editor:</p> <pre><code>-- Create a policy\nCREATE MASKING POLICY ON gold.customers.email METHOD hash\n\n-- Create with role exemptions\nCREATE MASKING POLICY ON gold.customers.ssn METHOD redact EXEMPT admin,editor\n\n-- Show all policies\nSHOW MASKING POLICIES\n\n-- Drop a policy\nDROP MASKING POLICY &lt;policy_id&gt;\n</code></pre>"},{"location":"masking/#via-rest-api","title":"Via REST API","text":"<pre><code># Create a policy\ncurl -X POST http://localhost:3000/api/masking/policies \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\n    \"schema_name\": \"gold\",\n    \"table_name\": \"customers\",\n    \"column_name\": \"email\",\n    \"method\": \"hash\",\n    \"exempted_roles\": [\"admin\"]\n  }'\n\n# Create a partial masking policy\ncurl -X POST http://localhost:3000/api/masking/policies \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\n    \"schema_name\": \"gold\",\n    \"table_name\": \"customers\",\n    \"column_name\": \"phone\",\n    \"method\": \"partial\",\n    \"method_config\": {\"show_first\": 0, \"show_last\": 4},\n    \"exempted_roles\": [\"admin\"]\n  }'\n</code></pre>"},{"location":"masking/#via-cli","title":"Via CLI","text":"<pre><code>havn mask add --schema gold --table customers --column email --method hash\nhavn mask add --schema gold --table customers --column ssn --method redact\nhavn mask list\nhavn mask remove --id &lt;policy_id&gt;\n</code></pre>"},{"location":"masking/#policy-properties","title":"Policy Properties","text":"Property Type Required Description <code>schema_name</code> string yes Table schema <code>table_name</code> string yes Table name <code>column_name</code> string yes Column to mask <code>method</code> string yes One of: <code>hash</code>, <code>redact</code>, <code>null</code>, <code>partial</code>, <code>truncate</code>, <code>email</code>, <code>phone</code>, <code>first_initial</code>, <code>ip_address</code>, <code>credit_card</code>, <code>range</code>, <code>noise</code>, <code>date_shift</code>, <code>consistent_hash</code> <code>method_config</code> object no Method-specific config (e.g., <code>{\"show_first\": 2}</code>) <code>condition_column</code> string no Column to check for conditional masking <code>condition_value</code> string no Value that triggers masking <code>exempted_roles</code> list no Roles that see unmasked data (default: <code>[\"admin\"]</code>)"},{"location":"masking/#conditional-masking","title":"Conditional Masking","text":"<p>Masking can be applied conditionally based on another column's value:</p> <pre><code>curl -X POST http://localhost:3000/api/masking/policies \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\n    \"schema_name\": \"gold\",\n    \"table_name\": \"customers\",\n    \"column_name\": \"email\",\n    \"method\": \"redact\",\n    \"condition_column\": \"country\",\n    \"condition_value\": \"EU\",\n    \"exempted_roles\": [\"admin\"]\n  }'\n</code></pre> <p>This masks the <code>email</code> column only for rows where <code>country = 'EU'</code>, useful for GDPR compliance.</p>"},{"location":"masking/#role-exemptions","title":"Role Exemptions","text":"<p>By default, only users with the <code>admin</code> role see unmasked data. You can customize this per policy:</p> <pre><code>{\n  \"exempted_roles\": [\"admin\", \"editor\"]\n}\n</code></pre> <p>When a user queries data: 1. havn loads all masking policies 2. For each policy, it checks if the user's role is in <code>exempted_roles</code> 3. If not exempted, the masking function is applied to matching columns</p>"},{"location":"masking/#how-masking-is-applied","title":"How Masking Is Applied","text":"<p>Masking uses a two-tier approach: pre-query SQL rewriting (primary) with a post-query fallback.</p>"},{"location":"masking/#pre-query-rewriting-primary","title":"Pre-query rewriting (primary)","text":"<p>Before a query executes, havn parses the SQL using SQLGlot and rewrites masked columns inline with their masking expressions. This prevents alias bypass -- <code>SELECT email AS x</code> still gets masked because the rewriter operates on the parsed AST, not column names in results. All 14 masking methods are supported in the rewriter.</p>"},{"location":"masking/#post-query-fallback","title":"Post-query fallback","text":"<p>When SQLGlot cannot parse a query (e.g., DuckDB-specific syntax), havn falls back to post-query masking -- applying masking functions to result columns after execution. This ensures masking is always applied, even for edge-case SQL.</p>"},{"location":"masking/#matching-behavior","title":"Matching behavior","text":"<ul> <li>Schema-aware matching -- When querying a specific table (e.g., <code>/api/tables/{schema}/{table}/sample</code>), policies are matched by exact schema and table name</li> <li>Column-name matching -- For ad-hoc queries (<code>/api/query</code>), policies are matched by column name alone (best-effort)</li> <li>Profile masking -- Sample values in table profiles are also masked</li> </ul>"},{"location":"masking/#security-masked-column-access-restrictions","title":"Security: Masked Column Access Restrictions","text":"<p>Non-exempt users are denied from using masked columns in filtering, sorting, joining, or grouping contexts. Specifically, the following operations on masked columns return HTTP 403:</p> <ul> <li>WHERE clauses -- <code>WHERE email = 'x'</code> is blocked</li> <li>ORDER BY clauses -- <code>ORDER BY email</code> is blocked</li> <li>JOIN ON conditions -- <code>JOIN ... ON a.email = b.email</code> is blocked</li> <li>HAVING clauses -- <code>HAVING email IS NOT NULL</code> is blocked</li> </ul> <p>This prevents data exfiltration through inference attacks (e.g., binary search via WHERE filters). Exempt roles (as configured per policy) are not restricted.</p>"},{"location":"masking/#managing-policies","title":"Managing Policies","text":""},{"location":"masking/#list-all-policies","title":"List All Policies","text":"<pre><code>curl http://localhost:3000/api/masking/policies\n</code></pre> <p>Or via SQL:</p> <pre><code>SHOW MASKING POLICIES\n</code></pre>"},{"location":"masking/#get-a-specific-policy","title":"Get a Specific Policy","text":"<pre><code>curl http://localhost:3000/api/masking/policies/&lt;policy_id&gt;\n</code></pre>"},{"location":"masking/#update-a-policy","title":"Update a Policy","text":"<pre><code>curl -X PUT http://localhost:3000/api/masking/policies/&lt;policy_id&gt; \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\"method\": \"partial\", \"method_config\": {\"show_first\": 2, \"show_last\": 0}}'\n</code></pre>"},{"location":"masking/#delete-a-policy","title":"Delete a Policy","text":"<pre><code>curl -X DELETE http://localhost:3000/api/masking/policies/&lt;policy_id&gt;\n</code></pre> <p>Or via SQL:</p> <pre><code>DROP MASKING POLICY &lt;policy_id&gt;\n</code></pre>"},{"location":"masking/#policy-storage","title":"Policy Storage","text":"<p>Policies are stored in <code>_havn.masking_policies</code> in DuckDB:</p> Column Type Description <code>id</code> VARCHAR Auto-generated UUID <code>schema_name</code> VARCHAR Target schema <code>table_name</code> VARCHAR Target table <code>column_name</code> VARCHAR Target column <code>method</code> VARCHAR Masking method <code>method_config</code> JSON Method configuration <code>condition_column</code> VARCHAR Conditional column <code>condition_value</code> VARCHAR Conditional value <code>exempted_roles</code> JSON Roles exempt from masking <code>created_at</code> TIMESTAMP Policy creation time"},{"location":"masking/#related-pages","title":"Related Pages","text":"<ul> <li>Auth -- RBAC roles and permissions</li> <li>Quality -- Data quality framework</li> <li>API Reference -- Masking API endpoints</li> <li>CLI Reference -- Masking CLI commands</li> </ul>"},{"location":"mcp/","title":"MCP Server","text":"<p><code>havn mcp</code> starts a Model Context Protocol server over stdio, exposing your warehouse to AI agents \u2014 Claude Code, Codex CLI, or any other MCP client. The agent can explore tables, run read-only queries, inspect the transform DAG and lineage, query semantic-layer metrics, and (optionally) build models.</p> <p>No extra dependencies: the protocol implementation ships with havn.</p>"},{"location":"mcp/#setup","title":"Setup","text":"<pre><code># Claude Code\nclaude mcp add havn -- havn mcp -p /path/to/your/project\n\n# Any other MCP client: configure a stdio server with\n#   command: havn\n#   args: [\"mcp\", \"-p\", \"/path/to/your/project\"]\n</code></pre> <p>Options:</p> Flag Meaning <code>-p, --project</code> Project directory (default: current dir) <code>-e, --env</code> Environment to use (<code>dev</code>, <code>prod</code>, ...) <code>--read-only</code> Disable the <code>run_transform</code> tool entirely"},{"location":"mcp/#tools","title":"Tools","text":"Tool Description <code>query</code> Read-only SQL (mutations and file-access functions are rejected) <code>list_tables</code> Tables and views, optionally filtered by schema <code>describe_table</code> Columns, types, and row count <code>list_models</code> Transform models with materialization and dependencies <code>get_model</code> A model's SQL source, config, and dependencies <code>model_lineage</code> Direct + transitive upstreams and downstreams <code>run_history</code> Recent pipeline runs from <code>_havn.run_log</code> <code>list_metrics</code> Semantic-layer metrics from <code>metrics/*.yml</code> <code>query_metric</code> Query a metric by dimensions / grain / time range <code>run_transform</code> Build models (omitted with <code>--read-only</code>)"},{"location":"mcp/#how-it-coexists-with-havn-serve","title":"How it coexists with <code>havn serve</code>","text":"<p>DuckDB allows one writing process at a time. When <code>havn serve</code> is running, it holds the warehouse lock \u2014 the MCP server detects this and routes read queries through the server's HTTP API instead of opening the file directly (the same strategy the CLI uses). <code>run_transform</code> refuses to run while the lock is held and tells the agent why.</p>"},{"location":"mcp/#safety-model","title":"Safety model","text":"<p>The MCP server has the same trust level as your local CLI:</p> <ul> <li>Every <code>query</code> / <code>query_metric</code> / introspection call goes through the same   read-only SQL validation as the web API: single statement, no mutation   verbs, no <code>read_csv</code> / <code>read_parquet</code> / HTTP functions.</li> <li>Reads use a read-only DuckDB connection \u2014 even a validator bypass could   not write.</li> <li><code>run_transform</code> is the only mutating tool. It builds your own transform   models (with Pipeline Rewind snapshots when enabled, so agent-triggered   builds can be rolled back like any other run). Start with <code>--read-only</code>   if the agent shouldn't build at all.</li> </ul>"},{"location":"notebooks/","title":"Notebooks","text":"<p>havn includes a custom notebook format (<code>.dpnb</code>) for interactive data exploration, pipeline development, and debugging. Notebooks combine code, SQL, and markdown cells in a single file that can be executed both interactively in the web UI and as part of data pipelines.</p>"},{"location":"notebooks/#notebook-format","title":"Notebook Format","text":"<p><code>.dpnb</code> files are JSON documents with a list of cells:</p> <pre><code>{\n  \"cells\": [\n    {\n      \"type\": \"markdown\",\n      \"source\": \"# Earthquake Data Analysis\\nExploring USGS earthquake data.\"\n    },\n    {\n      \"type\": \"code\",\n      \"source\": \"import requests\\nresponse = requests.get('https://earthquake.usgs.gov/...')\\ndata = response.json()\"\n    },\n    {\n      \"type\": \"sql\",\n      \"source\": \"SELECT COUNT(*) FROM landing.earthquakes\"\n    },\n    {\n      \"type\": \"ingest\",\n      \"source\": \"db.execute('CREATE OR REPLACE TABLE landing.raw AS SELECT * FROM ...')\"\n    }\n  ]\n}\n</code></pre>"},{"location":"notebooks/#cell-types","title":"Cell Types","text":""},{"location":"notebooks/#markdown","title":"markdown","text":"<p>Rendered as formatted text in the web UI. Use for documentation, headers, and explanations:</p> <pre><code>{\n  \"type\": \"markdown\",\n  \"source\": \"## Data Quality Check\\nVerify that all required columns have data.\"\n}\n</code></pre>"},{"location":"notebooks/#code","title":"code","text":"<p>Python code cells. The <code>db</code> DuckDB connection is available, just like in ingest scripts:</p> <pre><code>{\n  \"type\": \"code\",\n  \"source\": \"import pandas as pd\\nresult = db.execute('SELECT * FROM gold.summary').fetchdf()\\nprint(result.describe())\"\n}\n</code></pre>"},{"location":"notebooks/#sql","title":"sql","text":"<p>SQL cells that execute against the DuckDB warehouse:</p> <pre><code>{\n  \"type\": \"sql\",\n  \"source\": \"SELECT region, COUNT(*) AS event_count\\nFROM silver.earthquake_events\\nGROUP BY region\\nORDER BY event_count DESC\\nLIMIT 10\"\n}\n</code></pre> <p>SQL results are displayed as formatted tables in the web UI.</p>"},{"location":"notebooks/#ingest","title":"ingest","text":"<p>Special code cells that write data to the warehouse. Used for data loading operations:</p> <pre><code>{\n  \"type\": \"ingest\",\n  \"source\": \"db.execute('CREATE SCHEMA IF NOT EXISTS landing')\\ndb.execute('CREATE OR REPLACE TABLE landing.data AS ...')\"\n}\n</code></pre>"},{"location":"notebooks/#using-notebooks-in-pipelines","title":"Using Notebooks in Pipelines","text":"<p>Notebooks can be used as ingest or export steps in your pipeline:</p> <pre><code>streams:\n  full-refresh:\n    steps:\n      - ingest: [earthquakes]      # Runs ingest/earthquakes.dpnb\n      - transform: [all]\n      - export: [all]\n</code></pre> <p>Place notebooks in <code>ingest/</code> or <code>export/</code> directories. havn executes all code, SQL, and ingest cells in order, providing the <code>db</code> connection.</p>"},{"location":"notebooks/#pipeline-execution-rules","title":"Pipeline Execution Rules","text":"<ul> <li>Markdown cells are skipped during pipeline execution</li> <li>Code and ingest cells execute with <code>db</code> pre-injected</li> <li>SQL cells execute against the DuckDB connection</li> <li>Cell execution is sequential (top to bottom)</li> <li>Errors in any cell stop execution (for ingest notebooks)</li> </ul>"},{"location":"notebooks/#interactive-execution","title":"Interactive Execution","text":""},{"location":"notebooks/#web-ui","title":"Web UI","text":"<p>Start the web server and navigate to a notebook:</p> <pre><code>havn serve\n</code></pre> <p>The notebook UI provides: - Cell-by-cell execution - SQL result display as tables - Markdown rendering - Output capture for code cells</p>"},{"location":"notebooks/#running-from-cli","title":"Running from CLI","text":"<p>Execute a notebook as a script:</p> <pre><code>havn run ingest/earthquakes.dpnb\n</code></pre> <p>This runs all cells sequentially, logging output to the console.</p>"},{"location":"notebooks/#debug-notebooks","title":"Debug Notebooks","text":"<p>havn can automatically generate debug notebooks for failed models:</p> <pre><code>havn debug silver.customers\n</code></pre> <p>This creates <code>notebooks/debug_silver_customers.dpnb</code> pre-populated with:</p> <ol> <li>Error description -- The error message from the run log</li> <li>Upstream dependency cells -- SQL cells querying each upstream table</li> <li>Failing model SQL -- The model's SQL for interactive editing</li> <li>Assertion diagnostics -- If assertions failed, cells to investigate each failure</li> </ol>"},{"location":"notebooks/#debug-workflow","title":"Debug Workflow","text":"<ol> <li>A transform fails: <code>havn transform</code> shows <code>silver.customers: ERROR</code></li> <li>Generate a debug notebook: <code>havn debug silver.customers</code></li> <li>Open in the web UI: <code>havn serve</code>, navigate to notebooks</li> <li>Execute cells interactively to identify the issue</li> <li>Fix the SQL model and re-run: <code>havn transform</code></li> </ol>"},{"location":"notebooks/#promoting-notebook-sql-to-models","title":"Promoting Notebook SQL to Models","text":"<p>Convert a SQL query from a notebook into a proper transform model:</p> <pre><code>havn promote notebooks/explore.dpnb --name my_model --schema silver\n</code></pre> <p>This: 1. Extracts the last SQL cell from the notebook 2. Auto-detects table dependencies 3. Creates <code>transform/silver/my_model.sql</code> with proper config comments 4. Validates the new model fits in the DAG</p> <p>You can also promote from a literal SQL string or file:</p> <pre><code>havn promote \"SELECT * FROM bronze.data WHERE active = true\" --name active_data --schema silver\nhavn promote query.sql --name my_model --schema gold\n</code></pre>"},{"location":"notebooks/#notebook-api","title":"Notebook API","text":""},{"location":"notebooks/#list-notebooks","title":"List Notebooks","text":"<pre><code>curl http://localhost:3000/api/notebooks\n</code></pre>"},{"location":"notebooks/#read-a-notebook","title":"Read a Notebook","text":"<pre><code>curl http://localhost:3000/api/notebooks/explore.dpnb\n</code></pre>"},{"location":"notebooks/#execute-a-cell","title":"Execute a Cell","text":"<pre><code>curl -X POST http://localhost:3000/api/notebooks/explore.dpnb/execute \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\"cell_index\": 2}'\n</code></pre>"},{"location":"notebooks/#save-a-notebook","title":"Save a Notebook","text":"<pre><code>curl -X PUT http://localhost:3000/api/notebooks/explore.dpnb \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\"cells\": [...]}'\n</code></pre>"},{"location":"notebooks/#conversion","title":"Conversion","text":"<p>Notebooks support conversion between formats:</p> <ul> <li><code>.dpnb</code> to Python script -- Extracts code cells into a <code>.py</code> file</li> <li>Python script to <code>.dpnb</code> -- Wraps a script in a single code cell</li> </ul>"},{"location":"notebooks/#best-practices","title":"Best Practices","text":"<ol> <li> <p>Use notebooks for exploration -- Notebooks are ideal for ad-hoc analysis and debugging. For production transforms, promote queries to SQL model files.</p> </li> <li> <p>Keep pipeline notebooks focused -- Notebooks in <code>ingest/</code> should do one thing: load data. Keep exploration in <code>notebooks/</code>.</p> </li> <li> <p>Use markdown cells -- Document your analysis with markdown cells. Future you will thank present you.</p> </li> <li> <p>Debug with havn debug -- When transforms fail, use <code>havn debug</code> instead of manually creating debug notebooks.</p> </li> <li> <p>Version control notebooks -- <code>.dpnb</code> files are JSON and work well with git. They are part of your project.</p> </li> </ol>"},{"location":"notebooks/#related-pages","title":"Related Pages","text":"<ul> <li>Transforms -- SQL models (the production target for promoted queries)</li> <li>Pipelines -- Using notebooks in pipeline steps</li> <li>Getting Started -- Project structure overview</li> <li>CLI Reference -- Notebook-related commands</li> </ul>"},{"location":"pipelines/","title":"Pipelines","text":"<p>Pipelines in havn are called streams. A stream is an ordered sequence of steps (ingest, seed, transform, export) defined in <code>project.yml</code>. Streams provide a single command to run your entire data pipeline or any subset of it.</p>"},{"location":"pipelines/#defining-streams","title":"Defining Streams","text":"<p>Streams are configured in <code>project.yml</code> under the <code>streams:</code> key:</p> <pre><code>streams:\n  full-refresh:\n    description: \"Full pipeline rebuild\"\n    steps:\n      - seed: [all]\n      - ingest: [all]\n      - transform: [all]\n      - export: [all]\n\n  daily-etl:\n    description: \"Daily incremental ETL\"\n    schedule: \"0 6 * * *\"\n    steps:\n      - ingest: [all]\n      - transform: [all]\n\n  export-only:\n    description: \"Re-export without rebuilding\"\n    steps:\n      - export: [all]\n</code></pre>"},{"location":"pipelines/#stream-steps","title":"Stream Steps","text":"<p>Each step specifies an action and a list of targets:</p>"},{"location":"pipelines/#ingest","title":"Ingest","text":"<p>Runs Python scripts (<code>.py</code>) and notebooks (<code>.dpnb</code>) from the <code>ingest/</code> directory:</p> <pre><code>- ingest: [all]                    # Run all ingest scripts\n- ingest: [customers, orders]     # Run specific scripts\n</code></pre> <p>Scripts prefixed with <code>_</code> (e.g., <code>_helpers.py</code>) are skipped. The <code>db</code> DuckDB connection is pre-injected into each script.</p>"},{"location":"pipelines/#seed","title":"Seed","text":"<p>Loads CSV files from the <code>seeds/</code> directory into DuckDB tables:</p> <pre><code>- seed: [all]                     # Load all seeds\n</code></pre> <p>Seeds use change detection -- only modified CSVs are reloaded. See Seeds.</p>"},{"location":"pipelines/#transform","title":"Transform","text":"<p>Builds SQL models from the <code>transform/</code> directory in dependency order:</p> <pre><code>- transform: [all]               # Build all models\n- transform: [gold.summary]      # Build specific models\n</code></pre> <p>Uses SHA256 change detection to skip unchanged models. See Transforms.</p>"},{"location":"pipelines/#export","title":"Export","text":"<p>Runs Python scripts from the <code>export/</code> directory:</p> <pre><code>- export: [all]                   # Run all export scripts\n- export: [daily_report]         # Run specific scripts\n</code></pre>"},{"location":"pipelines/#running-streams","title":"Running Streams","text":""},{"location":"pipelines/#basic-execution","title":"Basic Execution","text":"<pre><code>havn jobs run full-refresh\n</code></pre> <p>Executes each step in order. If any ingest step fails, the pipeline stops immediately to preserve data integrity.</p>"},{"location":"pipelines/#force-rebuild","title":"Force Rebuild","text":"<pre><code>havn jobs run full-refresh --force\n</code></pre> <p>Forces all transform models to rebuild regardless of change detection.</p>"},{"location":"pipelines/#with-environment","title":"With Environment","text":"<pre><code>havn jobs run daily-etl --env prod\n</code></pre> <p>Uses the production database and environment settings.</p>"},{"location":"pipelines/#error-handling","title":"Error Handling","text":"<p>Streams have built-in error handling:</p> <ul> <li>Ingest failures stop the pipeline -- If an ingest script fails, subsequent steps (transform, export) are not executed. This prevents building models on incomplete data.</li> <li>Transform failures are reported -- Failed models are logged but other independent models continue.</li> <li>Export failures are logged -- Export errors do not affect upstream data.</li> </ul>"},{"location":"pipelines/#retry-support","title":"Retry Support","text":"<p>Streams support automatic retries for transient failures:</p> <pre><code>streams:\n  daily-etl:\n    description: \"Daily ETL with retries\"\n    retries: 3\n    retry_delay: 10\n    steps:\n      - ingest: [all]\n      - transform: [all]\n</code></pre> <ul> <li><code>retries</code> -- Number of retry attempts per failed step (default: 0)</li> <li><code>retry_delay</code> -- Seconds to wait between retries (default: 5)</li> </ul>"},{"location":"pipelines/#webhook-notifications","title":"Webhook Notifications","text":"<p>Get notified when a stream completes or fails:</p> <pre><code>streams:\n  daily-etl:\n    webhook_url: \"https://hooks.slack.com/services/T.../B.../...\"\n    steps:\n      - ingest: [all]\n      - transform: [all]\n</code></pre> <p>The webhook receives a JSON POST with:</p> <pre><code>{\n  \"stream\": \"daily-etl\",\n  \"status\": \"success\",\n  \"duration_seconds\": 12.3,\n  \"timestamp\": \"2025-01-15T06:00:00\"\n}\n</code></pre>"},{"location":"pipelines/#scheduling","title":"Scheduling","text":"<p>Streams can be scheduled with cron expressions:</p> <pre><code>streams:\n  daily-etl:\n    schedule: \"0 6 * * *\"    # 6 AM daily\n    steps:\n      - ingest: [all]\n      - transform: [all]\n</code></pre> <p>Start the scheduler:</p> <pre><code>havn schedule\n</code></pre> <p>See Scheduler for the full cron reference.</p>"},{"location":"pipelines/#python-ingest-scripts","title":"Python Ingest Scripts","text":"<p>Ingest scripts are plain Python files. A DuckDB connection is pre-injected as <code>db</code>:</p> <pre><code># ingest/customers.py\nimport requests\n\nresponse = requests.get(\"https://api.example.com/customers\")\ndata = response.json()\n\ndb.execute(\"CREATE SCHEMA IF NOT EXISTS landing\")\ndb.execute(\"CREATE OR REPLACE TABLE landing.customers AS SELECT * FROM ?\", [data])\n</code></pre>"},{"location":"pipelines/#legacy-format","title":"Legacy Format","text":"<p>The older <code>def run(db)</code> function format is still supported for backward compatibility:</p> <pre><code>def run(db):\n    db.execute(\"CREATE SCHEMA IF NOT EXISTS landing\")\n    db.execute(\"CREATE OR REPLACE TABLE landing.data AS SELECT 1\")\n</code></pre>"},{"location":"pipelines/#script-execution","title":"Script Execution","text":"<ul> <li>Scripts run as top-level code with <code>db</code> available in the namespace</li> <li><code>stdout</code> and <code>stderr</code> are captured and logged</li> <li>Scripts prefixed with <code>_</code> are skipped</li> <li>Script output is masked to prevent leaking secrets from <code>.env</code></li> </ul>"},{"location":"pipelines/#running-individual-steps","title":"Running Individual Steps","text":"<p>You can run steps independently without a stream:</p> <pre><code>havn run ingest/customers.py       # Run a single script\nhavn seed                          # Load all seeds\nhavn transform                     # Build all models\nhavn run export/daily_report.py    # Run a single export\n</code></pre>"},{"location":"pipelines/#pipeline-monitoring","title":"Pipeline Monitoring","text":""},{"location":"pipelines/#run-history","title":"Run History","text":"<pre><code>havn history\n</code></pre> <p>Shows all pipeline runs with type, target, status, duration, and row counts.</p>"},{"location":"pipelines/#freshness","title":"Freshness","text":"<pre><code>havn freshness --hours 24\n</code></pre> <p>Checks which models were last built more than 24 hours ago.</p>"},{"location":"pipelines/#project-status","title":"Project Status","text":"<pre><code>havn status\n</code></pre> <p>Shows project health: git info, warehouse stats, and last run status.</p>"},{"location":"pipelines/#related-pages","title":"Related Pages","text":"<ul> <li>Transforms -- SQL model details</li> <li>Configuration -- Full <code>project.yml</code> reference</li> <li>Scheduler -- Cron scheduling details</li> <li>Connectors -- Automated data ingestion</li> <li>CLI Reference -- All pipeline-related commands</li> </ul>"},{"location":"quality/","title":"Data Quality","text":"<p>havn provides a comprehensive data quality framework with three complementary systems: inline assertions in SQL models, automatic profiling, and freshness monitoring. For standalone quality rules, see Contracts.</p>"},{"location":"quality/#inline-assertions","title":"Inline Assertions","text":"<p>Add <code>@assert</code> directives to SQL model files. Assertions are evaluated after each model builds during <code>havn transform</code>:</p> <pre><code>@config materialized=table, schema=gold\n@assert row_count &gt; 0\n@assert unique(customer_id)\n@assert no_nulls(customer_id)\n@assert no_nulls(email)\n@assert accepted_values(status, ['active', 'inactive', 'suspended'])\n@assert \"lifetime_value &gt;= 0\"\n\nSELECT\n    customer_id,\n    email,\n    status,\n    SUM(order_total) AS lifetime_value\nFROM silver.customers\nGROUP BY 1, 2, 3\n</code></pre> <p>(Legacy <code>-- assert: ...</code> comment syntax still parses for back-compat.)</p>"},{"location":"quality/#available-assertion-types","title":"Available Assertion Types","text":""},{"location":"quality/#row_count-n","title":"<code>row_count &gt; N</code>","text":"<p>Checks that the table has more than N rows:</p> <pre><code>@assert row_count &gt; 0\n@assert row_count &gt; 100\n@assert row_count &gt;= 1000\n</code></pre>"},{"location":"quality/#uniquecolumn","title":"<code>unique(column)</code>","text":"<p>Checks that a column contains no duplicate values:</p> <pre><code>@assert unique(customer_id)\n@assert unique(email)\n</code></pre>"},{"location":"quality/#no_nullscolumn","title":"<code>no_nulls(column)</code>","text":"<p>Checks that a column contains no NULL values:</p> <pre><code>@assert no_nulls(customer_id)\n@assert no_nulls(email)\n</code></pre>"},{"location":"quality/#accepted_valuescolumn-values","title":"<code>accepted_values(column, [values])</code>","text":"<p>Checks that all values in a column are within the allowed set:</p> <pre><code>@assert accepted_values(status, ['active', 'inactive', 'suspended'])\n@assert accepted_values(country_code, ['US', 'CA', 'GB', 'DE'])\n</code></pre>"},{"location":"quality/#custom-sql-expressions","title":"Custom SQL Expressions","text":"<p>Any boolean SQL expression can be used as an assertion. Wrap complex expressions in quotes:</p> <pre><code>@assert \"AVG(amount) &gt; 0\"\n@assert \"MAX(created_at) &gt; CURRENT_DATE - INTERVAL '7 days'\"\n@assert \"COUNT(DISTINCT region) &gt; 1\"\n</code></pre> <p>Custom expressions are evaluated as <code>SELECT (&lt;expression&gt;) FROM &lt;table&gt;</code> and must return a single truthy value.</p>"},{"location":"quality/#assertion-behavior","title":"Assertion Behavior","text":"<ul> <li>Assertions run after a model is built (after <code>CREATE OR REPLACE TABLE</code>)</li> <li>If an assertion fails, the model status is set to <code>assertion_failed</code></li> <li>The model data is not rolled back -- the table exists but is flagged</li> <li>All assertions for a model are evaluated (not short-circuited)</li> <li>Results are stored in <code>_havn.assertion_results</code></li> </ul>"},{"location":"quality/#assertion-debugging","title":"Assertion Debugging","text":"<p>When an assertion fails, havn provides diagnostic details to help you find the problem:</p> <ul> <li><code>unique(col)</code> failure -- shows top 10 duplicated values with counts</li> <li><code>no_nulls(col)</code> failure -- shows null count, percentage, and sample rows with NULLs</li> <li><code>accepted_values(col, [...])</code> failure -- shows unexpected values with counts</li> <li><code>row_count</code> failure -- shows actual count vs threshold</li> </ul> <p>To re-run assertions with full diagnostics on demand:</p> <pre><code>curl http://localhost:3000/api/quality/assertion-debug/gold.customer_summary\n</code></pre> <p>In the web UI, click \"Re-run with diagnostics\" on any failed assertion in the Observe \u2192 Quality panel to see detailed debugging output.</p>"},{"location":"quality/#viewing-assertion-results","title":"Viewing Assertion Results","text":"<pre><code>havn assertions\n</code></pre> <p>Shows recent assertion results with pass/fail status and details.</p> <p>Via API:</p> <pre><code># All assertions\ncurl http://localhost:3000/api/assertions\n\n# For a specific model\ncurl http://localhost:3000/api/assertions/gold.customer_summary\n</code></pre>"},{"location":"quality/#data-profiling","title":"Data Profiling","text":"<p>havn automatically computes column-level statistics for every model after it builds. Profiles include:</p> <ul> <li>Row count -- Total number of rows</li> <li>Column count -- Number of columns</li> <li>Null percentages -- Percentage of NULL values per column</li> <li>Distinct counts -- Number of distinct values per column</li> </ul>"},{"location":"quality/#viewing-profiles","title":"Viewing Profiles","text":"<pre><code># Summary of all models\nhavn profile\n\n# Detailed profile for one model\nhavn profile gold.earthquake_summary\n</code></pre> <p>The detailed view shows per-column statistics:</p> <pre><code>gold.earthquake_summary  (1,234 rows, 8 columns)\n  Profiled at: 2025-01-15 06:00:12\n\n  Column         Null %   Distinct   Status\n  region             0%        42   ok\n  magnitude          0%       156   ok\n  location          12%       891   has nulls\n  depth              0%       734   ok\n</code></pre>"},{"location":"quality/#profile-via-api","title":"Profile via API","text":"<pre><code># All profiles\ncurl http://localhost:3000/api/profiles\n\n# Specific model\ncurl http://localhost:3000/api/profiles/gold.earthquake_summary\n</code></pre>"},{"location":"quality/#table-level-profiling","title":"Table-Level Profiling","text":"<p>The table browser in the web UI provides interactive profiling for any table:</p> <pre><code>curl http://localhost:3000/api/tables/gold/earthquake_summary/profile\n</code></pre> <p>Returns detailed statistics including min/max values, averages (for numeric columns), and sample values.</p>"},{"location":"quality/#freshness-monitoring","title":"Freshness Monitoring","text":"<p>Freshness monitoring detects models that have not been rebuilt within a specified time window.</p>"},{"location":"quality/#check-model-freshness","title":"Check Model Freshness","text":"<pre><code>havn freshness --hours 24\n</code></pre> <p>Shows all models with their last run time and whether they are stale (not rebuilt within 24 hours).</p>"},{"location":"quality/#check-source-freshness","title":"Check Source Freshness","text":"<pre><code>havn freshness --sources\n</code></pre> <p>Checks source freshness against SLAs declared in <code>project.yml</code>. See Sources.</p>"},{"location":"quality/#freshness-alerts","title":"Freshness Alerts","text":"<p>Send alerts for stale models:</p> <pre><code>havn freshness --hours 24 --alert\n</code></pre> <p>This sends notifications via Slack or webhook (requires alert configuration in <code>project.yml</code>).</p>"},{"location":"quality/#freshness-via-api","title":"Freshness via API","text":"<pre><code>curl \"http://localhost:3000/api/freshness?max_hours=24\"\n</code></pre>"},{"location":"quality/#combined-validation","title":"Combined Validation","text":"<p>The <code>havn check</code> command runs all quality checks in one pass:</p> <pre><code>havn check\n</code></pre> <p>This executes:</p> <ol> <li>Model validation -- SQL syntax, dependency resolution, column references</li> <li>Inline assertions -- <code>@assert</code> directives against live data</li> <li>YAML contracts -- Rules from <code>contracts/</code> directory</li> </ol>"},{"location":"quality/#cicd-integration","title":"CI/CD Integration","text":"<p>Use <code>havn check</code> in your CI/CD pipeline:</p> <pre><code>havn check --env test\n</code></pre> <p>Exit code 1 if any validation or assertion fails, making it suitable for automated quality gates.</p>"},{"location":"quality/#alerts","title":"Alerts","text":"<p>havn supports alerting for pipeline events:</p>"},{"location":"quality/#configuration","title":"Configuration","text":"<pre><code>alerts:\n  channels:\n    - slack\n    - webhook\n  slack_webhook_url: ${SLACK_WEBHOOK_URL}\n  webhook_url: \"https://alerts.internal/havn\"\n  on_success: false\n  on_failure: true\n</code></pre>"},{"location":"quality/#alert-types","title":"Alert Types","text":"<ul> <li>Pipeline failure -- Sent when a transform or stream fails</li> <li>Assertion failure -- Sent when data quality assertions fail</li> <li>Stale data -- Sent when models exceed freshness thresholds</li> </ul>"},{"location":"quality/#test-alerts","title":"Test Alerts","text":"<pre><code>curl -X POST http://localhost:3000/api/alerts/test \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\"channel\": \"slack\", \"slack_webhook_url\": \"https://hooks.slack.com/...\"}'\n</code></pre>"},{"location":"quality/#alert-history","title":"Alert History","text":"<pre><code>curl http://localhost:3000/api/alerts\n</code></pre>"},{"location":"quality/#anomaly-detection","title":"Anomaly Detection","text":"<p>havn tracks profile statistics over time and automatically detects anomalies using Z-score analysis. When the current run's metrics deviate significantly from the historical baseline, an anomaly is flagged.</p>"},{"location":"quality/#how-it-works","title":"How It Works","text":"<p>After each transform run, havn compares the current profile (row_count, null percentages, distinct counts) against the last N runs (default 30). If the Z-score exceeds the threshold (default 2.0), an anomaly is logged.</p>"},{"location":"quality/#configuration_1","title":"Configuration","text":"<pre><code>quality:\n  anomaly_detection:\n    enabled: true\n    lookback: 30          # Number of historical runs to compare\n    threshold: 2.0        # Z-score threshold for flagging\n    notify: [slack]       # Alert channels\n</code></pre>"},{"location":"quality/#viewing-anomalies","title":"Viewing Anomalies","text":"<pre><code># Recent anomalies\ncurl http://localhost:3000/api/anomalies\n\n# Anomalies for a specific model\ncurl http://localhost:3000/api/anomalies/gold.customer_summary\n</code></pre> <p>In the web UI, anomalies appear in the Observe \u2192 Quality panel with severity coloring, showing the current value vs expected range.</p>"},{"location":"quality/#edge-cases","title":"Edge Cases","text":"<ul> <li>Fewer than 3 historical profiles \u2192 skipped (not enough baseline data)</li> <li>Zero standard deviation \u2192 skipped (constant value, no deviation possible)</li> <li>First run \u2192 skipped (no baseline)</li> </ul>"},{"location":"quality/#related-pages","title":"Related Pages","text":"<ul> <li>Contracts -- Standalone YAML data quality rules</li> <li>Transforms -- Adding assertions to SQL models</li> <li>Sources -- Source freshness SLAs</li> <li>Lineage -- Understanding data dependencies</li> <li>CLI Reference -- Quality-related commands</li> </ul>"},{"location":"scheduler/","title":"Scheduler","text":"<p>havn includes a built-in scheduler for running streams on cron schedules. It also provides a file watcher that automatically rebuilds transforms when SQL files change.</p>"},{"location":"scheduler/#cron-scheduling","title":"Cron Scheduling","text":""},{"location":"scheduler/#defining-schedules","title":"Defining Schedules","text":"<p>Add a <code>schedule:</code> field to any stream in <code>project.yml</code>:</p> <pre><code>streams:\n  daily-etl:\n    description: \"Daily ETL pipeline\"\n    schedule: \"0 6 * * *\"         # 6 AM every day\n    steps:\n      - ingest: [all]\n      - transform: [all]\n\n  hourly-sync:\n    description: \"Hourly connector sync\"\n    schedule: \"0 * * * *\"         # Every hour on the hour\n    steps:\n      - ingest: [connector_stripe]\n      - transform: [all]\n\n  weekly-report:\n    description: \"Weekly summary export\"\n    schedule: \"0 9 * * 1\"         # 9 AM every Monday\n    steps:\n      - transform: [all]\n      - export: [weekly_report]\n</code></pre>"},{"location":"scheduler/#cron-syntax","title":"Cron Syntax","text":"<p>havn uses standard 5-field cron expressions:</p> <pre><code>\u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500 minute (0 - 59)\n\u2502 \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500 hour (0 - 23)\n\u2502 \u2502 \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500 day of month (1 - 31)\n\u2502 \u2502 \u2502 \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500 month (1 - 12)\n\u2502 \u2502 \u2502 \u2502 \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500 day of week (0 - 6, 0 = Monday)\n\u2502 \u2502 \u2502 \u2502 \u2502\n* * * * *\n</code></pre>"},{"location":"scheduler/#cron-patterns","title":"Cron Patterns","text":"Pattern Meaning <code>*</code> Every unit <code>*/5</code> Every 5 units <code>1,15</code> At 1 and 15 <code>1-5</code> From 1 through 5 <code>30</code> At exactly 30"},{"location":"scheduler/#common-schedules","title":"Common Schedules","text":"<pre><code>\"0 6 * * *\"      # Daily at 6 AM\n\"0 */2 * * *\"    # Every 2 hours\n\"*/15 * * * *\"   # Every 15 minutes\n\"0 9 * * 1\"      # Monday at 9 AM\n\"0 0 1 * *\"      # First day of every month at midnight\n\"30 8 * * 1-5\"   # Weekdays at 8:30 AM\n\"0 6,18 * * *\"   # Twice daily at 6 AM and 6 PM\n</code></pre>"},{"location":"scheduler/#starting-the-scheduler","title":"Starting the Scheduler","text":""},{"location":"scheduler/#cli","title":"CLI","text":"<pre><code>havn schedule\n</code></pre> <p>This shows all scheduled streams and starts the scheduler:</p> <pre><code>Scheduled Streams\n  Stream       Schedule       Description\n  daily-etl    0 6 * * *      Daily ETL pipeline\n  hourly-sync  0 * * * *      Hourly connector sync\n\nStarting scheduler... (Ctrl+C to stop)\n</code></pre> <p>The scheduler runs as a foreground process. Press Ctrl+C to stop it.</p>"},{"location":"scheduler/#how-it-works","title":"How It Works","text":"<ol> <li>The scheduler thread starts and reads schedules from <code>project.yml</code></li> <li>Every 30 seconds, it checks all cron expressions against the current time</li> <li>When a schedule matches the current minute, the corresponding stream is executed</li> <li>Each stream runs at most once per minute (deduplication)</li> <li>Config is re-read on each check, so schedule changes take effect without restart</li> </ol>"},{"location":"scheduler/#with-the-web-server","title":"With the Web Server","text":"<p>When you run <code>havn serve</code>, the scheduler is not started automatically. Start it separately in another terminal:</p> <pre><code>havn schedule\n</code></pre> <p>Or configure your deployment to run both processes.</p>"},{"location":"scheduler/#file-watcher","title":"File Watcher","text":"<p>The file watcher monitors <code>transform/</code> and <code>ingest/</code> for changes and automatically triggers rebuilds.</p>"},{"location":"scheduler/#starting-the-watcher","title":"Starting the Watcher","text":"<pre><code>havn watch\n</code></pre> <p>Output:</p> <pre><code>Watching for changes... (Ctrl+C to stop)\n  transform/  -&gt; auto-rebuild SQL models\n</code></pre>"},{"location":"scheduler/#how-it-works_1","title":"How It Works","text":"<ol> <li>Uses the <code>watchdog</code> library to monitor filesystem events</li> <li>Watches <code>transform/</code> and <code>ingest/</code> directories recursively</li> <li>When a <code>.sql</code> or <code>.py</code> file is modified, it triggers an action</li> <li>For <code>transform/</code> changes: runs <code>havn transform</code></li> <li>Includes 2-second debouncing to avoid multiple rebuilds for rapid saves</li> </ol>"},{"location":"scheduler/#example-session","title":"Example Session","text":"<pre><code>Watching for changes... (Ctrl+C to stop)\n\nWatcher: transform/silver/dim_customer.sql changed\nWatcher: Running transform...\n  skip  bronze.customers\n  done  silver.dim_customer (1,234 rows, 45ms)\n  done  gold.customer_summary (892 rows, 12ms)\nWatcher: Transform completed\n</code></pre>"},{"location":"scheduler/#scheduler-architecture","title":"Scheduler Architecture","text":""},{"location":"scheduler/#schedulerthread","title":"SchedulerThread","text":"<p>The scheduler runs as a daemon thread with a simple polling loop:</p> <ul> <li>Checks cron expressions every 30 seconds</li> <li>Reloads <code>project.yml</code> on each iteration (picks up config changes)</li> <li>Executes matching streams in the same thread (sequential)</li> <li>Logs all activity to the console and Python logging</li> </ul>"},{"location":"scheduler/#huey-integration","title":"Huey Integration","text":"<p>havn also includes a Huey-based scheduler (SqliteHuey) for more robust task queuing. The SQLite-backed Huey instance stores task state in <code>.havn_scheduler.db</code> at the project root.</p>"},{"location":"scheduler/#filewatcher","title":"FileWatcher","text":"<p>The file watcher runs as a separate daemon thread using the <code>watchdog</code> library. It:</p> <ul> <li>Monitors <code>transform/</code> and <code>ingest/</code> for <code>.sql</code> and <code>.py</code> changes</li> <li>Debounces rapid changes (2-second window)</li> <li>Creates a fresh database connection for each rebuild</li> <li>Reports results to the console</li> </ul>"},{"location":"scheduler/#scheduler-status","title":"Scheduler Status","text":""},{"location":"scheduler/#via-api","title":"Via API","text":"<pre><code>curl http://localhost:3000/api/scheduler\n</code></pre> <p>Returns:</p> <pre><code>{\n  \"scheduled_streams\": [\n    {\n      \"name\": \"daily-etl\",\n      \"description\": \"Daily ETL pipeline\",\n      \"schedule\": \"0 6 * * *\",\n      \"steps\": [\n        {\"action\": \"ingest\", \"targets\": [\"all\"]},\n        {\"action\": \"transform\", \"targets\": [\"all\"]}\n      ]\n    }\n  ]\n}\n</code></pre>"},{"location":"scheduler/#deployment-considerations","title":"Deployment Considerations","text":""},{"location":"scheduler/#running-as-a-service","title":"Running as a Service","text":"<p>For production deployments, run the scheduler as a background service:</p> <pre><code># Using systemd (Linux)\n# Create /etc/systemd/system/havn-scheduler.service\n\n# Using nohup\nnohup havn schedule &gt; /var/log/havn-scheduler.log 2&gt;&amp;1 &amp;\n</code></pre>"},{"location":"scheduler/#multiple-workers","title":"Multiple Workers","text":"<p>The scheduler runs streams sequentially within a single thread. For parallel execution of independent streams, consider running multiple scheduler instances or using external orchestration tools.</p>"},{"location":"scheduler/#monitoring","title":"Monitoring","text":"<ul> <li>Check <code>havn history</code> to verify scheduled runs are completing</li> <li>Use <code>havn freshness</code> to detect if scheduled pipelines have stopped running</li> <li>Configure webhook notifications on streams for immediate failure alerts</li> </ul>"},{"location":"scheduler/#related-pages","title":"Related Pages","text":"<ul> <li>Pipelines -- Stream configuration and execution</li> <li>Configuration -- Schedule configuration in project.yml</li> <li>Connectors -- Scheduling connector syncs</li> <li>CLI Reference -- Schedule and watch commands</li> </ul>"},{"location":"seeds/","title":"Seeds","text":"<p>Seeds are CSV files that are loaded into DuckDB as reference tables. They provide a simple way to include static or slowly-changing data (lookup tables, configuration data, reference codes) in your data warehouse without writing ingest scripts.</p>"},{"location":"seeds/#directory-structure","title":"Directory Structure","text":"<p>Place CSV files in the <code>seeds/</code> directory at the project root:</p> <pre><code>my-project/\n  seeds/\n    country_codes.csv\n    magnitude_scale.csv\n    status_definitions.csv\n</code></pre> <p>Each CSV file becomes a table in the <code>seeds</code> schema. For example, <code>seeds/country_codes.csv</code> creates <code>seeds.country_codes</code>.</p>"},{"location":"seeds/#csv-format","title":"CSV Format","text":"<p>Seeds are standard CSV files with a header row:</p> <pre><code>code,name,magnitude_min,magnitude_max,description\nmicro,Micro,0.0,1.9,\"Generally not felt\"\nminor,Minor,2.0,3.9,\"Often felt, rarely causes damage\"\nlight,Light,4.0,4.9,\"Noticeable shaking, minor damage\"\nmoderate,Moderate,5.0,5.9,\"Can cause damage to poorly constructed buildings\"\nstrong,Strong,6.0,6.9,\"Can cause severe damage in populated areas\"\nmajor,Major,7.0,7.9,\"Can cause serious damage over large areas\"\ngreat,Great,8.0,9.9,\"Can cause devastating damage near the epicenter\"\n</code></pre> <p>DuckDB auto-detects column types from the CSV content. Headers with special characters are handled automatically.</p>"},{"location":"seeds/#loading-seeds","title":"Loading Seeds","text":""},{"location":"seeds/#load-all-seeds","title":"Load All Seeds","text":"<pre><code>havn seed\n</code></pre> <p>Seeds use change detection via content hashing. Only modified CSV files are reloaded.</p>"},{"location":"seeds/#force-reload","title":"Force Reload","text":"<pre><code>havn seed --force\n</code></pre> <p>Reloads all seeds regardless of whether they have changed.</p>"},{"location":"seeds/#custom-schema","title":"Custom Schema","text":"<pre><code>havn seed --schema reference_data\n</code></pre> <p>Loads seeds into a schema other than the default <code>seeds</code>.</p>"},{"location":"seeds/#as-part-of-a-stream","title":"As Part of a Stream","text":"<pre><code>streams:\n  full-refresh:\n    steps:\n      - seed: [all]\n      - ingest: [all]\n      - transform: [all]\n</code></pre>"},{"location":"seeds/#change-detection","title":"Change Detection","text":"<p>havn tracks seed changes using SHA256 content hashing:</p> <ol> <li>When a seed is loaded, a hash of the CSV file content is computed</li> <li>The hash is stored in <code>_havn.model_state</code></li> <li>On subsequent runs, the current hash is compared against the stored hash</li> <li>If the hash matches, the seed is skipped</li> <li>If the hash differs, the seed is reloaded</li> </ol> <p>This makes repeated <code>havn seed</code> calls fast -- only changed CSVs are processed.</p>"},{"location":"seeds/#using-seeds-in-transforms","title":"Using Seeds in Transforms","text":"<p>Reference seed tables in your SQL models:</p> <pre><code>@config materialized=table, schema=silver\n\nSELECT\n    e.earthquake_id,\n    e.magnitude,\n    m.name AS magnitude_category,\n    m.description AS magnitude_description\nFROM bronze.earthquakes e\nLEFT JOIN seeds.magnitude_scale m\n    ON e.magnitude BETWEEN m.magnitude_min AND m.magnitude_max\n</code></pre> <p>havn auto-detects <code>seeds.magnitude_scale</code> from the <code>LEFT JOIN</code> clause and includes it in the DAG. Add <code>@depends_on</code> only if you reference a seed through a function call or string interpolation that the parser can't see.</p>"},{"location":"seeds/#seed-status-in-the-dag","title":"Seed Status in the DAG","text":"<p>Seeds appear as special nodes in the DAG visualization (web UI). They are shown with a distinct icon to differentiate them from SQL models and ingest scripts.</p>"},{"location":"seeds/#empty-csvs","title":"Empty CSVs","text":"<p>If a CSV file is empty (contains only whitespace or newlines), havn creates an empty table with a single <code>empty_file BOOLEAN</code> column. This prevents errors in downstream models that reference the seed.</p>"},{"location":"seeds/#discovering-seeds","title":"Discovering Seeds","text":""},{"location":"seeds/#cli","title":"CLI","text":"<pre><code>havn seed\n</code></pre> <p>Lists all discovered CSV files and their load status (built, skipped, or error).</p>"},{"location":"seeds/#api","title":"API","text":"<pre><code>curl http://localhost:3000/api/seeds\n</code></pre> <p>Returns a list of seed files with their names, schemas, and file paths.</p>"},{"location":"seeds/#loading-via-api","title":"Loading via API","text":"<pre><code>curl -X POST http://localhost:3000/api/seeds \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\"force\": false, \"schema_name\": \"seeds\"}'\n</code></pre>"},{"location":"seeds/#best-practices","title":"Best Practices","text":"<ol> <li> <p>Keep seeds small -- Seeds are best for lookup tables and reference data (hundreds to low thousands of rows). For larger datasets, use ingest scripts.</p> </li> <li> <p>Use descriptive filenames -- The filename becomes the table name. Use <code>country_codes.csv</code> not <code>data.csv</code>.</p> </li> <li> <p>Include headers -- Always include a header row. DuckDB uses it for column naming.</p> </li> <li> <p>Version control seeds -- CSV seed files should be committed to git. They are part of your project definition.</p> </li> <li> <p>Reference seeds in SQL -- <code>FROM seeds.&lt;name&gt;</code> (or <code>JOIN seeds.&lt;name&gt;</code>) is enough; havn auto-detects the dependency for DAG ordering. Use <code>@depends_on</code> only if the seed reference isn't visible in plain SQL.</p> </li> </ol>"},{"location":"seeds/#related-pages","title":"Related Pages","text":"<ul> <li>Transforms -- Using seeds in SQL models</li> <li>Pipelines -- Loading seeds as a pipeline step</li> <li>Configuration -- Project configuration</li> <li>Lineage -- Seeds in the dependency graph</li> </ul>"},{"location":"semantic-layer/","title":"Semantic Layer","text":"<p>Define a metric once in YAML, query it consistently everywhere \u2014 the CLI, the API, dashboards, and AI agents via MCP. No more five slightly different definitions of \"revenue\" scattered across saved queries.</p>"},{"location":"semantic-layer/#defining-metrics","title":"Defining metrics","text":"<p>Metrics live in YAML files under <code>metrics/</code> in your project root:</p> <pre><code># metrics/revenue.yml\nmetrics:\n  - name: revenue\n    description: Total order revenue\n    model: gold.orders            # table or view to aggregate over\n    measure: SUM(amount)          # any SQL aggregate expression\n    dimensions: [region, status]  # columns that may be grouped by\n    time_dimension: order_date    # column used for time bucketing\n    filters:\n      - status != 'cancelled'     # always-applied WHERE predicates\n</code></pre> Key Required Meaning <code>name</code> yes Metric identifier (letters, digits, underscores) <code>model</code> yes Schema-qualified table or view, e.g. <code>gold.orders</code> <code>measure</code> yes SQL aggregate expression, e.g. <code>SUM(amount)</code>, <code>COUNT(DISTINCT user_id)</code> <code>description</code> no Shown in listings <code>dimensions</code> no Whitelist of columns callers may group by <code>time_dimension</code> no Column used for <code>--grain</code> / <code>--start</code> / <code>--end</code> <code>filters</code> no Predicates ANDed into every query <p>One file can declare many metrics; many files are merged. Invalid definitions are reported but don't break loading of the valid ones.</p>"},{"location":"semantic-layer/#querying-metrics","title":"Querying metrics","text":"<pre><code>havn metrics                       # list all metrics\nhavn metrics sql revenue --by region --grain month     # show compiled SQL\nhavn metrics query revenue --by region --grain month   # run it\nhavn metrics query revenue --start 2026-01-01 --end 2026-02-01 --json\n</code></pre> <p><code>havn metrics query</code> compiles the metric to a single <code>SELECT</code> and runs it read-only \u2014 routed through a running <code>havn serve</code> when one holds the warehouse lock, exactly like <code>havn query</code>.</p> <p>The compiled SQL for the example above:</p> <pre><code>SELECT\n    DATE_TRUNC('month', order_date) AS month,\n    region,\n    SUM(amount) AS revenue\nFROM gold.orders\nWHERE (status != 'cancelled')\nGROUP BY 1, 2\nORDER BY 1, 2\n</code></pre>"},{"location":"semantic-layer/#api","title":"API","text":"Endpoint Description <code>GET /api/semantic/metrics</code> List metric definitions + load errors <code>POST /api/semantic/compile</code> Compile a metric query to SQL without running it <code>POST /api/semantic/query</code> Compile and execute <p><code>POST /api/semantic/query</code> body:</p> <pre><code>{\n  \"metric\": \"revenue\",\n  \"dimensions\": [\"region\"],\n  \"grain\": \"month\",\n  \"start\": \"2026-01-01\",\n  \"end\": \"2026-07-01\",\n  \"limit\": 1000\n}\n</code></pre> <p>Responses include the compiled <code>sql</code> alongside <code>columns</code> / <code>rows</code>, so it's easy to inspect what actually ran. Masking policies apply to API metric queries the same way they apply to <code>/api/query</code>.</p>"},{"location":"semantic-layer/#safety","title":"Safety","text":"<ul> <li>Requested dimensions must be declared on the metric \u2014 anything else is   rejected before SQL is generated.</li> <li>Grains are restricted to <code>hour</code>, <code>day</code>, <code>week</code>, <code>month</code>, <code>quarter</code>, <code>year</code>.</li> <li><code>start</code> / <code>end</code> values are escaped string literals cast to <code>TIMESTAMP</code>.</li> <li>The compiled statement is run through the same read-only validation as   <code>/api/query</code> (no mutations, no file-access functions), and through the   masking rewriter when queried via the API.</li> </ul>"},{"location":"sources/","title":"Sources","text":"<p>Sources are declarations of external data that your havn project depends on. They document where data comes from, define freshness SLAs, and provide metadata for column-level validation and documentation.</p>"},{"location":"sources/#declaring-sources","title":"Declaring Sources","text":"<p>Sources are defined in <code>project.yml</code> under the <code>sources:</code> key:</p> <pre><code>sources:\n  - name: production_db\n    schema: landing\n    description: \"Production PostgreSQL database\"\n    connection: prod_postgres\n    freshness_hours: 24\n    tables:\n      - name: customers\n        description: \"Customer records from the CRM\"\n        loaded_at_column: updated_at\n        columns:\n          - name: customer_id\n            description: \"Primary key, auto-incremented\"\n          - name: email\n            description: \"Customer email address\"\n          - name: created_at\n            description: \"Account creation timestamp\"\n      - name: orders\n        description: \"E-commerce order records\"\n        loaded_at_column: order_date\n        columns:\n          - name: order_id\n            description: \"Unique order identifier\"\n          - name: customer_id\n            description: \"Foreign key to customers\"\n          - name: total_amount\n            description: \"Order total in USD\"\n</code></pre>"},{"location":"sources/#source-properties","title":"Source Properties","text":""},{"location":"sources/#source-level","title":"Source-Level","text":"Property Type Required Description <code>name</code> string yes Identifier for the source <code>schema</code> string yes DuckDB schema where data lands (e.g., <code>landing</code>) <code>description</code> string no Human-readable description <code>connection</code> string no Reference to a connection in <code>project.yml</code> <code>freshness_hours</code> float no Maximum age in hours before data is considered stale"},{"location":"sources/#table-level","title":"Table-Level","text":"Property Type Required Description <code>name</code> string yes Table name <code>description</code> string no Table description <code>loaded_at_column</code> string no Timestamp column for freshness checks <code>columns</code> list no Column definitions"},{"location":"sources/#column-level","title":"Column-Level","text":"Property Type Required Description <code>name</code> string yes Column name <code>description</code> string no Column description"},{"location":"sources/#freshness-monitoring","title":"Freshness Monitoring","text":"<p>When <code>freshness_hours</code> is set, havn can check whether source data is stale:</p>"},{"location":"sources/#cli","title":"CLI","text":"<pre><code>havn freshness --sources\n</code></pre>"},{"location":"sources/#api","title":"API","text":"<pre><code>curl http://localhost:3000/api/sources/freshness\n</code></pre> <p>Returns:</p> <pre><code>[\n  {\n    \"source\": \"production_db\",\n    \"table\": \"landing.customers\",\n    \"sla_hours\": 24,\n    \"last_loaded\": \"2025-01-14T06:00:00\",\n    \"hours_ago\": 28.5,\n    \"is_stale\": true\n  }\n]\n</code></pre>"},{"location":"sources/#how-freshness-is-determined","title":"How Freshness Is Determined","text":"<p>havn checks freshness in two ways:</p> <ol> <li>loaded_at_column -- If specified, havn queries <code>MAX(loaded_at_column)</code> from the table to find the most recent data timestamp.</li> <li>Run log fallback -- If no <code>loaded_at_column</code> is set, havn checks the most recent successful run in <code>_havn.run_log</code> for that table.</li> </ol> <p>A source is marked stale if the hours since the last load exceed <code>freshness_hours</code>.</p>"},{"location":"sources/#freshness-alerts","title":"Freshness Alerts","text":"<p>Combine freshness checks with alerting:</p> <pre><code>havn freshness --hours 24 --alert\n</code></pre> <p>This sends a Slack or webhook notification for any stale sources (requires alerts configured in <code>project.yml</code>).</p>"},{"location":"sources/#sources-in-validation","title":"Sources in Validation","text":"<p>Source declarations are used during <code>havn check</code> validation:</p> <ol> <li> <p>Dependency resolution -- Source tables are recognized as valid upstreams when auto-extracted from <code>FROM</code>/<code>JOIN</code> or declared via <code>@depends_on</code>, preventing false \"unknown table\" warnings.</p> </li> <li> <p>Column validation -- If columns are declared in a source, havn validates that SQL models referencing those tables use valid column names.</p> </li> </ol> <pre><code>havn check\n</code></pre> <p>Example output when a model references a non-existent source column:</p> <pre><code>  warn  silver.customers: column 'non_existent' not found in source landing.customers\n</code></pre>"},{"location":"sources/#sources-in-the-dag","title":"Sources in the DAG","text":"<p>Source tables appear as special nodes in the DAG visualization. They are shown with a \"source\" type and include their description. This provides visibility into where data originates when viewing the dependency graph.</p>"},{"location":"sources/#full-dag","title":"Full DAG","text":"<p>The full DAG view (<code>/api/dag/full</code>) includes sources, seeds, models, and exposures:</p> <pre><code>curl http://localhost:3000/api/dag/full\n</code></pre>"},{"location":"sources/#sources-in-documentation","title":"Sources in Documentation","text":"<p>Source metadata appears in the auto-generated documentation:</p> <pre><code># Via API\ncurl http://localhost:3000/api/docs/markdown\n</code></pre> <p>This generates Markdown documentation that includes source tables, their descriptions, column definitions, and freshness status.</p>"},{"location":"sources/#listing-sources","title":"Listing Sources","text":""},{"location":"sources/#cli_1","title":"CLI","text":"<p>Sources are displayed as part of the project context:</p> <pre><code>havn context\n</code></pre>"},{"location":"sources/#api_1","title":"API","text":"<pre><code>curl http://localhost:3000/api/sources\n</code></pre> <p>Returns all declared sources with their tables and columns.</p>"},{"location":"sources/#best-practices","title":"Best Practices","text":"<ol> <li> <p>Declare all external dependencies -- Every table in <code>landing</code> that comes from an external source should have a source declaration. This enables validation and documentation.</p> </li> <li> <p>Set freshness SLAs -- Define <code>freshness_hours</code> for critical sources. Use <code>havn freshness</code> in CI/CD to catch data delivery issues.</p> </li> <li> <p>Document columns -- Column descriptions flow into auto-generated documentation and help team members understand the data.</p> </li> <li> <p>Use loaded_at_column -- When available, specify the timestamp column for more accurate freshness checks.</p> </li> </ol>"},{"location":"sources/#related-pages","title":"Related Pages","text":"<ul> <li>Quality -- Data quality overview including freshness</li> <li>Configuration -- Full project.yml reference</li> <li>Lineage -- Sources in the dependency graph</li> <li>Transforms -- Referencing sources in SQL models</li> </ul>"},{"location":"transforms/","title":"SQL Transforms","text":"<p>SQL transforms are the core of havn's data pipeline. Every <code>.sql</code> file in the <code>transform/</code> directory is a model that produces a table or view in DuckDB. Models are parsed, ordered by dependency, and executed automatically.</p>"},{"location":"transforms/#file-structure","title":"File Structure","text":"<p>Models are organized into subdirectories that correspond to schemas:</p> <pre><code>transform/\n  bronze/           # Schema: bronze\n    customers.sql\n    orders.sql\n  silver/           # Schema: silver\n    dim_customer.sql\n    fact_orders.sql\n  gold/             # Schema: gold\n    customer_summary.sql\n</code></pre> <p>The folder name determines the default schema. A file at <code>transform/silver/dim_customer.sql</code> produces a table at <code>silver.dim_customer</code>.</p>"},{"location":"transforms/#sql-model-format","title":"SQL Model Format","text":"<p>Every SQL model file starts with directive lines followed by a SELECT statement:</p> <pre><code>@config materialized=table, schema=silver\n@description Customer dimension with order counts\n@col customer_id: Unique customer identifier\n@col order_count: Total number of orders\n@assert row_count &gt; 0\n@assert unique(customer_id)\n@assert no_nulls(customer_id)\n\nSELECT\n    c.customer_id,\n    c.name,\n    c.email,\n    COUNT(o.order_id)   AS order_count,\n    SUM(o.total_amount) AS lifetime_value\nFROM bronze.customers c\nLEFT JOIN bronze.orders o ON c.customer_id = o.customer_id\nGROUP BY 1, 2, 3\n</code></pre> <p>Notice there is no <code>@depends_on</code> line: havn parses the <code>FROM</code> and <code>JOIN</code> clauses with <code>sqlglot</code> and adds <code>bronze.customers</code> and <code>bronze.orders</code> to the DAG automatically. You only need <code>@depends_on</code> when the parser can't see a reference (e.g. a table name passed through a function or string-built dynamically).</p>"},{"location":"transforms/#directives","title":"Directives","text":"<p>Every directive supports two forms -- bare and parenthesised:</p> <pre><code>@config materialized=table, schema=gold\n@config(materialized=table, schema=gold)\n</code></pre> <p>Both parse identically. Pick whichever reads better in your editor; the bare form is canonical in the kit and templates.</p>"},{"location":"transforms/#config","title":"<code>@config</code>","text":"<p>Sets materialization, schema, and per-model engine settings:</p> <pre><code>@config materialized=table, schema=gold, unique_key=customer_id, incremental_strategy=delete+insert\n</code></pre> Key Values Default <code>materialized</code> <code>table</code>, <code>view</code>, <code>incremental</code> <code>view</code> <code>schema</code> any valid schema name folder name (e.g. <code>bronze</code> for <code>transform/bronze/</code>) <code>unique_key</code> column name none (required for incremental merges) <code>incremental_strategy</code> <code>delete+insert</code>, <code>merge</code>, <code>append</code> <code>delete+insert</code> <code>incremental_filter</code> SQL expression (e.g. <code>event_time &gt;= ...</code>) none <code>partition_by</code> column name none"},{"location":"transforms/#depends_on-optional","title":"<code>@depends_on</code> (optional)","text":"<p>Declares upstream dependencies explicitly. Auto-extraction handles most cases:</p> <pre><code>@depends_on bronze.customers, bronze.orders\n</code></pre> <p>Use this when a dependency isn't visible in plain SQL (e.g. a model name interpolated through a Python macro). When you write <code>@depends_on</code>, havn uses your list and skips auto-extraction.</p>"},{"location":"transforms/#description","title":"<code>@description</code>","text":"<p>One-line model description, surfaces in the catalog and UI:</p> <pre><code>@description Customer dimension table with lifetime metrics\n</code></pre>"},{"location":"transforms/#col","title":"<code>@col</code>","text":"<p>Per-column documentation. Repeat one line per documented column:</p> <pre><code>@col customer_id: Unique customer identifier\n@col lifetime_value: Sum of all settled non-reversed order amounts\n</code></pre>"},{"location":"transforms/#assert","title":"<code>@assert</code>","text":"<p>Defines data-quality assertions evaluated after the model builds. Repeat one line per assertion:</p> <pre><code>@assert row_count &gt; 0\n@assert unique(customer_id)\n@assert no_nulls(email)\n@assert accepted_values(status, ['active', 'inactive'])\n@assert \"total_amount &gt;= 0\"\n</code></pre> <p>See Quality for the full assertion reference.</p>"},{"location":"transforms/#legacy-comment-syntax-still-supported","title":"Legacy comment syntax (still supported)","text":"<p>Older projects use SQL-comment-prefixed directives. They still parse correctly, so you can mix and match while migrating:</p> <pre><code>-- config: materialized=table, schema=silver\n-- depends_on: bronze.customers\n-- assert: row_count &gt; 0\n</code></pre> <p>New code should use the <code>@</code>-prefixed form.</p>"},{"location":"transforms/#change-detection","title":"Change Detection","text":"<p>havn uses SHA256 hashing to detect when a model's SQL has changed. On each <code>havn transform</code> run:</p> <ol> <li>The SQL content is normalized (whitespace-insensitive)</li> <li>A SHA256 hash is computed from the normalized SQL</li> <li>The hash is compared against the stored hash in <code>_havn.model_state</code></li> <li>If the hash matches and the combined hash of all transitive upstreams hasn't changed, the model is skipped</li> <li>Otherwise the model is rebuilt</li> </ol> <p>Most <code>havn transform</code> runs only rebuild what has actually changed, making iterative development fast.</p>"},{"location":"transforms/#dag-ordering","title":"DAG Ordering","text":"<p>Models are automatically sorted in topological order based on their dependencies (auto-extracted from SQL or declared via <code>@depends_on</code>). This ensures upstream tables exist before downstream models try to read from them.</p> <pre><code>bronze.customers \u2500\u2500\u2510\n                   \u251c\u2500\u2500&gt; silver.dim_customer \u2500\u2500&gt; gold.customer_summary\nbronze.orders \u2500\u2500\u2500\u2500\u2500\u2518\n</code></pre> <p>If a circular dependency is detected, <code>havn transform</code> will fail with an error. Use <code>havn validate</code> to check for circular dependencies without running transforms.</p>"},{"location":"transforms/#running-transforms","title":"Running Transforms","text":""},{"location":"transforms/#build-all-models","title":"Build All Models","text":"<pre><code>havn transform\n</code></pre> <p>Only rebuilds models whose SQL has changed or whose upstream dependencies were rebuilt.</p>"},{"location":"transforms/#force-rebuild-everything","title":"Force Rebuild Everything","text":"<pre><code>havn transform --force\n</code></pre> <p>Ignores change detection and rebuilds all models.</p>"},{"location":"transforms/#build-specific-models","title":"Build Specific Models","text":"<pre><code>havn transform gold.customer_summary silver.dim_customer\n</code></pre> <p>Builds only the specified models (and their upstream dependencies if needed).</p>"},{"location":"transforms/#parallel-execution","title":"Parallel Execution","text":"<pre><code>havn transform --parallel --workers 4\n</code></pre> <p>Runs independent models concurrently. Models at the same level in the DAG (no dependencies between them) execute in parallel.</p>"},{"location":"transforms/#environment-override","title":"Environment Override","text":"<pre><code>havn transform --env prod\n</code></pre> <p>Uses the database path and settings from the <code>prod</code> environment. See Environments.</p>"},{"location":"transforms/#materialization","title":"Materialization","text":""},{"location":"transforms/#view-default","title":"View (Default)","text":"<pre><code>@config materialized=view\n</code></pre> <p>Creates a view using <code>CREATE OR REPLACE VIEW ... AS SELECT ...</code>. The query runs on read, so data is always current but queries may be slower for complex logic.</p>"},{"location":"transforms/#table","title":"Table","text":"<pre><code>@config materialized=table\n</code></pre> <p>Creates a persistent table using <code>CREATE OR REPLACE TABLE ... AS SELECT ...</code>. Data is stored on disk and queries are fast.</p>"},{"location":"transforms/#incremental","title":"Incremental","text":"<pre><code>@config materialized=incremental, unique_key=event_id, incremental_strategy=delete+insert\n</code></pre> <p>Builds the table incrementally: on first run, it materialises the full result; on subsequent runs, only new rows (filtered by <code>incremental_filter</code> if provided) are appended or merged. See <code>incremental_strategy</code> in the <code>@config</code> table above.</p>"},{"location":"transforms/#plain-sql-no-templating","title":"Plain SQL -- No Templating","text":"<p>havn uses plain SQL with no Jinja, no macros, and no templating language. This means:</p> <ul> <li>SQL files work directly in any DuckDB client (just delete the <code>@config</code> line first)</li> <li>No learning curve beyond standard SQL</li> <li>Full DuckDB syntax support (window functions, CTEs, UNNEST, etc.)</li> <li>Easy to test and debug</li> </ul> <p>If you need dynamic behavior, use Python ingest/export scripts or parameterize via environment variables in <code>project.yml</code>. For reusable scalar logic, register a Python macro and call it directly in SQL -- see Macros.</p>"},{"location":"transforms/#validation","title":"Validation","text":"<p>Check your models for errors without running them:</p> <pre><code>havn check\n</code></pre> <p>This validates: - SQL syntax (via sqlglot AST parsing) - Auto-extracted and explicit <code>@depends_on</code> references resolve to known models, seeds, or sources - Column references against known upstream table schemas - Inline assertions against live data (if warehouse exists) - YAML contracts from <code>contracts/</code></p>"},{"location":"transforms/#related-pages","title":"Related Pages","text":"<ul> <li>Pipelines -- Run transforms as part of multi-step streams</li> <li>Quality -- Data quality assertions and profiling</li> <li>Lineage -- Column-level lineage and impact analysis</li> <li>Seeds -- Load CSV reference data</li> <li>CLI Reference -- Full command reference for <code>havn transform</code></li> </ul>"},{"location":"versioning/","title":"Versioning and Time Travel","text":"<p>havn provides two versioning systems: snapshots for lightweight project state comparison, and versions for full data time travel with Parquet-backed table recovery.</p>"},{"location":"versioning/#snapshots","title":"Snapshots","text":"<p>Snapshots capture a fingerprint of the current project and data state. They are fast to create and useful for comparing changes over time.</p>"},{"location":"versioning/#creating-snapshots","title":"Creating Snapshots","text":"<pre><code>havn snapshot\n</code></pre> <p>Or with a custom name:</p> <pre><code>havn snapshot --name before-refactor\n</code></pre> <p>Via API:</p> <pre><code>curl -X POST http://localhost:3000/api/snapshot \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\"name\": \"before-refactor\"}'\n</code></pre>"},{"location":"versioning/#what-snapshots-capture","title":"What Snapshots Capture","text":"<ul> <li>File manifest -- SHA256 hashes of all files in <code>transform/</code>, <code>ingest/</code>, <code>export/</code>, and <code>project.yml</code></li> <li>Table signatures -- Row counts and column schema hashes for every user table</li> <li>Project hash -- Combined hash of all project files</li> </ul> <p>Snapshots do not store actual data -- they are metadata-only.</p>"},{"location":"versioning/#listing-snapshots","title":"Listing Snapshots","text":"<pre><code># Via API\ncurl http://localhost:3000/api/snapshots\n</code></pre>"},{"location":"versioning/#comparing-against-a-snapshot","title":"Comparing Against a Snapshot","text":"<pre><code>havn diff --snapshot before-refactor\n</code></pre> <p>Shows:</p> <pre><code>Snapshot: before-refactor\nCreated: 2025-01-15 06:00:00\n\nFile changes:\n  + transform/gold/new_model.sql\n  ~ transform/silver/dim_customer.sql\n  - transform/bronze/old_cleanup.sql\n\nTable Changes\n  Table                    Status     Before Rows   After Rows\n  gold.new_model          added                        1,234\n  silver.dim_customer     modified        5,000         5,432\n  bronze.old_cleanup      removed         2,100\n</code></pre>"},{"location":"versioning/#deleting-snapshots","title":"Deleting Snapshots","text":"<pre><code># Via API\ncurl -X DELETE http://localhost:3000/api/snapshots/before-refactor\n</code></pre>"},{"location":"versioning/#versions-time-travel","title":"Versions (Time Travel)","text":"<p>Versions create full Parquet snapshots of table data, enabling point-in-time recovery and data comparison.</p>"},{"location":"versioning/#creating-versions","title":"Creating Versions","text":"<pre><code># Via API\ncurl -X POST http://localhost:3000/api/versions \\\n  -H \"Content-Type: application/json\"\n</code></pre> <p>Versions are stored in the <code>_snapshots/</code> directory:</p> <pre><code>_snapshots/\n  v1-20250115-060000/\n    gold.customers.parquet\n    gold.orders.parquet\n    _manifest.json\n  v2-20250116-060000/\n    ...\n</code></pre>"},{"location":"versioning/#version-triggers","title":"Version Triggers","text":"<p>Versions can be created:</p> <ul> <li>Manually -- Via API or CLI</li> <li>Before restores -- Auto-snapshot created before any restore operation</li> <li>After transforms -- Optionally snapshot after successful pipeline runs</li> </ul>"},{"location":"versioning/#listing-versions","title":"Listing Versions","text":"<pre><code># Via API\ncurl http://localhost:3000/api/versions\n</code></pre> <p>Returns:</p> <pre><code>[\n  {\n    \"version_id\": \"v2-20250116-060000\",\n    \"created_at\": \"2025-01-16 06:00:00\",\n    \"description\": \"\",\n    \"trigger\": \"manual\",\n    \"table_count\": 8,\n    \"total_rows\": 45678\n  }\n]\n</code></pre>"},{"location":"versioning/#version-details","title":"Version Details","text":"<pre><code>curl http://localhost:3000/api/versions/v2-20250116-060000\n</code></pre> <p>Returns detailed information including per-table row counts and column schemas.</p>"},{"location":"versioning/#diff-between-versions","title":"Diff Between Versions","text":"<p>Compare two versions or a version against current state:</p> <pre><code># Compare two versions\ncurl \"http://localhost:3000/api/versions/v1-20250115-060000/diff?to_version=v2-20250116-060000\"\n\n# Compare a version against current state\ncurl http://localhost:3000/api/versions/v1-20250115-060000/diff\n</code></pre> <p>Returns:</p> <pre><code>{\n  \"from_version\": \"v1-20250115-060000\",\n  \"to_version\": \"current\",\n  \"tables_compared\": 8,\n  \"changes\": [\n    {\n      \"table\": \"gold.customers\",\n      \"change\": \"modified\",\n      \"rows_before\": 5000,\n      \"rows_after\": 5432,\n      \"row_diff\": 432\n    },\n    {\n      \"table\": \"gold.new_report\",\n      \"change\": \"added\",\n      \"rows\": 1234\n    },\n    {\n      \"table\": \"gold.deprecated\",\n      \"change\": \"removed\",\n      \"rows_before\": 100\n    }\n  ]\n}\n</code></pre> <p>Changes include: - added -- Table exists in the \"to\" version but not the \"from\" version - removed -- Table exists in the \"from\" version but not the \"to\" version - modified -- Row count or schema changed between versions - Schema changes -- Columns added or removed between versions</p>"},{"location":"versioning/#restoring-versions","title":"Restoring Versions","text":"<p>Restore tables from a version snapshot:</p> <pre><code>curl -X POST http://localhost:3000/api/versions/v1-20250115-060000/restore\n</code></pre> <p>The restore process:</p> <ol> <li>Auto-snapshots current state -- Creates a version before restore (trigger: <code>restore</code>)</li> <li>Reads Parquet files -- Loads each table from the version's Parquet snapshots</li> <li>Replaces current tables -- Uses <code>CREATE OR REPLACE TABLE</code> from Parquet data</li> <li>Reports results -- Returns per-table status (success/error/skipped)</li> </ol>"},{"location":"versioning/#selective-restore","title":"Selective Restore","text":"<p>Restore specific tables by specifying a table list in the request body.</p>"},{"location":"versioning/#path-safety","title":"Path Safety","text":"<p>Parquet file paths are validated to prevent path traversal attacks. Files must reside within the project's <code>_snapshots/</code> directory.</p>"},{"location":"versioning/#table-timeline","title":"Table Timeline","text":"<p>View the history of a specific table across all versions:</p> <pre><code>curl http://localhost:3000/api/versions/timeline/gold.customers\n</code></pre> <p>Returns:</p> <pre><code>[\n  {\n    \"version_id\": \"v3-20250117-060000\",\n    \"created_at\": \"2025-01-17 06:00:00\",\n    \"row_count\": 5500,\n    \"columns\": [\n      {\"name\": \"customer_id\", \"type\": \"INTEGER\"},\n      {\"name\": \"name\", \"type\": \"VARCHAR\"}\n    ]\n  },\n  {\n    \"version_id\": \"v2-20250116-060000\",\n    \"created_at\": \"2025-01-16 06:00:00\",\n    \"row_count\": 5432,\n    \"columns\": [...]\n  }\n]\n</code></pre>"},{"location":"versioning/#cleanup","title":"Cleanup","text":"<p>Remove old versions to save disk space:</p> <pre><code># Keep only the 10 most recent versions (via API or engine function)\n</code></pre> <p>The cleanup function: - Deletes Parquet files from <code>_snapshots/</code> - Removes metadata from <code>_havn.version_history</code> - Validates paths before deletion (safety)</p>"},{"location":"versioning/#model-level-diff","title":"Model-Level Diff","text":"<p>In addition to version-based diff, havn supports model-level diff that compares the current SQL output against the materialized table:</p> <pre><code>havn diff\n</code></pre> <p>This re-executes each model's SQL and compares the result against the existing table:</p> <pre><code>Diff Summary\n  Model                    Before   After   Added   Removed   Modified   Schema\n  gold.customer_summary     5,000   5,432    +432         0          0     --\n  silver.dim_customer       3,200   3,200       0         0         15     --\n</code></pre>"},{"location":"versioning/#git-aware-diff","title":"Git-Aware Diff","text":"<p>Compare only models whose SQL files changed relative to a git branch:</p> <pre><code>havn diff --against main\n</code></pre> <p>This finds SQL files that differ between HEAD and <code>main</code>, then diffs only those models.</p>"},{"location":"versioning/#backup-and-restore","title":"Backup and Restore","text":"<p>havn also provides database-level backup and restore:</p> <pre><code># Create a backup\nhavn backup\n\n# Restore from a backup\nhavn restore warehouse.duckdb.backup_20250115_060000\n</code></pre> <p>Backups are full copies of the DuckDB file. The backup command flushes the WAL (Write-Ahead Log) first to ensure consistency.</p>"},{"location":"versioning/#related-pages","title":"Related Pages","text":"<ul> <li>Transforms -- SQL models and change detection</li> <li>Pipelines -- Running transforms</li> <li>CLI Reference -- Diff, snapshot, and version commands</li> <li>API Reference -- Versioning API endpoints</li> </ul>"},{"location":"internal/ideas-catalog/","title":"havn Ideas Catalog","text":"<p>Compiled 2026-03-20. Six-agent deep analysis of the full codebase: backend engine, connectors/CLI, server/API, tests/docs, UI design, and UI edge cases.</p>"},{"location":"internal/ideas-catalog/#how-to-read-this","title":"How to read this","text":"<ul> <li>Priority: HIGH = blocking or high-value, MEDIUM = important but not urgent, LOW = nice-to-have</li> <li>Effort: S = hours, M = days, L = weeks</li> <li>Items marked with a star are quick wins (high impact, low effort)</li> </ul>"},{"location":"internal/ideas-catalog/#1-backend-engine","title":"1. Backend Engine","text":""},{"location":"internal/ideas-catalog/#transform-engine","title":"Transform Engine","text":"# Idea Priority Effort Notes 1.1 Incremental config validation at discovery time (require unique_key for merge/delete+insert, validate partition_by columns exist) HIGH M Currently only fails at runtime 1.2 Execution step logging &amp; replay (<code>havn replay &lt;model&gt;</code> with per-SQL-step timing) HIGH M Hard to debug parallel failures today 1.3 Partition-by auto-detection (suggest partition columns from GROUP BY/ORDER BY heuristics) HIGH M Reduces config burden 1.4 Schema evolution audit trail (track column additions in audit_log, add schema_hash to model_state) HIGH M No rollback path today 1.5 Model-level retry with backoff for transforms (transient lock/contention failures) MEDIUM M Circuit breaker exists for scripts but not transforms 1.6 Soft delete incremental strategy (flag deleted rows instead of hard delete, with purge CLI) MEDIUM M Needed for CDC sources 1.7 Model execution timing breakdown (t_plan, t_staging, t_merge, t_insert per model) MEDIUM S Currently just total duration 1.8 Column lineage caching in metadata table (invalidate on upstream content_hash change) MEDIUM S Full lineage recompute is slow on 100+ models"},{"location":"internal/ideas-catalog/#diff-engine","title":"Diff Engine","text":"# Idea Priority Effort Notes 1.9 3-mode diff: single model, changed+downstream, full database HIGH M In progress \u2014 being built now 1.10 Dry-run / what-if mode (<code>havn transform --dry-run &lt;model&gt;</code> \u2014 diff without persisting) MEDIUM S"},{"location":"internal/ideas-catalog/#snapshots-rewind","title":"Snapshots &amp; Rewind","text":"# Idea Priority Effort Notes 1.11 Point-in-time restore by timestamp (<code>havn rewind --to \"2h ago\"</code>) HIGH M Currently requires knowing exact run_id 1.12 Incremental/delta snapshots (store only changed rows for incremental models, ~70% storage savings) HIGH M Full parquet per snapshot is wasteful 1.13 Snapshot diff preview before restore (show what restore would change) MEDIUM M 1.14 GC dashboard (track freed bytes, deletion history, storage growth chart) MEDIUM S"},{"location":"internal/ideas-catalog/#data-quality","title":"Data Quality","text":"# Idea Priority Effort Notes 1.15 Contract-based alerting with thresholds (<code>row_count &gt; {previous * 0.9}</code>, freshness escalation) HIGH M Binary pass/fail is too simple 1.16 Assertion debugging (on failure: show top 10 duplicates, actual values, sample null rows) HIGH M Currently just \"5 duplicates\" with no detail 1.17 Anomaly detection via statistical profiling (Z-score on row_count/null%/distinct% over N runs) HIGH M 1.18 Assertion sampling for large tables (TABLESAMPLE for non-exhaustive checks) MEDIUM S 1.19 Incremental assertion evaluation (skip assertions for unchanged models) MEDIUM M"},{"location":"internal/ideas-catalog/#masking-security","title":"Masking &amp; Security","text":"# Idea Priority Effort Notes 1.20 Masking policy versioning &amp; audit trail (who changed what, when, revert) HIGH M 1.21 Fine-grained role-based masking exemptions (analyst sees email but not credit_card) HIGH M Currently all-or-nothing per role 1.22 Masking performance: column-level lazy evaluation, pre-compiled mask plans MEDIUM M Slow on 1M rows with 50 policies"},{"location":"internal/ideas-catalog/#circuit-breaker-error-handling","title":"Circuit Breaker &amp; Error Handling","text":"# Idea Priority Effort Notes 1.23 Circuit breaker status UI in Observe section (state, failure count, recovery time, manual reset) MEDIUM M 1.24 Error categorization (transient vs permanent) with auto-recovery suggestions MEDIUM M"},{"location":"internal/ideas-catalog/#scheduling-orchestration","title":"Scheduling &amp; Orchestration","text":"# Idea Priority Effort Notes 1.25 Stream dependency ordering (cross-stream DAG with <code>depends_on: [other_stream]</code>) HIGH M 1.26 Schedule preview (<code>havn schedule --preview</code> \u2014 show next 10 runs with timestamps) HIGH M 1.27 Backfill with date range (<code>havn stream backfill &lt;stream&gt; --from --to</code>) HIGH M 1.28 File watcher debounce configuration (currently hardcoded 2s) MEDIUM S"},{"location":"internal/ideas-catalog/#2-connectors-cli","title":"2. Connectors &amp; CLI","text":""},{"location":"internal/ideas-catalog/#missing-connectors","title":"Missing Connectors","text":"# Idea Priority Effort Notes 2.1 Snowflake connector HIGH M Critical for competitive positioning / migration story 2.2 BigQuery connector HIGH M GCP trifecta 2.3 Redshift connector HIGH M AWS trifecta 2.4 MongoDB connector MEDIUM M Unstructured data gap 2.5 Salesforce connector MEDIUM M Alongside HubSpot 2.6 Airtable, Jira, Slack, GitHub/GitLab connectors LOW L Community value"},{"location":"internal/ideas-catalog/#connector-framework","title":"Connector Framework","text":"# Idea Priority Effort Notes 2.7 ParamSpec validation (regex, enums, min/max, cross-param checks) HIGH S Catches config errors early 2.8 Connector dry-run (<code>havn connect --dry-run</code> \u2014 preview generated script) MEDIUM S 2.9 Connector versioning &amp; upgrade (<code>havn connect --upgrade &lt;name&gt;</code>) MEDIUM M Scripts are frozen after generation today 2.10 Standardize incremental sync across all connectors (high-watermark pattern) MEDIUM M Only Postgres/MySQL have CDC today 2.11 Multi-table transaction safety (atomic sync option) MEDIUM S 2.12 Connector health dashboard (last sync, duration, failure rate per source) LOW M 2.13 Community connector registry (pip-installable connector packs) LOW L"},{"location":"internal/ideas-catalog/#cli-commands","title":"CLI Commands","text":"# Idea Priority Effort Notes 2.14 <code>havn env</code> command (list/use/create environments) HIGH S Currently repeating <code>--env</code> on every command 2.15 <code>havn sample &lt;table&gt;</code> (quick data preview without writing SQL) MEDIUM S 2.16 <code>havn export &lt;model&gt; --format csv/parquet</code> MEDIUM S 2.17 <code>havn model &lt;name&gt; --watch</code> (auto-rebuild on save with live feedback) MEDIUM M FileWatcher exists, just needs single-model mode 2.18 <code>havn repl</code> (interactive SQL shell against warehouse) MEDIUM M 2.19 <code>havn monitor --stream &lt;name&gt;</code> (live pipeline status in terminal) MEDIUM S 2.20 <code>havn test</code> (end-to-end: ingest sample data, transform, validate, export) MEDIUM M 2.21 <code>havn context</code> (dump project metadata for debugging/onboarding) LOW S 2.22 Better error messages with hints (\"No project.yml found\" \u2192 suggest <code>havn init</code>) MEDIUM S 2.23 Grouped output &amp; summary stats for large outputs (<code>--errors-only</code>, <code>--json</code>) MEDIUM S 2.24 <code>--profile</code> flag on major commands (per-step timing breakdown) HIGH S"},{"location":"internal/ideas-catalog/#configuration","title":"Configuration","text":"# Idea Priority Effort Notes 2.25 Config validation (cron expressions, retention duration format, stream step targets) HIGH M 2.26 Config inheritance for environments (deep merge base + env overrides) MEDIUM M Currently must duplicate entire connection block 2.27 Secret reference validation (check all <code>${VAR}</code> refs exist in .env) MEDIUM S 2.28 Per-stream config (timeout, retries, retry_delay, alert channels) MEDIUM S 2.29 Config versioning with migration logic for breaking changes LOW S 2.30 Project templates (<code>havn init --template ecommerce/saas</code>) MEDIUM S"},{"location":"internal/ideas-catalog/#3-server-api","title":"3. Server &amp; API","text":""},{"location":"internal/ideas-catalog/#api-design","title":"API Design","text":"# Idea Priority Effort Notes 3.1 Standardized response envelope (<code>{success, data, error: {code, message}}</code>) HIGH M Inconsistent formats across 150+ endpoints 3.2 Pagination for high-volume list endpoints (history, audit, rewind runs, table samples) HIGH M 3.3 Centralized request validation module (file paths, SQL identifiers, git refs, resource names) HIGH M Currently scattered, inconsistent 3.4 Request ID tracking (<code>X-Request-ID</code> header for correlation) MEDIUM M 3.5 Endpoint documentation (docstrings, example requests/responses on all routes) MEDIUM S FastAPI auto-generates OpenAPI but descriptions are sparse 3.6 Helpful error messages (\"Invalid identifier 'foo'\" \u2192 \"Must start with letter, contain only alphanumeric + underscore\") MEDIUM S"},{"location":"internal/ideas-catalog/#auth-security","title":"Auth &amp; Security","text":"# Idea Priority Effort Notes 3.7 Token refresh flow (short-lived access + long-lived refresh token) HIGH M Currently fixed 30-day tokens 3.8 Token metadata (device name, last used, IP address) MEDIUM M 3.9 Custom RBAC roles with fine-grained permissions MEDIUM M 3.10 Schema-level access control (role can query gold but not landing) MEDIUM M 3.11 Permission denial audit logging HIGH S 3.12 Secret rotation strategy (versioning, expiry dates, rotation reminders) MEDIUM M 3.13 SQL injection audit of dynamic SQL paths HIGH S"},{"location":"internal/ideas-catalog/#audit-observability","title":"Audit &amp; Observability","text":"# Idea Priority Effort Notes 3.14 Expand audit actions (auth_failed, permission_denied, connector_sync, masking_policy changes, agent file edits) HIGH S 3.15 Persistent pipeline event log (store SSE events to <code>_havn.pipeline_events</code>) MEDIUM M Lost on server restart today 3.16 Circuit breaker state change logging MEDIUM S 3.17 Webhook retry with exponential backoff (currently fire-and-forget) MEDIUM S"},{"location":"internal/ideas-catalog/#pipeline-execution","title":"Pipeline Execution","text":"# Idea Priority Effort Notes 3.18 Query cancellation via DuckDB API (cancel in-flight SQL for long transforms) HIGH M Cancel flag not always checked 3.19 Pipeline pause &amp; resume (pause after current step, resume from checkpoint) MEDIUM M 3.20 Ring buffer for pipeline events (prevent OOM on very long runs) MEDIUM S Currently unbounded list"},{"location":"internal/ideas-catalog/#collaboration","title":"Collaboration","text":"# Idea Priority Effort Notes 3.21 WebSocket heartbeat/ping mechanism HIGH M No reconnection on network blips 3.22 Session persistence to <code>_havn</code> (survive server restarts) MEDIUM L"},{"location":"internal/ideas-catalog/#alerting","title":"Alerting","text":"# Idea Priority Effort Notes 3.23 New alert types: circuit_breaker_open, schema_drift, slow_query, token_expiring MEDIUM S 3.24 Alert deduplication (skip if same alert sent in last 5 min) MEDIUM S 3.25 Email alert channel MEDIUM M Only Slack/webhook/log today 3.26 Alert health check endpoint (is Slack webhook still working?) MEDIUM S"},{"location":"internal/ideas-catalog/#missing-endpoints","title":"Missing Endpoints","text":"# Idea Priority Effort Notes 3.27 Pipeline dry-run (<code>POST /api/stream/{name}/dry-run</code>) MEDIUM M 3.28 Config validation endpoint (<code>POST /api/validate/config</code>) MEDIUM S 3.29 Audit log export (<code>GET /api/audit/export?format=csv</code>) MEDIUM S 3.30 User session management (list/force-logout active sessions) MEDIUM M"},{"location":"internal/ideas-catalog/#4-frontend-design-ux","title":"4. Frontend \u2014 Design &amp; UX","text":""},{"location":"internal/ideas-catalog/#critical-ship-blockers","title":"Critical (Ship Blockers)","text":"# Idea Priority Effort Notes 4.1 Accessibility: ARIA labels, roles, semantic landmarks across all components HIGH L Zero WCAG support today 4.2 Visible focus indicators for keyboard navigation HIGH S 4.3 Modal focus trapping (dialogs don't trap focus or block background interaction) HIGH M"},{"location":"internal/ideas-catalog/#moderate","title":"Moderate","text":"# Idea Priority Effort Notes 4.4 Consistent disabled button styling and hover states MEDIUM S Mix of inline handlers and missing states 4.5 Color contrast validation across all 8 themes MEDIUM S No WCAG AA checks 4.6 Reusable LoadingSpinner, EmptyState, ErrorState components MEDIUM M Different patterns everywhere 4.7 Scroll hints on long lists (shadow/gradient at bottom) MEDIUM S 4.8 Responsive design fixes (sidebar collapse, dialog overflow at &lt;1200px) MEDIUM M Desktop-only today 4.9 Actionable error messages (\"Failed to open: Error\" \u2192 explain + suggest fix) MEDIUM S 4.10 Search/filter in file tree, schema tree, table browser MEDIUM M No search on large lists 4.11 Consistent spacing system (CSS variables instead of hardcoded px values) MEDIUM M 4.12 Viewport-aware positioning for dropdowns and tooltips MEDIUM S PipelineMenu, GuideTour can overflow"},{"location":"internal/ideas-catalog/#minor-polish","title":"Minor Polish","text":"# Idea Priority Effort Notes 4.13 Tooltips for truncated table cells LOW S 4.14 Proper file type icons (replace colored dots and \"V\"/\"T\" letters) LOW S 4.15 Consistent date/time formatting (relative time everywhere, absolute on hover) LOW S 4.16 Breadcrumb / back navigation for deep views LOW S 4.17 Standardize button text casing (mix of \"Run\", \"LINT\", \"Format SQL\") LOW S 4.18 DAG node dynamic sizing for long model names LOW S Fixed 160px truncates 4.19 Colorblind-safe status indicators (icons + text, not just red/green) LOW S 4.20 Dark mode <code>&lt;meta name=\"color-scheme\"&gt;</code> tag LOW S"},{"location":"internal/ideas-catalog/#5-frontend-edge-cases-bugs","title":"5. Frontend \u2014 Edge Cases &amp; Bugs","text":""},{"location":"internal/ideas-catalog/#critical","title":"Critical","text":"# Idea Priority Effort Notes 5.1 SSE stream reconnection with backoff (server restart mid-pipeline = silent hang) HIGH M Most critical frontend bug 5.2 API error handling: retry logic, human-readable errors instead of raw HTML HIGH M 5.3 File edit conflict detection (etag/version check before save, agent vs user) HIGH M 5.4 Monaco size limit check (warn &gt;10MB, refuse &gt;50MB) MEDIUM S"},{"location":"internal/ideas-catalog/#moderate_1","title":"Moderate","text":"# Idea Priority Effort Notes 5.5 Autocomplete debounce (fires on every keystroke, no debounce) MEDIUM S 5.6 Schema cache expiry (global Map with no TTL, stale completions) MEDIUM S 5.7 Git panel merge conflict detection and display MEDIUM M Pull succeeds but conflicts invisible 5.8 Git branch name validation before creation MEDIUM S 5.9 Rewind timeline virtualization (5000 runs = 250K snapshots in state) MEDIUM M 5.10 Prevent multiple dialogs opening simultaneously (single dialog queue) MEDIUM S 5.11 Unsaved changes warning before file delete MEDIUM S 5.12 WarehouseContext: show error when initial load fails (tables/files/streams) MEDIUM S Fails silently, shows blank 5.13 sessionStorage quota handling (try-catch on writes, LRU eviction) MEDIUM S 5.14 FileTree memoization and virtualization for large projects MEDIUM M Re-sorts on every render 5.15 OutputPanel regex link detection improvements (Windows paths, dashes in names) MEDIUM S 5.16 WebSocket cleanup on sidebar close / page unload MEDIUM S 5.17 Autocomplete dropdown viewport clamping LOW S 5.18 DAG layout cycle handling (circular deps = corrupted visualization) LOW M 5.19 Query history deduplication and bounded growth LOW S"},{"location":"internal/ideas-catalog/#6-testing-code-quality","title":"6. Testing &amp; Code Quality","text":"# Idea Priority Effort Notes 6.1 Custom exception hierarchy (<code>HavnError</code>, <code>TransformError</code>, <code>ConnectorError</code>, etc.) HIGH M 83 bare <code>except Exception:</code> blocks 6.2 Performance benchmarks with regression detection (pytest-benchmark) HIGH M No guardrails against perf regressions 6.3 Chaos/failure injection tests (connection loss mid-pipeline, timeouts, corrupted snapshots) HIGH M 6.4 Structured logging with correlation IDs (JSON format, run_id tracing) MEDIUM M 123 logging calls, no structure 6.5 Centralized input validation module (Pydantic models for identifiers, paths, cron) MEDIUM M Duplicated across modules 6.6 Shared test fixtures in conftest.py (sample_project, complex_dag) MEDIUM M Repeated setup across 40 files 6.7 Windows-specific CI tests (file locking, path separators, CRLF) MEDIUM M 6.8 Integration tests with real external services (Postgres, MySQL via docker-compose) MEDIUM L All tests use local DuckDB only"},{"location":"internal/ideas-catalog/#7-new-feature-ideas","title":"7. New Feature Ideas","text":"# Idea Priority Effort Notes 7.1 Query plan visualization in web UI (EXPLAIN ANALYZE with operator tree) HIGH M DuckDB generates detailed plans, UI doesn't show them 7.2 Model validation report UI (run all checks before pipeline, show fixes) MEDIUM M 7.3 Column ownership &amp; CODEOWNERS-style docs per model/column MEDIUM M 7.4 Script parameter passing (<code>havn run ingest/fetch.py --param source=prod</code>) HIGH M Currently all hardcoded 7.5 Structured script output protocol (JSON metrics from scripts, not just regex) HIGH M 7.6 CDC watermark management UI (inspect, reset, sync history per connector) HIGH M Currently requires raw SQL to fix 7.7 Multi-project workspace support (multiple warehouses in one server) MEDIUM L 7.8 OpenTelemetry distributed tracing (optional) LOW M 7.9 Health check &amp; Prometheus metrics endpoints MEDIUM S 7.10 Architecture Decision Records (ADRs) in <code>docs/adr/</code> MEDIUM S"},{"location":"internal/ideas-catalog/#quick-wins-summary","title":"Quick Wins Summary","text":"<p>High impact, low effort \u2014 do these first:</p> # Idea Est. 2.7 Connector ParamSpec validation hours 2.14 <code>havn env</code> command hours 2.24 <code>--profile</code> flag on major commands hours 3.14 Expand audit log actions hours 3.6 Helpful API error messages hours 3.13 SQL injection audit of dynamic SQL hours 4.2 Visible focus indicators hours 5.5 Autocomplete debounce hours 5.8 Git branch name validation hours 5.11 Unsaved changes warning before delete hours"},{"location":"internal/ideas-catalog/#strategic-priorities","title":"Strategic Priorities","text":""},{"location":"internal/ideas-catalog/#for-competitive-positioning-vs-databrickssnowflake","title":"For competitive positioning (vs Databricks/Snowflake)","text":"<ul> <li>Snowflake + BigQuery + Redshift connectors (2.1\u20132.3)</li> <li>Query plan visualization (7.1)</li> <li>Anomaly detection (1.17)</li> </ul>"},{"location":"internal/ideas-catalog/#for-enterprise-readiness","title":"For enterprise readiness","text":"<ul> <li>Token refresh flow (3.7)</li> <li>Custom RBAC (3.9)</li> <li>Audit completeness (3.14)</li> <li>Masking policy versioning (1.20)</li> </ul>"},{"location":"internal/ideas-catalog/#for-developer-experience","title":"For developer experience","text":"<ul> <li><code>havn env</code> (2.14), <code>havn sample</code> (2.15), <code>havn export</code> (2.16)</li> <li>Watch mode (2.17)</li> <li>Better error messages (2.22, 3.6)</li> <li>Assertion debugging (1.16)</li> </ul>"},{"location":"internal/ideas-catalog/#for-production-reliability","title":"For production reliability","text":"<ul> <li>SSE reconnection (5.1)</li> <li>Chaos tests (6.3)</li> <li>Circuit breaker UI (1.23)</li> <li>Pipeline pause/resume (3.19)</li> </ul> <p>Generated by 6 parallel analysis agents scanning the full havn codebase.</p>"},{"location":"internal/to-do/","title":"havn \u2014 To-Do","text":""},{"location":"internal/to-do/#uxui-sweep-2026-06-09-high-value-fixed-rest-open","title":"UX/UI sweep (2026-06-09) \u2014 high-value fixed, rest open","text":"<p>Multi-agent UX + accessibility review (static + live Playwright). Fixed and committed: stuck \"Run Sample Pipeline\" button; NewModelDialog/ModelNotebookView theming; FocusTrap + Orchestration modal Escape; native alert() removal; DataSources keyboard-operable cards; showDocs leak; progress-bar/logo theming; run-status-dot a11y.</p> <p>Remaining (lower priority, not yet fixed): - [x] Disabled action buttons give no reason/tooltip \u2014 fixed 2026-06-13:   App Run/Run Model titles explain \"a run is in progress\"; Masking   Add/Edit/Delete titles explain \"finish the open form first\". - [x] Number inputs in Orchestration interval/range silently coerce invalid   values \u2014 fixed 2026-06-12: raw value while typing, clamp on blur, and the   cron builder / job save clamp again so NaN can't leak into schedules. - [x] DataSources \"Skip Test\"/Continue advances with empty required fields \u2014   fixed 2026-06-13: both Test Connection and Skip Test now validate the   connection name + required params and show an inline error instead of   advancing to a connector that can't connect. - [ ] DataSources file upload: no progress/loading indicator during upload. - [x] QueryPanel \"Query this table\" auto-run limit bypass \u2014 verified stale   2026-06-12: the 0.2.22 query editor rework routes auto-run through   runQuery(), which applies the limit. - [x] WikiPanel fetch failure / raw <code>&lt;</code> \u2014 fixed 2026-06-12: prose is   HTML-escaped before dangerouslySetInnerHTML (code blocks extracted first),   and fetch failures render a styled error with a Retry button. - [ ] Dashboard/Widget/Filter modals aren't FocusTrap-wrapped (no Escape/focus   mgmt); their close <code>\u00d7</code> buttons lack aria-label. - [ ] SortableTable headers / column-menu / pin items are mouse-only (no   tabIndex/keydown); column-type indicator relies on color (add title). - [ ] NotebookPanel save has no dirty/saved indicator. - [ ] MaskingPanel inline \"Add Policy\" form isn't scrolled into view on open. - [ ] FileTree per-file delete (\u00d7) and dir \"+\" are hover-only (not focusable). - [ ] Color-only status in DiffPanel / connector dots (add glyph/text). - [ ] CommandPalette results lack listbox/option roles + aria-activedescendant. - [x] GitPanel hardcoded button text color (#0B0E14) \u2014 fixed 2026-06-13:   the commit button now uses #fff on the accent like every sibling button,   so it no longer vanishes on dark-accent themes.</p>"},{"location":"internal/to-do/#issue-hunt-sweep-2026-06-09-all-resolved","title":"Issue-hunt sweep (2026-06-09) \u2014 all resolved","text":"<p>Multi-agent review (Playwright + code review, adversarially verified). All findings have been fixed and committed:</p> <ul> <li>[x] P1 path traversal in the SPA catch-all \u2014 containment check added.</li> <li>[x] P1 notebook sandbox escape \u2014 closed via <code>guard_sql_calls()</code> AST   rewrite (<code>__havn_sql_guard</code> validates every <code>.execute/.sql/.query</code> argument at   runtime regardless of aliasing/dynamic SQL, while keeping the call direct so   DuckDB's DataFrame replacement scan still works). Regression tests added.</li> <li>[x] Cancel-callback memory leak in <code>resource_manager._finish_task</code>.</li> <li>[x] Stale <code>_havn.model_state</code> on file delete (wrong column name).</li> <li>[x] Empty USGS feed aborting the starter pipeline.</li> <li>[x] Misleading <code>ModuleNotFoundError</code> install hints (<code>deps.py</code>).</li> <li>[x] DAGPanel redraw churn (shared <code>EMPTY</code> constant).</li> <li>[x] Query Cancel button no-op \u2014 AbortController wired through <code>api.runQuery</code>.</li> <li>[x] Wiki cross-reference links + \"Open in New Tab\" (in-place nav, no JSON tab).</li> <li>[x] RunSummary success card auto-dismisses after 12s.</li> <li>[x] TablesPanel catalog-qualified name parsing (<code>splitTableName</code>).</li> <li>[x] QueryPanel <code>extractAliases</code> mis-parse + autocomplete async race.</li> <li>[x] SQL cell <code>total_rows</code> mislabel (renamed to <code>displayed_rows</code>).</li> <li>[x] <code>notebooks.py</code> identifier validation moved to top of each handler.</li> <li>[x] Incremental staging-column query constrained to the temp catalog.</li> </ul>"},{"location":"internal/to-do/#cloud-hosted-version","title":"Cloud / Hosted Version","text":""},{"location":"internal/to-do/#database-abstraction-layer-databasepy","title":"Database Abstraction Layer (<code>database.py</code>)","text":"<ul> <li>[ ] Design a backend interface that supports both direct DuckDB (self-hosted) and DuckLake (cloud)</li> <li>[ ] Sketch out the abstraction: <code>connect()</code>, <code>execute()</code>, <code>cursor()</code> should work the same regardless of backend</li> <li>[ ] DuckLake backend: catalog in Postgres (schema-per-tenant or row-level), data as Parquet in S3/R2</li> <li>[ ] Keep all engine code (transforms, DAG, quality, connectors) backend-agnostic \u2014 they just call the abstraction</li> <li>[ ] \"Download as .duckdb\" export feature \u2014 reads from DuckLake, writes single file for local use</li> <li>[ ] Zero lock-in pitch: user can leave cloud and run self-hosted anytime with one export</li> </ul>"},{"location":"internal/to-do/#multi-user-concurrency","title":"Multi-User / Concurrency","text":"<ul> <li>[ ] DuckLake enables real concurrent read/write from multiple DuckDB workers</li> <li>[ ] Watch DuckLake maturity (v0.3 now, 1.0 expected early 2026) \u2014 concurrent write issues (#233, #243) still being fixed</li> <li>[ ] Team/org model: upgrade existing auth.py with org membership, roles, SSO (SAML/OIDC)</li> </ul>"},{"location":"internal/to-do/#infrastructure","title":"Infrastructure","text":"<ul> <li>[ ] Container-per-tenant on Fly.io or K8s for initial hosted version</li> <li>[ ] Control plane service: tenant provisioning, billing (Stripe), DNS routing</li> <li>[ ] Gateway: route <code>{tenant}.havn.dev</code> to correct container/worker</li> <li>[ ] Automated backups: Parquet files in S3 = naturally durable, add scheduled catalog snapshots</li> <li>[ ] Consider Fly.io Machines for scale-to-zero (ETL workloads are bursty)</li> </ul>"},{"location":"internal/to-do/#presentations","title":"Presentations","text":"<ul> <li>[x] Create presentations folder with original deck</li> <li>[x] Data Engineering deck (9 slides \u2014 architecture, transform engine, tradeoffs)</li> <li>[x] VC / Investors deck (8 slides \u2014 market, business model, GTM, roadmap)</li> <li>[x] Open-Source Community deck (8 slides \u2014 getting started, features, contributing)</li> <li>[x] All exported as PDF</li> <li>[ ] Review decks in PowerPoint and refine any text/layout issues</li> <li>[ ] Add web UI screenshots to decks</li> <li>[ ] Add real traction metrics when available</li> </ul>"},{"location":"internal/to-do/#python-sql-macros-udfs","title":"Python SQL Macros (UDFs)","text":"<p>Replace Jinja with Python functions callable directly in SQL. \"Why template SQL when you have Python right there?\"</p>"},{"location":"internal/to-do/#approach-duckdb-scalar-udfs","title":"Approach: DuckDB scalar UDFs","text":"<ul> <li>[x] <code>macros/</code> directory convention: Python files with decorated functions</li> <li>[x] Auto-discover and register UDFs at connection time (<code>database.py</code> or <code>connect()</code>)</li> <li>[x] Decorator API: <code>@macro</code> decorator, importable as <code>from havn import macro</code></li> <li>[x] DuckDB <code>create_function()</code> for scalar UDFs (str, int, float, bool, date, datetime)</li> <li>[x] SQL macros via <code>CREATE MACRO</code> in <code>.sql</code> files in <code>macros/</code></li> <li>[x] CLI: <code>havn macros</code> to list registered UDFs with signatures</li> <li>[x] API: <code>GET /api/macros</code> endpoint for editor autocomplete</li> <li>[x] Scaffold: <code>havn init</code> creates <code>macros/</code> with example <code>utils.py</code></li> <li>[ ] Support table-returning UDFs via <code>duckdb.create_function(..., type=\"table\")</code> for reusable CTEs</li> <li>[ ] Hot-reload: file watcher re-registers UDFs when macros/ changes</li> <li>[ ] Web UI: show available macros in editor autocomplete + hover docs</li> <li>[ ] Aggregate UDFs for custom rollups</li> <li>[ ] Share macro packs (pip-installable macro libraries?)</li> </ul>"},{"location":"internal/to-do/#shipped-2026-03-20","title":"Shipped (2026-03-20)","text":""},{"location":"internal/to-do/#3-mode-diff-system","title":"3-Mode Diff System","text":"<ul> <li>[x] Single model diff (<code>havn diff gold.orders</code>)</li> <li>[x] Changed + downstream diff (<code>havn diff --changed</code>, smart default)</li> <li>[x] Full database diff (<code>havn diff --all</code>, explicit opt-in)</li> <li>[x] Frontend: mode selector, model autocomplete, skipped model toggle</li> </ul>"},{"location":"internal/to-do/#sse-stream-reconnection","title":"SSE Stream Reconnection","text":"<ul> <li>[x] Exponential backoff (1s\u219216s) with jitter, max 10 retries</li> <li>[x] Heartbeat timeout (30s), resume from last event index</li> <li>[x] \"Reconnecting...\" / \"Connection lost\" banners in OutputPanel</li> </ul>"},{"location":"internal/to-do/#assertion-debugging","title":"Assertion Debugging","text":"<ul> <li>[x] Diagnostic details on failure (duplicated values, null samples, unexpected values)</li> <li>[x] <code>GET /api/quality/assertion-debug/{model}</code> endpoint</li> <li>[x] \"Re-run with diagnostics\" button in QualityPanel</li> </ul>"},{"location":"internal/to-do/#havn-env-command","title":"<code>havn env</code> Command","text":"<ul> <li>[x] <code>havn env list/use/show/reset</code> with <code>.havn-env</code> file</li> <li>[x] Config loading respects <code>.havn-env</code>, <code>--env</code> flag overrides</li> </ul>"},{"location":"internal/to-do/#query-plan-visualization","title":"Query Plan Visualization","text":"<ul> <li>[x] <code>explain.py</code> module with DuckDB JSON EXPLAIN parsing</li> <li>[x] <code>POST /api/query/explain-analyze</code> endpoint</li> <li>[x] ExplainPanel with collapsible tree, color-coded operators, timing bars</li> <li>[x] \"Explain Analyze\" button + \"Plan\" tab in QueryPanel</li> </ul>"},{"location":"internal/to-do/#file-edit-conflict-detection","title":"File Edit Conflict Detection","text":"<ul> <li>[x] SHA256 file hash on read (ETag header), 409 Conflict on stale save</li> <li>[x] Conflict dialog: Cancel / Load latest / Overwrite</li> </ul>"},{"location":"internal/to-do/#agent-onboarding","title":"Agent Onboarding","text":"<ul> <li>[x] Improve CLAUDE.md and project documentation for fast agent onboarding</li> <li>[x] Root CLAUDE.md with full project structure, conventions, common tasks</li> <li>[x] .claude/CLAUDE.md with dev-specific instructions, feature map, architecture decisions</li> <li>[x] .claude/rules/ with auto-loading context per module (engine.md, etc.)</li> </ul>"}]}