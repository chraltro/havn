# Reset OS targeting so npm auto-detects the current platform.
# Without this, a user-level ~/.npmrc with `os=linux` (common after Docker/WSL setups)
# will cause rollup native binaries to not be installed on Windows, breaking the build.
os=""
